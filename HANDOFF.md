# Handoff — 현재 진행 상황

**2026-08-16.** 첫 바퀴 검토를 시작해 **672장(29.0%)** 을 봤다. 그러면서
**스키마 두 자리가 자료에 반증되어 뒤집혔고**, 검토 화면이 크게 늘었다.

지나간 것은 `devlog/20260816_001`(첫 바퀴) · `_002`(스키마를 뒤집은 이유) ·
`_003`(조작을 고친 것), 앞으로 할 일은 `TODOs.md`.

## 지금 있는 것

| | |
|---|---|
| 표본 | 관찰일 20일 (2016-03-15 ~ 2019-11-05) · 상자 2,315 · 사진 1,909 |
| 크롭 | 640×640 · 154MB · `crops/` |
| 마스크 | 2,315개 — 상자마다 하나씩. SAM2.1 · 2080ti · run 5 |
| **검토** | **672 / 2,315 = 29.0%** (판정 717건) — 관찰일 6일치 |
| **밑동 라벨** | **500** (키포인트 문턱 300 넘음) |
| 사진 원본 | **붙었다** — `/mnt/p/JikhanJung/dolfinimage`, 1,909장 전부 열림 |

`fin.db` 백업: `../fin.db.20260816-*.bak` (저장소 밖. `.gitignore` 의 `*.db` 는
`.bak` 을 안 잡는다 — 안에 두면 `git add -A` 에 딸려 들어간다)

## 서버는 사진 경로를 물려서 띄운다

```bash
FIN_PHOTOS=/home/jikhanjung/dolfin-photos python manage.py runserver 0.0.0.0:8900
```

`~/dolfin-photos/nas` 가 `/mnt/p/JikhanJung/dolfinimage` 로 가는 심볼릭 링크다.
DB 가 `nas/2016/…` 로 들고 있어서 그렇다 — **코드에서 접두어를 벗기지 않은
이유**는 `devlog/…_002` 6절에 있다.

## 672장이 말한 것

| | 값 | 뜻 |
|---|---|---|
| `fix` (지느러미류) | **504/587 = 86%** | 밑동 제안이 쓸모없다. 처음부터 안 움직인다 |
| 윗윤곽 교정 | **9** | 편집기를 실제로 쓰고 있다. 여전히 드물다 |
| **교정 대기** | **5** | 그 대기줄이 제 일을 한다 |
| `cls != fin` | 108/672 = 16% | **2016-07-20 하루에 몰려 있다** (아래) |
| `edges != both` | **0/587** | **아직 아무 판단도 안 들어갔다** |
| `base_partial` | 29 | |

내보내기는 진짜 판정으로 돈다. 뺀 것은 **전부 "아직 안 봄"** 이고, 판정이 든 것
중 버려지는 것은 없다.

새 분류가 쓰이기 시작했다 — `fluke` 10 · `body` 5 · `bird` 4 · `head` 3 ·
`pectoral` 1.

### 2016-07-20 은 하루가 통째로 이상하다

| 관찰일 | 검토 | `none` | |
|---|---|---|---|
| 2016-03-15 | 109 | 3 | 2% |
| 2016-05-07 | 116 | 2 | 1% |
| **2016-07-20** | 138 | **70** | **50%** |
| 2016-09-05 | 114 | 2 | 1% |
| 2016-10-24 | 104 | 5 | 4% |
| 2017-07-25 | 91 | 3 | 3% |

여섯 날 중 그날 하나만 튄다. 옛 YOLOv5 가 절반을 헛봤다. **이제 원본 사진을 볼
수 있으니 왜 그런지 확인할 수 있다** (우클릭 → 원본 사진).

## 스키마가 두 곳 바뀌었다 (오늘)

**둘 다 자료를 보기 전에 정한 규칙이 자료를 보고 나서 틀린 것으로 드러난
경우다.** 이유는 `finseg/models.py`·`finseg/rules.py` 안에 적혀 있다.

1. **분류가 아홉이다** — 등지느러미·꼬리·주둥이·**머리·몸통·새·가슴지느러미**·
   기타·아님. 옛 검출기가 헛보는 것을 `none`(배경)에 두면 같은 동물을 배경이라
   가르친다. **`CLASSES` 순서가 곧 YOLO 클래스 번호라, 한 번이라도 내보낸 뒤에는
   `none` 앞에 덧붙이기만 할 것**
2. **`edges` 는 순수 re-ID 축이다** — `neither` → **`tip`(끄트머리)**, 그리고
   **더 이상 크롭을 버리지 않는다.** 끄트머리만 보인다는 건 앞에 온전한
   지느러미가 있다는 뜻이라, 버리면 가장 좋은 표본이 함께 사라졌다

## 검토 화면 (`review/`)

```
1~9 분류 · e 날(차림표) · x 교정필요 · p 아래경계불완전 · 점을 끌면 밑동
우클릭 메뉴 · [ ] 페이지 · Enter 저장(되짚기에서는 다음 페이지)
[검토할 것] [검토한 것] [교정 대기]
```

- **되짚기는 즉시 저장된다.** `검토한 것`·`교정 대기` 에서는 고치면 바로 들어가고
  칸이 초록으로 번쩍인다. 저장 단추가 없다. `검토할 것` 만 묶음 저장이다
- **`e` 는 차림표를 띄운다** — 순환이 아니다 (`devlog/…_003` 3절)
- **원본 사진** `/photo/<box_id>` — 휠 확대 · `Space`+움직임 이동 · `1` 이 상자로
- **윤곽 고치기** `/edit/<box_id>` — 상자로 꼭짓점 골라 함께 옮김 · `Del` 삭제 ·
  `Space` 이동 · `Ctrl+Z`. **`Esc` 로는 안 닫힌다** (고치던 것이 날아간다)
- 손 안 댄 묶음은 `Enter` 로 저장 안 된다 (`y` 나 저장 단추라야)

## 다음에 할 것

1. **검토를 이어 간다.** 남은 1,643장. **`e`(날) 를 실제로 쓸 것**
2. **키포인트 모델** — 라벨 500장. `fix` 86%를 끌어내리는 유일한 한 수다 (4단계)
3. `export_yolo` → `train` → `eval_masks`. **성적은 `val_date` 로 읽는다**

## 알아 둘 것

- **밑동을 수면선으로 자동 제안하지 말 것.** 되기는 하지만 답이 아니다
  (`devlog/…_001` 3절). 삽입점 자동 탐지 실패 기록(`finseg/baseline.py`)의
  세 번째 항목이다
- **`edges` 로 크롭을 버리던 규칙을 되돌렸다.** 되돌리려거든
  `finseg/rules.py` 의 2026-08-16 항목을 먼저 읽을 것
- **순환하는 조작을 만들지 말 것.** 중간값을 반드시 지나가게 되고, 그것이
  판단이 아닌데도 판정으로 저장된다. 시간으로 묶는 것은 가리는 것이지 없애는
  것이 아니다 (`devlog/…_003` 3절 — 그 틀린 고침을 한 번 했다)
- **재현율의 천장이 옛 YOLOv5 다.** 2016-07-20 이 그 천장을 보여 준 날이다
- **2080ti 는 Turing(sm_75) 이라 bf16 이 없다.** AMP 는 fp16
- **`fin.db` 를 두 기계에서 함께 열지 않는다.** 판정 717건은 다시 못 만든다
- **`Review` 는 쌓인다, 덮어쓰지 않는다.** 판정을 고칠 때는 새로 얹는다.
  되짚기에서 자기 기준이 언제 움직였는지 재려면 그 자취가 있어야 한다
- **화면이 자기 상태를 말하는지 늘 의심할 것.** 하루에 "값은 맞는데 화면이
  안 말한다" 를 **일곱 번** 만났다 (`devlog/…_002` 1절 · `_003` 끝).
  붉은 줄(`.warn`)이 그 자리다
