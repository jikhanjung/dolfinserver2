# Handoff — 현재 진행 상황

**2026-08-16.** 첫 바퀴 검토를 시작했다. **456장(19.7%) 을 봤고 밑동 라벨이
333장** 이라 키포인트 모델 문턱(300)을 넘겼다.

지나간 것은 `devlog/20260816_001_첫_바퀴_검토_시작과_검토_화면_세_고침.md`,
앞으로 할 일은 `TODOs.md`.

## 지금 있는 것

| | |
|---|---|
| 표본 | 관찰일 20일 (2016-03-15 ~ 2019-11-05) · 상자 2,315 · 사진 1,909 |
| 크롭 | 640×640 · 154MB · `crops/` |
| 마스크 | 2,315개 — 상자마다 하나씩. SAM2.1 · 2080ti · run 5 |
| **검토** | **456 / 2,315 = 19.7%** — 관찰일 4일치 |
| **밑동 라벨** | **333** ← 키포인트 학습에 쓸 것 |

`fin.db` 백업: `../fin.db.20260816-0140.bak` (저장소 밖. `.gitignore` 의 `*.db`
는 `.bak` 을 안 잡는다 — 안에 두면 `git add -A` 에 딸려 들어간다)

## 첫 456장이 말한 것

| | 값 | 뜻 |
|---|---|---|
| `fix` (지느러미만) | **331/379 = 87%** | **밑동 제안이 쓸모없다** |
| 윗윤곽 교정 | **0** | SAM2 마스크 자체는 한 번도 안 틀렸다 |
| `cls != fin` | 77/456 = 17% | 한 날에 몰려 있다 (아래) |
| `edges != both` | **0/379** | **아직 아무 판단도 안 들어갔다** |
| `base_partial` | 17 = 3.7% | |

`fix` 는 구간(60장)별로 94·91·84·90·93·75·77·83% — **표류가 없다.**

**틀리는 것은 마스크가 아니라 아래 경계뿐이고, 아래 경계는 SAM2 가 낸 것이 아니라
우리가 상자에서 지어낸 것이다.** 3단계에서 YOLO11-seg 를 SAM2.1 과 견줄 때
기준선이 높다는 뜻이기도 하다.

### 2016-07-20 은 하루가 통째로 이상하다

| 관찰일 | 검토 | `none` | |
|---|---|---|---|
| 2016-03-15 | 109 | 3 | 3% |
| 2016-05-07 | 116 | 3 | 3% |
| **2016-07-20** | 138 | **70** | **51%** |
| 2016-09-05 | 93 | 1 | 1% |

옛 YOLOv5 가 그날 절반을 헛봤다. 마스크 conf 도 갈린다 (`none` 0.807 / `fin`
0.949). **남은 16일에 같은 날이 더 있으면 검토량 추정이 달라진다.**

판정은 딴 자료와 안 어긋난다 — 옛 "지느러미 아님" 3건은 3건 모두 `none`,
개체명 18건은 18건 모두 `fin`.

## 여기까지 됐다

- `migrate` · `import_boxes` · `crops` · `segment` — 돈다 (segment 는 전량, 실패 0)
- 검토 화면 — **오늘 세 군데 고쳤다** (devlog 4·6절)
  - 밑동 점 집히는 범위가 화면에서 2.6px 였다 → 보이는 점과 갈라 14배로
  - **손 안 댄 묶음은 `Enter` 로 저장 안 된다** — `y` 나 저장 단추라야 한다
  - 저장 직후 마우스를 안 움직이면 모든 키가 죽던 것 (`hover`)
- `export_yolo` — 사본 DB 로 경로만 확인. **진짜 판정으로는 아직 안 돌렸다**
- `train` · `infer` · `eval_masks` — **한 번도 안 돌렸다**
- `python manage.py test` — 26개 통과. 오늘 고친 것은 전부 템플릿이라 시험 밖이다

## 운영 자리는 2080ti 다 (JikhanDesktop)

**`fin.db` 는 이제 여기 것이다** — 사람의 판정 456건이 들어 있고 다시 만들 수
없다. 다른 기계에서 열지 않는다.

| | |
|---|---|
| GPU | RTX 2080 Ti · 연산능력 **(7,5) = sm_75** |
| 꾸러미 | `.venv/` — torch·ultralytics·SAM-2·huggingface_hub |
| 사진 `/srv/dolfinserver/uploads` | **없다.** NAS 미연결 — 표본을 다시 뽑으려면 필요하다 |
| 옛 DB `../dolfinserver/db.sqlite3` | **0바이트 껍데기다** |

## 다음에 할 것

1. **검토를 이어 간다.** 남은 1,859장. 화면에서 **`e`(날) 를 실제로 쓸 것** —
   379장 전부 `both` 인 것은 판단이 아니라 기본값이다
2. **키포인트 모델** — 라벨 333장으로 YOLO11-pose. 4단계이고, 되면 `fix` 87%가
   내려간다. 지금 검토 속도를 가장 크게 바꿀 수 있는 한 수다
3. `export_yolo` → `train` → `eval_masks`. **성적은 `val_date` 로 읽는다**

## 알아 둘 것

- **밑동을 수면선으로 자동 제안하지 말 것.** 되기는 하지만 답이 아니다 — 이유
  세 가지가 `devlog/20260816_001` 3절에 있다. 삽입점 자동 탐지 실패 기록
  (`finseg/baseline.py`)에 붙는 세 번째 항목이다
- **재현율의 천장이 옛 YOLOv5 다.** 그 검출기가 못 본 지느러미는 이 루프 안에서
  표시할 방법이 없다. 2016-07-20 이 그 천장을 처음으로 보여 준 날이다
- **2080ti 는 Turing(sm_75) 이라 bf16 이 없다.** AMP 는 fp16
  (`segment.py:autocast_dtype()`)
- **`fin.db` 를 두 기계에서 함께 열지 않는다.** SQLite 는 쓰기가 하나다
- 옛 DB 는 **읽기 전용으로만** 연다 — 운영 웹서버가 같은 파일을 쓰고 있다
- **`Review` 는 쌓인다, 덮어쓰지 않는다.** 판정을 고칠 때는 지우지 말고 새로
  얹는다 (`rules.resolve` 가 최신 것을 고른다). 오늘 24행을 지운 것은 판정이
  아니라 입력 사고였기 때문이고, 그때도 백업을 먼저 떴다
