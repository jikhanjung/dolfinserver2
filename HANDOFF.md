# Handoff — 현재 진행 상황

**2026-08-27.** **세 자리가 다 `0.19.1` 이고 두 자리가 하루 한 번 주고받는다**
(m710q `work` · GCP `reid` · m710q 시험). 오늘 붙은 것은 **`/dataset` 자료 상태
화면** · **판정에 남는 자리(`ip`)** · **`/catalog` 에서 번호를 붙이는 길**이다
(`devlog/20260827_001` 11~20절). 차림표가 이제 제 판을 말한다 —
`DolfinServer2 v0.19.1`.

> **1차 목표를 숫자로 세웠다** — 개체 **42 → 60** · 조각 **563 → 1,200** ·
> 모든 개체를 **10장 이상 · 2일 이상**으로. 근거는 우리가 잰 곡선(4~9장 11.1%
> → 10~19장 30.6%, **문턱이 10에 있다**)과 같은 개체군 선행연구
> (`devlog/20260827_002`)다. **화면이 그것을 보여 주고 다음에 할 일 순서로
> 세운다** (`/dataset`).

> **`fin.db` 를 든 자리는 `m710q` 다.** 2026-08-26 에 세어 확인했다
> (`## fin.db 를 든 자리` 절). 8월 25일에 2080ti(=**JikhanDesktop**, WSL)로
> 옮겨 앉은 기록이 아래 `## 이어받기` 절에 남아 있는데, **그 뒤로 자리가
> 여기로 돌아왔다** — 그 절은 절차로만 읽을 것.
> **다음 큰 걸음은 서버의 역할을 나누는 것** — re-ID 는 GCP 만, 나머지는
> 여기만 (`## 서버를 둘로 나눈다`).

- **검출** `detect-v2` 완료 — 공평한 날에서 **재현율로 옛 검출기를 처음 넘었다**
- **분할** `seg-v3-s` 로 갈아 끼웠다 — 독립 IoU 0.673 → **0.841**,
  아무것도 못 내던 것이 569 → **6개**
- **re-ID** 조각 1,735장 · 손으로 분류하는 화면(`/reid`) · 개체 상자 **35개**
  재는 것은 이제 명령이다 — `reid_eval` (자리 둘 · `--as-of` 로 옛 정답에도 선다)
- **분류기** `reid_cls` — 카탈로그를 클래스로 놓고 배운다. kNN 25.5% 대
  **44.5%**, 묶어서 물으면 47.5% · top-5 **75%**
- **화면이 확률을 낸다** `묶음 제안` 이 분류기로 후보를 내고 `98.3%` 처럼
  퍼센트로 보인다 (`reid_cls --fit-all` 이 저장한 것을 읽는다)
- **지느러미 아닌 것 짚기** `fin_filter` — 전체 성적은 분할 엔진이 낫지만
  **그것이 놓친 것을 짚는다**. `지느러미가 아닐 듯` 정렬 · `p(fin)`
- **묶어서 묻기** `/reid` 의 `묶음 제안` — 판단 한 번이 여러 장을 덮는다
  (5장 묶음이면 top-5 **80%**). 묶는 근거는 프레임 간격 ≤2 **와** 거리 ≤0.06
- **자를 고쳐 잡았다** 이 과제는 열린 판이 아니라 **닫힌 판**이다 (`devlog/004`)
- **후보 공급** 옛 검출기 상자 **999,528개**에서 조각 2,324장을 길어 격자에
  섞었다 (`reid_chips`) — 사람 판정 없이 끝까지 간다. **값을 했다**:
  개체 42마리 중 **23마리가 옛 격자 + 새 조각이 섞인 것**이고, 날을 건너뛴 짝이
  1,289 → **5,379** 가 됐다. 2026-08-24 에 승격해 지금 이것이 `fin.db` 다
- **밖으로** 브라우저 추론(`/detect`) · 묶음(`export_bundle`) ·
  데스크톱 저장소 [`DolfinDesktop2`](https://github.com/jikhanjung/DolfinDesktop2)
- **자료 상태** `/dataset` — 목표 넷의 진척과 **다음에 할 일 목록**(문턱에 가장
  가까운 개체부터). 세는 자리는 `reid.dataset_state()` 하나다
- **번호를 붙이는 길** `/catalog` 에서 **번호·별명·메모**를 눌러서 고친다.
  알아보는 자리와 이름 붙이는 자리를 하나로 뒀다 — 조각을 다 펴 놓고 보아야
  "이건 JTA0xx 다" 를 말할 수 있다. **`JTA###` 는 공식 개체 ID 라 미리 박아
  두지 않는다**: 지금 42상자가 전부 `상자 N` 이고 그 일은 전문가의 몫이다
- **판정에 자리** `Review.ip`·`Identification.ip` (때는 `at` 로 원래 있었다).
  붙이다가 `gate.client_ip` 이 **위조할 수 있는 헤더 자리**를 읽고 있던 것을
  함께 고쳤다 — 그것이 접속 잠금의 열쇠이기도 했다

지나간 것은 `devlog/` 에 있다. `20260827_001` 이 두 자리가 주고받는 것과 오늘
화면 손본 것을, **`20260827_002` 가 같은 개체군 선행연구와 우리 자를 맞춰 본
것**을 담았다 (Kim et al. 2024 — **합성 데이터는 안 하기로 했고 왜인지 거기
있다**). 앞으로 할 일은 `TODOs.md`.

## 한 줄로

| 단계 | | |
|---|---|---|
| 1 자료 경로 | 완료 | |
| 2 첫 바퀴 검토 | 완료 | 상자 2,315 |
| 3 분할 학습 | **세 바퀴** | v3 독립 IoU **0.841** · 못 내던 것 569 → 6 |
| 4 키포인트 | 완료 | 밑동 오차 37.6 → 21.6px |
| 5 검출기 | **한 바퀴 돌았다** | 정밀도 87.3% · 새 지느러미 **419** · 재현율 97.5% |
| 6 re-ID | **착수** | 격자 **7,912**(v3) · 개체 **45**(조각이 든 것 42) · 조각 **563** · 날 건너뛴 개체 **34** ← 지금 |
| — 1차 목표 | | 개체 42/**60** · 조각 563/**1,200** · 10장이상 21/42 · 2일이상 34/42 (`/dataset`) |

## 지금 있는 것

| | |
|---|---|
| 표본 | 관찰일 **73일** (2016-03-15 ~ 2019-11-05) · 사진 **7,927** |
| 상자 | **10,537** — `yolov5` 9,847(옛 검출기 + 옛 DB 에서 길어 온 것) + `yolo11` **690**(우리 검출기) |
| 크롭 | 10,537개 전부 · 640×640 · `crops/` |
| 마스크 | 25,655 · 지금 쓰는 것은 **`seg-v3-s` 10,247** + `sam2.1` 47 + `seg-v2-s` 23 + `seg-v1-s` 4 |
| 검토 | 상자 **3,024** · 판정 **6,141건**. 이제 판정마다 **때와 자리**가 남는다(`at`·`ip`) — 다만 옛 줄은 `ip` 가 빈다 |
| **새 검출** | **690 전부 검토** — 등지느러미 **419** · 딴 부위 152 · 헛것 119 |
| 사람이 그린 것 | 윗윤곽 **675** · 밑동 3,580 |
| **개체 분류** | 격자 **7,912**(`reid/v3`) · **조각 563 · 개체 42**(`catalog()`) · `Individual` 45 + 개체가 아닌 자리 2 · 날을 건너뛴 개체 **34** · 개체판정 **1,338** |
| **1차 목표** | 개체 42/**60** · 조각 563/**1,200** · 10장 이상 **21**/42 · 2일 이상 **34**/42 · 모자란 조각 **109** (`/dataset` · `reid.GOAL`) |

**안 본 상자 7,513 은 분할 검토 대기열이 아니다.** 전부 `yolov5` 출처이고
옛 운영 DB 에서 **re-ID 후보로** 들여온 것이다 — 그중 6,174 가 격자에 있고,
사람 판정 없이 `/reid` 로 바로 간다 (`devlog/20260824_003`). 상자 수만 보고
"검토가 7,513개 밀렸다" 고 읽으면 안 된다.

상자 3,024개의 판정(가장 늦은 것): `fin` 2,460 · **`none` 216** · `person` 110 ·
`body` 78 · `rostrum` 55 · `fluke` 39 · `head` 27 · `bird` 19 · `pectoral` 10 ·
`other` 10. 학습 자료로는 **양성 2,808 · 배경 216**.

`마스크없음 55` 는 남은 일이 아니다 — 사람이 직접 그렸거나 `none` 이다.
`엔진바뀜 2,876` 도 마찬가지다: v3 로 갈아 끼워 생긴 것이고 **마스크가 좋아진
것이지 나빠진 것이 아니다.** 안 봐도 자료는 이미 v3 로 나간다.

```
db/fin.db                                  판정 6,039 + 개체판정 1,319 — 다시 못 만든다
db/dolfinserver_prod_2026-08-17.sqlite3    옛 운영 DB · 사진 544,585 · 상자 999,528
```

`.gitignore` 가 `db/` 를 통째로 무시한다.

**백업은 NAS 로 뜬다** — `manage.py backup` (2026-08-24 에 붙였다).

```
<NAS>/dolfinserver2_backup/                      537MB
  db/fin.db.<날짜>.bak    날짜별로 쌓는다 (30일 치)
  weights/<run>/best.pt   이름별로 한 벌 · 내용이 같으면 건너뛴다
  derived/crops · reid     `--derived` 를 줄 때만 (다른 기계로 옮기려는 것)
  MANIFEST.<날짜>.json    그날 무엇이 있었나 · sha256
```

**다시 만들 수 없는 것만 든다** — 크롭·자료 꾸러미·조각은 `fin.db` 와 사진에서
다시 뽑는다. sqlite **온라인 백업 API**(`Connection.backup`)로 뜨므로 검토
화면이 열려 있어도 안전하고, 뜬 뒤에 `integrity_check` 로 **읽어 본다** —
확인 안 한 백업은 백업이 아니다.

옛 운영 DB 는 NAS 가 날마다 01:00 에 따로 뜨고 있다(`dolfinid_backup/`).
**정작 다시 만들 수 없는 `fin.db` 가 그 레인 밖이었다.**

**`<NAS>` 는 기계마다 다르다.** 같은 NAS 인데 보는 길이 다를 뿐이다 —
2080ti(WSL)는 `/mnt/p/JikhanJung`, m710q(NFS)는 `/nas/JikhanJung`.
명령에 박아 두지 않는다. 뿌리는 `finseg/nas.py` 한 곳이고 `backup` 과
`export_detect` 가 그것을 받아 쓴다(`FIN_NAS` 로 대 줄 수 있다) —
**박아 두면 다른 기계에서 "NAS 가 안 붙어 있다" 고 잘못 말한다.**

### 다른 기계(`m710q`)에서 일하려면

GPU 가 필요 없는 일 — 검토·개체 분류·문서 — 은 거기서 해도 된다. 다만
**`fin.db` 는 한 기계에서만 연다.**

```bash
# 2080ti 에서 — 떠 놓고 손을 뗀다
python manage.py backup --derived

# m710q 에서 — 거기 NAS 는 `/nas/JikhanJung` 이다
git pull
B=/nas/JikhanJung/dolfinserver2_backup
mkdir -p db && cp $B/db/fin.db.<날짜>.bak db/fin.db
sha256sum db/fin.db          # MANIFEST 의 것과 견준다 — 안 견준 복원은 복원이 아니다
cp -r $B/derived/crops/. crops/
cp -r $B/derived/reid/.  reid/
ln -s /nas/JikhanJung/dolfinimage ~/dolfin-photos/nas   # 원본 사진 (`/photo`·`/edit`)
python manage.py migrate && python manage.py runserver 0.0.0.0:8900

# 일이 끝나면 거기서 다시 떠서 2080ti 로 가져온다
python manage.py backup --derived      # 뜰 곳은 `finseg/nas.py` 가 안다
```

**양쪽에서 동시에 열면 어느 쪽 판정이 이기는지 아무도 모른다.** 합치는 길이
없다 — `Review`·`Identification` 이 둘 다 쌓이는 테이블이라 번호가 겹친다.
(**GCP 와는 다르다** — 거기는 쓰는 테이블을 아예 갈라 두었다.
`## 서버를 둘로 나눈다`.)

**`m710q` 에서 `git pull` 하면 그쪽의 옛 `crops/`·`db/fin.db` 가 워킹트리에서
지워진다** (`5d63a5c` 가 추적을 멈춘 커밋이다). **지워져도 된다** — 히스토리에
남은 그때의 `fin.db`(`git show 1cf6301:fin.db`)를 꺼내 세어 보니 **판정이
0건**이었고, 지금 `fin.db` 의 첫 판정(2026-08-15 15:48)부터 6,014건 전부 이
기계에서 만든 것이다. `m710q` 는 표본을 들여 크롭을 자르고 넘긴 데까지다
(run 1~3).

옛 `TODOs` 에 "거기 판정 717건이 있다" 고 적혀 있었는데 **틀렸다** — 그 717은
`devlog/20260816_003` 의 *이 기계* 진행 상황(검토 672 / 2,315 · 판정 717건)이
`m710q` 설명으로 옮겨 붙은 것이다. **숫자를 옮겨 적을 때 그것이 무엇을 세는
숫자인지 함께 옮겨야 한다.**

저장소 밖 로컬 사본도 있다 — `../fin.db.*.bak` (`*.db` 규칙이 `.bak` 을 안 잡는다).

## 검출기 — 정밀도를 쟀다 (690/690)

```
    문턱     본 것   등지느러미   딴 부위   헛것    정밀도
  ≥0.40      229       200       22      7    87.3%
  ≥0.25      319       258       49     12    80.9%
  ≥0.15      421       313       78     30    74.3%
  ≥0.05      690       419      152    119    60.7%
```

**옛 검출기의 천장 위로 등지느러미 419개를 올렸다** — 옛 상자의 `fin` 2,044 에
대해 +20.5%.

띠로 끊어 보면 문턱이 정해진다. `≥0.40` 87.3% · `0.25~0.40` 64.4% ·
`0.15~0.25` 53.9% · **`<0.15` 39.4%**. 가장 낮은 띠조차 5장에 2장이 진짜라,
"헛것은 키 한 번에 걸러지지만 놓친 지느러미는 걸러낼 기회조차 없다" 를 그대로
대면 **`conf 0.05` 를 유지하는 쪽**이다.

### 새 검출기가 여는 것은 **가려진 지느러미**다

| | `fin` | `tip`(끄트머리만) |
|---|---|---|
| 옛 YOLOv5 | 2,044 | 62 (**3.0%**) |
| 새 검출기 | 419 | 281 (**67.1%**) |

옛것이 못 본 게 아니라 **그 종류를** 못 봤다. `rules.py` 가 되돌린
"`edges` 로 크롭 버리기" 규칙을 살려 뒀으면 이 수확의 3분의 2를 규칙으로
버렸을 것이다.

### 반쯤 재고 멈추면 낙관 쪽으로 기운다

508개(마스크가 붙은 것)만 봤을 때와 690개 전량은 이렇게 갈렸다 —
`≥0.40` 89.8→**87.3** · `≥0.25` 85.1→**80.9** · `≥0.15` 81.1→**74.3** ·
`≥0.05` 75.2→**60.7**. 마스크를 받은 508개는 `fin` 이 75.2%인데 못 받은 182개는
**20.3%** 였다 — **분할 모델이 마스크를 내느냐가 "진짜냐" 와 상관이 있다.**
문턱이 낮을수록 벌어진다.

## 검출기 — 재현율에서 옛것을 처음 넘었다

공평한 날 2016-03-15 (사진 1,109 · 정답 1,968).

```
검출기                     낸 것    맞힘    정밀도   재현율   못 찾음
옛 YOLOv5 (conf .25)      1,905  1,859   97.6%   94.5%     109
detect-v2 conf 0.05       3,195  1,918   60.0%   97.5%      50
detect-v2 conf 0.40       2,317  1,882   81.2%   95.6%      86
```

**못 찾은 것이 109 → 50개로 반 이하가 됐다.** 지난 바퀴의 `detect-merged` 는
옛것이 놓친 109개 중 84개를 찾으면서도 전체 재현율에서는 못 이겼는데 넘었다.

`정밀도` 열은 그대로 읽으면 안 된다 — 정답이 옛 검출기 출력에서만 만들어져
새 검출기가 더 뱉은 것은 자동으로 오답이 된다. 그것을 묻는 자리가 위의
`690/690` 표다.

학습은 `runs/detect-v2-det` · 120에폭 16시간 36분 · 최고 mAP50 **0.9637**(77) ·
mAP50-95 **0.7173**(101). 자료는 사진 4,240 · 상자 9,795.

### `close_mosaic` 의 마지막 10에폭은 두 번 다 값을 안 했다

| | 전체 최고 | 모자이크 꺼진 뒤(111~120) |
|---|---|---|
| `detect-merged` | 0.9599 / 0.6773 (108) | 0.9593 / 0.6765 |
| `detect-v2` | 0.9637 / 0.7173 (77 / 101) | 0.9613 / 0.7164 |

두 run 이 같은 답을 냈다 — **그 설정을 줄일 근거**가 된다.

## 분할 — 세 바퀴 · v3 로 갈아 끼웠다

**독립 표본 270장**(사람이 윗윤곽을 직접 다시 그린 것)에서 잰다. 나머지는
사람이 `통과` 를 누른 것이라 정답이 곧 그때 엔진의 출력이고, 그 엔진에게는
자기 답을 채점하는 셈이 된다.

| run | 모델 | 자료 | 평균 IoU | ≥0.7 | 못 낸 것 |
|---|---|---|---|---|---|
| 5 | SAM2.1 | — | 0.575 | 53.0% | 577 |
| 16 | s | seg-v1 | 0.658 | 69.6% | 571 |
| 23 | s | seg-v2 | 0.673 | 71.5% | 569 |
| 39 | m | seg-v3 | **0.852** | 88.9% | 6 |
| **41** | **s** | **seg-v3** | **0.841** | 86.7% | **6** |

**아무것도 못 내던 것이 569 → 6개.** 매번 "이것이 이 엔진의 재현율 손실이다"
라고 경고를 달던 문제가 사실상 사라졌다.

### 자료가 모델보다 15배 셌다

```
자료 v2 → v3 (모델 s 고정):  0.673 → 0.841   +0.168
모델 s → m  (자료 v3 고정):  0.841 → 0.852   +0.011
```

앞 바퀴의 "모델 `s`→`m` 이 +0.006" 이 거의 그대로 재현됐다. **`s` 를 쓴다** —
`m` 이 평균 IoU 에서 +0.011 앞서지만 `≥0.9` 는 `s` 가 낫고(64.4% 대 63.7%),
`val` mAP50-95 도 `s` 가 앞서며(0.5583 대 0.5446), 무엇보다 **20MB 대 44MB** 다.

`seg-v3` 자료는 크롭 3,005 · 라벨 3,611 — `dolphin` 이 88 → **271** 로 세 배가
되어 그 클래스가 평균을 끌어내리던 것이 풀렸다.

## 명령 흐름

```bash
FIN_PHOTOS=/home/jikhanjung/dolfin-photos python manage.py runserver 0.0.0.0:8900

# 분할 (3단계) — v3-s 가 현재다 (run 41 · promote run 42)
python manage.py export_yolo --out datasets/seg-v4 --group coarse --val-date 2019-06-17
python manage.py train --data datasets/seg-v4 --model yolo11s-seg.pt   # [GPU] 1시간 20분
python manage.py infer --weights runs/seg-v4-seg/weights/best.pt --compare-only
python manage.py eval_masks --runs 16 23 41 <새 run>          # **독립 열로 읽는다**
python manage.py promote --run <run> && python manage.py infer_base --redo

# 검출 (5단계) — detect-v2 가 현재다 (run 36)
python manage.py export_detect --out datasets/detect-v3       # fin.db 판정을 읽는다
python manage.py train --data datasets/detect-v3 --imgsz 1280 --batch 8   # [GPU] 17시간
python manage.py eval_detect --date 2016-03-15 --weights runs/detect-v3-det/weights/best.pt
python manage.py infer_boxes --weights <새 가중치> --date <날짜>
python manage.py crops                                        # 새 상자를 자른다
python manage.py eval_detect                                  # 사람이 본 것으로 문턱별 정밀도

# 밖으로 내보내기
python manage.py export_bundle --run 36 --det-weights runs/detect-v2-det/weights/best.onnx \
    --seg-run 41 --out bundles/v1        # 가중치 + onnxdet.py + MODEL.json 한 벌
#   받는 쪽: DolfinDesktop2 의 `python -m dolfin <폴더> --bundle bundles/v1`
python manage.py import_detections scan.json      # 그쪽이 낸 것을 들인다

# re-ID (6단계)
python manage.py reid_cluster --dry-run           # 문턱별로 어떻게 갈리나
python manage.py reid_eval                                # 자를 잰다
python manage.py reid_eval --emb emb-dinov2.npz
python manage.py reid_eval --as-of <개체판정 번호>          # 옛 정답에 새 자를 세운다
python manage.py reid_probe --loss triplet                # 배우면 나아지나
python manage.py reid_probe --hold-individuals 12         # **열린 판**
python manage.py reid_chips --out reid/v3 --emb-only --backbone dinov2 \
    --emb-name emb-dinov2.npz                            # 조각은 두고 자만 바꾼다
#   **`--dir` 을 안 준다.** 기본값이 `FIN_REID`(화면이 보는 격자)라, 박아 두면
#   격자를 갈아 끼울 때마다 조용히 옛것을 잰다 (`9fc484d`)
#   조각 만들기는 아직 스크립트다 — 명령으로 뽑는 것이 남았다 (`reid_chips` 쯤)
#   → 화면 `/reid` 에서 손으로 상자에 넣는다

python manage.py test          # 95개. fin.db 를 안 건드린다
```

**자료·가중치가 어느 것인지**: 검출은 `detect-human`(817장, 실험) →
`detect-merged`(2,508) → **`detect-v2`(4,240 · 상자 9,795)**, 분할은
`seg-v1` → `seg-v2`(둘 다 크롭 2,315) → **`seg-v3`(3,005 · 라벨 3,611)**.
지금 쓰는 것은 **검출 `runs/detect-v2-det`, 분할 `runs/seg-v3-s`(run 41)** 이다.

`~/dolfin-photos/nas` 가 `<NAS>/dolfinimage` 로 가는 심볼릭 링크다
(2080ti 는 `/mnt/p/…`, m710q 는 `/nas/…` — 위의 `<NAS>` 와 같은 이야기다).
DB 가 `nas/2016/…` 로 들고 있어서다 — 코드에서 접두어를 안 벗긴 이유는
`devlog/…0816_002` 6절.

## re-ID — 사람이 손으로 분류한다 (`/reid`)

**개체명이 거의 없다.** 옛 DB 에 이름 166종 · 상자 435개인데 **상자 5개 이상인
개체가 다섯**이고, 그 이름 중에 `까치`·`황여새` 같은 새 이름이 섞여 있다.
지도학습을 할 자료가 못 된다.

### 군집을 보여 주다 그만뒀다

묶어서 줄로 보였는데 쓸모가 없었다. 둘 때문이다.

- **묶음이 밝기로 뭉쳤다** — 그 정도가 68%였다(묶음 안 밝기 표준편차 0.027 대
  같은 크기 무작위 0.085). 밝기를 지우고 다시 재니 **오히려 나빠졌다** —
  양성쌍이 같은 장면이라 **자 자체가 조명 닮음에 상을 준다.**
  **정답 없이 재는 것은 여기까지다**
- **특이한 개체가 오히려 안 보였다** — 묶음은 둘 이상 든 것만 내는데, 특이하다는
  것은 남과 안 닮았다는 뜻이라 혼자 남는다. 사람이 가장 쉽게 알아보는 애들이
  화면에서 빠지는 것은 앞뒤가 안 맞는다

그래서 뒤집었다. **판단은 사람이 하고 모델은 순서만 돕는다.**

### 무엇이 조각이 되나

상자 3,005개 중 **1,735장**. 거르는 축이 전부 검토에서 남긴 것들이다 —
`cls=fin` · 뒷날이 보임(`edges` 가 `both`/`trailing`) · **뒷삽입점이 짐작이 아님**
(`base_partial`) · 앞쪽을 앎(`facing`) · 넓이 ≥15,000px².

`CLAUDE.md` 가 "분할에는 필요 없어 보여도 적어 둔다" 고 한 것이 여기서 값을 한다.

**뒷삽입점이 짐작이면 뺀다** — 밑동 현이 흔들려 정규화 기준이 못 된다.
앞쪽만 짐작인 것은 쓴다(현 각도는 흔들려도 뒷날은 온전하다).

### 조각을 어떻게 만드나

밑동 현으로 회전·크기를 맞춘다(`reid.frame` — **변환식은 여기 하나뿐이다**).
모델이 먹는 것은 배경을 지운 회색조 128px, 사람이 보는 것은 **색·배경 그대로
320px** 다. 정렬은 둘이 같다 — 사람이 "이건 달라" 라고 한 것이 모델에게는
다른 자리였다면 그 판정을 못 쓴다.

**배경을 지우는 이유**는 그것이 그날 바다이기 때문이다. 안 지우면 같은 날
찍힌 것끼리 물빛이 닮아 개체보다 먼저 묶인다.

### 뒷날 곡선 대 전체 모양 — **사람이 붙인 정답으로 갈렸다**

**재는 것은 `reid_eval` 이다** (2026-08-24 에 명령으로 뽑았다). 스크립트로 한 번
돌리고 표만 남겼던 자리다 — 정답이 늘 때마다 손으로 다시 하면 그 사이에 규칙이
조용히 달라진다.

```bash
python manage.py reid_eval                  # 지금 정답으로
python manage.py reid_eval --as-of 448      # 그때 정답으로 (자를 견줄 때)
```

정답 **조각 238장 · 개체 35마리** · 질의 154. 같은 개체 짝 1,784개 중
**날을 건너뛴 것 1,289개** · **좌우가 갈려 못 재는 것 752개**.

**날을 건너뛴 짝만** 본다. 서로 다른 날은 빛도 물빛도 달라서, 아래 "자가
조명을 재고 있었다" 는 교란이 상당 부분 걷힌 조건이다. 좌현·우현은 안 섞는다
(`normalize` 가 한쪽을 거울처럼 뒤집는다).

| 자 | 1등 | 첫 정답 | mAP | 개체평균 1등 |
|---|---|---|---|---|
| 뒷날 곡선 | 14.9% | 12위 | 12.9% | 12.3% |
| 조각 화소 그대로 | 7.1% | 10위 | 12.0% | 8.4% |
| 조각 실루엣만 | 17.5% | 9위 | 13.8% | 9.4% |
| **ResNet18 · 조각** | **32.5%** | **3위** | **26.5%** | **27.2%** |

후보를 **격자 전체(1,735장)** 로 넓히면 — 실제로 카탈로그를 뒤지는 상황이다:

| 자 | 1등 | 첫 정답 |
|---|---|---|
| 뒷날 곡선 | 3.2% | 86위 |
| 조각 화소 그대로 | 0.0% | 85위 |
| 조각 실루엣만 | 6.5% | 54위 |
| **ResNet18 · 조각** | **18.8%** | **20위** |

**전체 모양이 뒷날 곡선을 크게 이긴다.** 어느 자로 재도 같은 방향이고, 정답이
두 배가 되어도(119 → 238) 순서가 안 바뀌며, 미시 평균과 개체 평균 양쪽에서
그렇다.

**표에 앉은 `ResNet18 · 조각` 은 학습 없는 ImageNet 특징이다** — `reid_train`
은 `backbone.pt` 와 `emb.npz` 를 함께 쓰는데 옆에 `backbone.pt` 가 없다.
명령이 그 판단을 화면에 적는다. 대조학습의 **출발선**이 저 32.5% 다.

`ResNet18 · 실루엣만`(옛 표 29.1%)은 지금 표에 없다 — ImageNet 가중치를 새로
통과시켜야 하는데 m710q 에 캐시가 없다. 나머지 넷은 `reid/v1` 의 저장물로
계산한 것인데, **`v1` 은 2026-08-26 에 지웠다** — 다시 내려면 NAS 사본을
가져와야 한다 (`9fc484d`).

### **정답이 다른 두 판을 세로로 견주지 말 것**

정답이 늘면 두 자리가 **반대로** 움직인다.

| | 후보 | ResNet18 · 조각 1등 |
|---|---|---|
| 좁은 후보 · 정답 119 | 52장 | 41.9% |
| 좁은 후보 · 정답 238 | **131장** | **32.5%** |
| 격자 전체 · 정답 119 | 824장 | 8.1% |
| 격자 전체 · 정답 238 | **822장** | **18.8%** |

`좁은 후보` 는 후보가 함께 늘어 **어려워지고**, `격자 전체` 는 후보가 그대로인
채 정답만 늘어 **쉬워진다.** "8.1% → 18.8%" 는 자가 좋아진 것이 아니다.
그래서 표에 `후보` 열이 있고, `--as-of` 가 옛 정답에 새 자를 세운다.

**옛 표가 `--as-of 448` 에서 재현됐다** — 조각 화소·실루엣·ResNet18 세 줄이
소수점까지 같다. `뒷날 곡선` 만 16.3% → 19.8% 로 갈리는데, 지금 것은
`reid.distance` 에 묶여 있고 돌 때마다 그 식과 표본으로 견준다. 옛 스크립트가
곡선을 어떻게 뽑았는지는 안 남아 있어 **근거가 있는 쪽은 지금 것이다.**

**아직 실용은 아니다.** 전체 후보에서 1등 18.8% · 중앙값 20위 — "닮은 것 스무
장" 을 보여 주는 검색 보조로는 쓸 만하고 자동 식별은 아니다.

곡선이 진 것이 방식 자체가 틀려서인지는 아직 모른다 — **내 구현이 순진했을
수 있다.** [finFindR](https://www.biorxiv.org/content/10.1101/825661v1) 은 뒷날을
마스크 부산물로 자르지 않고 전용 신경망으로 그리고, CurvRank 는 곡률 기술자를
쓴다. 다만 **전체 모양을 쓰는 쪽이 이 자료에서 낫다**는 것은 이제 재어졌다.

### 옛 상자에서 후보를 길어 온다 (2026-08-24 · **승격 완료**)

사람이 알아볼 수 있는 것을 격자에서 거의 다 골라낸 뒤의 길이다. 남은 것을
더 훑는 대신 **후보 자체를 더 가져온다** — 옛 운영 DB 에 상자 999,528개,
그중 15,000px² 이상이 **400,309개 · 245일** 있다.

```bash
python manage.py import_boxes --src <옛 DB> --only-dates 2016-08-09,… --boxes 480
python manage.py crops
python manage.py infer --weights runs/seg-v3-s/weights/best.pt        # [GPU]
python manage.py infer_base --weights runs/pose-v1/weights/best.pt    # [GPU]
python manage.py reid_chips --out reid/v3 --auto-facing --auto-cls --min-area 15000
python manage.py reid_chips --out reid/v3 --emb-only --backbone dinov2 \
    --emb-name emb-dinov2.npz                  # 조각은 두고 자만 바꾼다

# 분류 (닫힌 판) — **성적을 잴 때와 화면에 쓸 때가 다르다**
python manage.py reid_cls --epochs 2000                          # 날로 갈라 잰다
python manage.py reid_cls --epochs 2000 --group                  # 묶어서 잰다
python manage.py reid_cls --epochs 2000 \
    --fit-all reid/v3/cls-dinov2.npz           # 아는 것을 다 써서 배운다 (화면용)

# 지느러미가 아닌 것 짚기
python manage.py fin_filter --fit --score reid/v3 --write
```

**검토 안 된 상자에서 막는 축은 `facing` 하나였다.** `usable()` 이 판정 없으면
`edges`·`base_partial` 을 통과값으로 읽는다 — `TODOs` 가 "대체가 없다" 고 적어
둔 두 축이 실은 안 막고 있었다. `facing` 은 `propose_facing`(기하), `cls` 는
분할 엔진이 메운다. **짐작한 것은 `facing_src`·`cls_src` 에 적힌다.**

두 바퀴 돌려 격자를 1,735 → **4,059** 로 늘렸다 (m710q CPU 75분).

| 격자 | 후보 | 1등 | 첫 정답 | 비례라면 |
|---|---|---|---|---|
| 1,735 | 819 | 18.2% | 20위 | — |
| 2,233 | 1,029 | 17.6% | 25위 | 25.1위 |
| 4,059 | 2,137 | 14.5% | **39위** | 52.2위 |

**후보가 2.6배가 됐는데 순위는 2.0배만 밀렸다** — 새 조각이 옛 조각과 구별되지
않는 품질이라는 뜻이다. 상위 10 후보에서의 몫도 정확히 제 몫이다(63.4% 대 63.5%).

**그리고 진짜 재회가 들어 있었다.** 사람이 새 격자에서 682건을 더 판정한 결과 —

```
개체 42마리 중   옛 격자만 16 · **옛것+새것이 섞인 것 23** · 새것만 5
상자에 든 조각 511 중 **옛 상자에서 온 것 215**
날을 건너뛴 짝  1,289 → **5,379**   ·  날을 건너뛴 개체 15 → **32**
```

한 개체는 47장·**20일**에 걸치고(옛 18 + 새 29), 옛 격자에 3장뿐이던 개체가
새 조각 17장을 얻어 11일짜리가 되기도 했다. **지표로는 답할 수 없던 자리를
사람이 답했다** — 그 앞의 성적표는 라벨 없는 후보를 오답으로 세는 자였다.

### 격자를 갈아 끼운다 — `FIN_REID`

```bash
cd /srv/dolfinserver2 && docker compose up -d       # 정식 화면은 컨테이너다
#   **지금 격자는 `reid/v3`(7,912장) 다.** `v1`·`v2` 는 2026-08-26 에 지웠고
#   (`9fc484d` — `v3` 가 `v2` 를 비트까지 포함한다), NAS 사본만 남는다.
#   `FIN_REID` 를 안 주면 없는 자리를 보고 **격자가 조용히 빈다**

FIN_DB=<사본> FIN_CROPS=<사본 크롭> FIN_REID=<시험 격자> \
  python manage.py runserver 0.0.0.0:8901     # 시험은 이렇게 따로 띄운다
```

**`fin.db` 를 안 건드리고 시험한다.** 사본을 물려 다른 포트에 띄우면 거기서
무엇을 해도 진짜 판정은 안 바뀐다.

### **넓이 문턱이 코드와 자료에서 갈려 있다**

격자는 **15,000** 으로 만들어졌는데(`v3/items.json` 의 `min_area`) `reid.MIN_AREA` 는 **30,000** 이다.
모르고 다시 만들면 **사람이 이미 분류한 조각 1,034장이 격자에서 사라진다.**
`reid_chips` 가 `--min-area` 를 받아 `items.json` 에 적고 다르면 멎지만,
**어느 쪽이 옳은지는 아직 안 정했다** (`TODOs`).

### **이 과제는 닫힌 판이다** — 자를 그렇게 잡는다

사람 re-ID 는 **처음 보는 개체**를 알아보는지로 잰다. 감시 카메라가 날마다
모르는 사람을 만나기 때문이다. **우리는 그 문제가 아니다** — 제주 남방큰돌고래는
개체군이 100~120 규모라 **개체 수의 천장이 개체군 크기**이고, 목표는
"카탈로그의 몇 번인가" 다.

    닫힌 판 (배운 개체를 다시 만남)   ImageNet 28.0%  →  학습 43.0%
    열린 판 (처음 보는 개체)          ImageNet 37.8%  →  학습 31.7%

**같은 학습이 한쪽에서 이기고 한쪽에서 진다.** 지는 쪽만 보고 "학습이 안 된다"
고 읽었던 것을 고쳤다. 그러면 이 과제는 metric learning 이 아니라 **닫힌 집합
분류**이고, 그쪽은 자료 효율이 훨씬 좋다.

**남는 한정 셋**은 진짜다 — 개체군이 완전히 닫혀 있지 않고(새끼), 지느러미가
변하고(notch 가 새로 생긴다), 지금 덮개가 고르지 않다(62장짜리와 1장짜리).
`reid_probe --hold-individuals` 는 버릴 자가 아니라 **"모르는 것" 문턱을 재는
자**로 역할이 바뀐다.

### 길은 하나로 정해졌다 — **사람이 붙이고, 분류기가 넓힌다**

    사람이 최대한 많이 분류한다  →  그 정답으로 얼린 특징 위에 분류기를 배운다
                                 →  분류기가 나머지에 답을 달아 **큰 정답지**를 만든다
                                 →  사람은 그것을 확인하고 고친다 (다시 위로)

**연속 프레임 대조학습은 접었다** (`TODOs` 의 안 하기로 한 것). 열린 판에서
기준선을 못 넘었고, 애초에 그것이 묻는 것(처음 보는 개체)이 우리 문제가
아니었다 — 개체군이 100~120이라 **개체 수의 천장이 개체군 크기**다.

### 쫓는 숫자는 하나다 — **아는 개체를 얼마나 맞히나**

새로 들어오는 개체는 **그때 생각한다.** 1차 목표는 카탈로그에 이미 있는 개체를
얼마나 잘 찾느냐이고, 그것이 곧 사람이 하나하나 안 붙여도 되게 하는 길이다.

    **재는 날을 돌려서 잰다** (`reid_cls --folds 5 --seeds 3`) — 질의 493
                                    top-1     top-5    top-10
    기준선 — 가장 닮은 조각(kNN)      27.4%     63.5%    80.7%
    분류기 · 지금 화면이 쓰는 것       44.9%     75.0%    84.3%
    **분류기 · PCA 256 + L1 1e-5**  **48.1%**  77.5%    84.5%   ← 아직 안 걸었다

    **top-10 을 남과 그대로 견주지 말 것** — 42마리에서 열을 찍으면 23.8%가
    맞고 Kim et al. 의 79마리에서는 12.7%다. 명령이 그 값을 함께 찍는다

    옛 고정 갈래(가장 큰 8일 · 질의 137)로도 재 둔다 — 42.3% / 64.2%.
    **HANDOFF 에 오래 적혀 있던 44.5 / 68.6 은 정답이 더 적던 때의 것**이라
    그대로 견주면 안 된다 (`정답이 다른 두 판을 세로로 견주지 말 것`)

**폴드끼리 40.0 ~ 53.4 로 13.4%p 가 흔들리는데 씨앗끼리는 ±0.1 이다** — 잡음은
머리의 초기값이 아니라 **어느 날을 재느냐**에서 온다. 고정 갈래의 한 숫자는 그
분포에서 뽑은 한 장이었다. `--folds` 가 사는 것은 참값이 아니라 **A와 B를 같은
폴드에 세워 493문항으로 견줄 수 있게 되는 것**이다 (눈금 0.73%p → 0.20%p).

이것을 움직이는 것 셋은 재어 두었다.

    개체당 조각 수     4~9장 11.1% → 20장 이상 33.8%
    묶어서 묻기        낱장 22.7% → 5장 묶음 40.0%
    닫힌 판 학습       25.5% → 44.5%   ← **오늘 여기를 팠다**

### **길을 정했다 — 찍힌 차례로, 최대한 많이 빨리** (2026-08-27)

**닮음으로 세우는 길을 접었다.** `묶음 제안`·`닮은 것끼리` 가 실제로 지느러미
형태를 보고 판단할 때 도움이 안 된다는 것이 **화면을 쓰는 사람의 판단**이고,
왜인지가 두 갈래로 세어진다.

- **자가 재는 조건이 화면의 조건이 아니다.** 분류기의 48.1%는 "진짜 답이
  카탈로그 안에 있을 때" 의 값인데, **카탈로그가 개체군의 38%(42/110)밖에
  안 덮는다.** 곱하면 18% — 다섯을 내밀면 네 번 중 셋 이상은 다 틀리고,
  화면은 그때도 `98.3%` 같은 숫자를 붙인다
- **모델이 보는 닮음이 사람이 보는 닮음이 아니다.** 사람은 뒷날 notch 로
  알아보는데 그것이 조각(128px)에서 **2~5px** 이다. DINOv2 ViT-S/**14** 는
  14×14 패치로 보므로 **notch 가 패치 하나보다 작다.** 조각을 224 로 키워도
  5px 남짓이라 안 풀린다 — **패치 기반 ViT 로 notch 를 보게 하는 길이 막혀
  있다.** 묶음이 밝기로 뭉친 정도 68%, `rough` 사분위별 top-1 차이 8pp 가
  같은 말을 하고 있었다

**그래서 지금은 날짜·시간순으로 놓고 개체 할당을 최대한 많이, 빨리 한다.**
근거가 모델이 아니라 자료에 있다 — 잇달아 찍은 프레임은 같은 개체일 가능성이
높고, 사람이 눈으로 쓰면 모델보다 정확하다. **이 길에서는 개체 수가 곧
성능**이고 그것이 `/dataset` 의 1차 목표다. 그다음은 정답이 모인 뒤에 다시 본다.

### 자를 세우고 나니 규제 쪽이 열렸다 (2026-08-27) — **재어 두었으나 안 걸었다**

**"가중치 감쇠가 안 움직였으니 과적합이 아니라 자료를 다 못 담고 있다" 고
적어 두었던 것은 틀렸다.** 용량을 줄이니 올랐다.

```
설정 (5폴드 · 씨앗 3 · 같은 493문항)   top-1      top-5      top-10
ViT-S 384 (오래 쓰던 것)              44.9±0.1   75.0±0.1   84.3±0.3
+ PCA 256 (정답 뺀 축)                47.3±0.2   77.1±0.0   83.7±0.1
+ L1 1e-5                            48.1±0.2   77.5±0.0   84.5±0.3
                                      **+3.2**   **+2.5**    +0.2
```

- **백본을 키우는 것은 졌다.** ViT-B/14(768)가 어느 차원에서도 ViT-S 를 못
  이긴다 — top-5 는 3%p 넘게 진다. ViT-L 은 그래서 안 뽑았다
- **L2 는 지렛대가 아니다.** `wd 0` 과 `wd 1e-1` 이 같다(1000배 범위에서 한
  자리도 안 움직인다). 안 움직인 것은 과적합이 아니라서가 아니라 **L2 가 이
  자료에서 안 듣는 것**이었다
- **듣는 것은 축을 죽이는 쪽**이다 — PCA(분산 작은 축을 버린다)와 L1(라벨을
  보고 죽인다). 같은 "규제" 라는 말로 묶이지만 다른 일이다
- **편향을 감쇠에서 빼는 것은 아무 차이가 없다** — 관례였을 뿐이라는 것을 이제
  재어서 안다

**PCA 축을 전부로 잡으면 샌다** — 재는 날 조각의 분산이 축에 들어 top-1 이
47.3 → 48.5 로 부푼다. 재는 자리에서는 `--pca-unlabeled` 를 쓴다.

**갈아 끼우기를 접었다** (위 절). +2.5%p 가 좋게 하는 것이 `묶음 제안` 의
top-5 인데 그 길을 안 쓴다. 게다가 갈아 끼우면 `LINK_DIST`(0.06 — 384차원에서
실측한 값)를 다시 재야 하고 격자 순서가 통째로 흔들린다 — **안 쓰는 것을
좋게 하려고 쓰는 것을 흔드는 셈**이다. 개체 수가 늘거나 notch 축이 서면 그때
함께 올린다. 연장은 남는다(`--folds`·`--pca`·`--l1`·top-10).

머리를 키우는 것(2층 MLP · ArcFace · 백본 마지막 블록 녹이기)은 **자료가 는
뒤**다 — 지금 자료에서 손잡이 2만 개는 쓸 데가 없다는 것이 ViT-B 로 확인됐다.

### 지금 무엇이 top-1 을 정하나

    전체 top-1 29.7% · top-5 64.5%   (격자 4,059 · 개체 42 · 질의 437)

    그 개체가 가진 조각 수     4~9장 11.1% · 10~19장 30.6% · 20장 이상 33.8%
    확신(1·2등 차이) 상위 10%  69.8% 맞음      ← **1등 점수는 쓸모없다** (38%)
    notch 사분위              매끈한 쪽 24.2% · 패인 쪽 32.5%

**늘릴 것은 한 개체의 사진 수와 개체 수 둘 다**이고, 확신을 읽을 때는
**1·2등이 벌어진 정도**를 본다(코사인이 다 0.9대라 절대값은 뜻이 없다).

### 화면

```
왼쪽 격자 1,735장  ·  오른쪽 상자 목록
정렬  닮은 것끼리(임베딩 사슬) · 특징적인 것부터(notch) · 날짜순
고르기 클릭 더하기·빼기 · Shift 범위 · 빈 곳 해제 · 우클릭 원본
넣기  끌어서 상자로 · 맨 아래 빈 상자에 넣으면 새 상자
붙박이 `상자에서 뺌` · **보류함** · `새 상자`
상자  대표 한 장만 보인다 (`보기` 로 펴서 별표로 고른다)
```

**보류함은 개체가 아니라 자리다** (`Individual.holding`). 어디에 넣을지
모르겠는 것을 아무 상자에나 넣으면 그 상자가 오염되고, 미분류로 두면 다음에
또 같은 고민을 처음부터 한다. `catalog()` 가 안 센다.

`Identification` 은 **쌓인다** — 상자를 옮긴 자취가 남아야 판단이 언제
바뀌었는지 잴 수 있다. `individual=NULL` 은 뺀 것이고 그것도 답이다.

### notch 는 볼록껍질로 재면 안 됐다

`convexityDefects` 로 쟀더니 깊이 중앙값이 현의 **19%**, 659개 중 641개가
"특징적" 이었다. 진짜 notch 는 2~5%다 — 볼록껍질과의 차이는 notch 가 아니라
**끝에서 밑동으로 내려오는 자연스러운 휨**을 재고 있었다. 다 특징적이면
아무것도 특징적이지 않다. **자기 자신을 매끄럽게 편 것과 견주는** 쪽으로
바꿨다(`reid.roughness`).

## 이어받기 — 기계를 옮겨 앉는 절차 (2026-08-25 에 쓴 것)

> **여기 적힌 "지금 자리" 는 낡았다.** 8월 25일에 2080ti(JikhanDesktop)로
> 옮겨 앉으며 쓴 글이고, **그 뒤로 자리가 m710q 로 돌아왔다** (`## fin.db 를
> 든 자리` 절에서 세어 확인했다). **절차로만 읽을 것** — 명령과 함정은 그대로
> 유효하다. 다만 `FIN_REID` 는 이제 `reid/v3` 다.

`fin.db` 는 한 기계에서만 연다.

받은 것 — 상자 **5,882** · 판정 6,014 · 개체 판정 **1,298** · 개체 **46** ·
마스크 21,079 · 크롭 5,882장 · 조각 `v1` 3,479 + `v2` 4,064.
`sha256` 이 `MANIFEST.2026-08-24.json` 의 것과 맞고 `integrity_check ok` 다.
덮어쓰기 전에 이 기계에 있던 것이 **NAS 것의 완전한 부분집합**임을 축마다
확인했다(개체 판정·판정·상자·개체 모두 이쪽에만 있는 것 0). 그때 것은
`db/fin.before-restore.2026-08-25.bak` 으로 옆에 있다 — 복원이 확인됐으니
지워도 된다.

```bash
# 넘길 기계에서
python manage.py backup --derived

# 받을 기계에서
git pull
B=$(python -c 'from finseg import nas; print(nas.root() / "dolfinserver2_backup")')
# **자리는 `/srv/dolfinserver2/db/` 다** — 저장소 `db/` 가 아니다 (2026-08-26)
cp $B/db/fin.db.<날짜>.<넘긴 기계>.bak /srv/dolfinserver2/db/fin.db
sha256sum /srv/dolfinserver2/db/fin.db             # MANIFEST 와 비교한다
rsync -a $B/derived/crops/ crops/                  # 10,537장
rsync -a $B/derived/reid/  reid/                   # **v3 7,912**
python manage.py migrate
cd /srv/dolfinserver2 && docker compose up -d       # 정식 화면은 컨테이너다
```

**`cp -r` 말고 `rsync` 를 쓴다.** 조각 `look/` 이 7,912장이라 NAS 에서 몇 분씩
걸리는데, 중간에 끊기면 `cp` 는 어디까지 왔는지 모른다 — 실제로 한 번 끊겨
1,333장에서 멈췄다. `rsync` 는 있는 것을 건너뛰고 이어받는다.

**`FIN_REID=reid/v3` 를 빠뜨리지 말 것.** 안 주면 화면이 `settings` 기본값을
보는데 **`v1`·`v2` 는 2026-08-26 에 지웠다** (`9fc484d`). 개체 판정이 새 조각을
가리키고 있어 **격자만 조용히 빈다.**

**`FIN_PHOTOS` 도 빠뜨리지 말 것.** 기본값 `/srv/dolfinserver/uploads` 는 이
기계에 없다. `~/dolfin-photos/nas → /mnt/p/JikhanJung/dolfinimage` 가 걸려
있으니 뿌리로 `~/dolfin-photos` 를 준다 (DB 는 `nas/2016/03/15/…` 로 들고 있다).

되돌릴 자리도 NAS 에 있다 — `db/fin.before-promote.2026-08-24.bak`(개체판정 574,
옛 상자를 들이기 전). 지금 것은 그것의 정확한 상위집합이다.

**화면까지 봤다** (2026-08-25) — `/`·`/reid`·`/photo`·`/edit`·`/compare`·
`/detect` 가 200 이고 **이미지 세 갈래**(크롭·원본 사진·조각)가 다 나온다.
`/reid` 가 590KB 로 온다 — 격자가 찼다는 뜻이다(빈 격자면 훨씬 작다).
조각은 40장을 골라 sha256 을 견줬고 다른 것 0.

**백업 이름에 기계가 들어간다** (2026-08-25). `fin.db.<날짜>.<기계>.bak` 이고
`MANIFEST.<날짜>.<기계>.json` 이다 — 전에는 날짜뿐이라 **나중에 뜬 기계가
먼저 뜬 기계의 것을 갈아 치웠다.** `--keep` 도 제 갈래만 지운다. 기계 이름
없이 뜬 옛 `fin.db.2026-08-24.bak` 은 어느 기계 것인지 몰라 안 지운다.

**2080ti 에만 있는 것**: `ultralytics`·GPU. m710q 에서는 CPU 로 돌려
`infer` 13분 / `infer_base` 35분 / DINOv2 임베딩 6분이 걸렸다.

## fin.db 를 든 자리 — 2026-08-26 · **m710q**

**세어서 정했다.** 문서는 2080ti(JikhanDesktop)라고 적고 있었는데 재 보니 이
기계가 모든 축에서 앞선다. NAS 백업에도 `…2080ti.bak` 갈래가 **아예 없다** —
새 이름 규칙(`fin.db.<날짜>.<기계>.bak`)이 생긴 뒤로 뜬 것은 `m710q` 것뿐이다.

| | 문서가 적고 있던 것 (2080ti · 08-25) | NAS 마지막 백업 (m710q · 08-25 11:27) | **여기 지금** |
|---|---:|---:|---:|
| `image` | 4,181 | 4,181 | **7,927** |
| `box` | 5,882 | 5,882 | **10,537** |
| `review` | 6,014 | 6,039 | **6,039** |
| `identification` | 1,298 | 1,300 | **1,319** |

> **2080ti 는 확인하지 못했다.** 지금 꺼져 있다(tailnet 에서 `jikhandesktop`,
> 9시간 전 마지막). 거기 안 뜬 채 남은 판정이 있을 가능성은 **NAS 백업이
> 없다는 것으로만** 낮다고 말할 수 있다 — 켜지면 세어 볼 것.

### `fin.db` — 다시 만들 수 없는 것 (`/srv/dolfinserver2/db/`)

크기 35,565,568 · 마지막 수정 **2026-08-25 14:13** · `integrity_check ok` ·
`journal_mode=wal` · sha256 `d0ea0bd4c586…`

| 테이블 | 건수 | | 테이블 | 건수 |
|---|---:|---|---|---:|
| `image` | 7,927 | | `mask` | 25,655 |
| `box` | 10,537 | | `review` | **6,039** |
| `crop` | 10,537 | | `identification` | **1,319** |
| `run` | 57 | | `individual` | **46** (개체 45 + 보류함 1) |

**사람이 손댄 마지막 때** — 이 셋만 다시 만들 수 없다.

| | 처음 | 마지막 | 마지막 id |
|---|---|---|---:|
| `review` | 2026-08-15 15:48 | **2026-08-25 02:08** | 6,077 |
| `identification` | 2026-08-23 22:11 | **2026-08-25 05:13** | 1,319 |
| `individual` | 2026-08-23 22:11 | 2026-08-24 13:21 | 47 |

`review` 의 마지막 id(6,077)가 건수(6,039)보다 큰 것은 **지운 자리가 있어서**다
(`individual` 도 47 대 46). **번호 공간을 가를 때 보는 것은 건수가 아니라 이
id 다** (`## 서버를 둘로 나눈다`).

상자 출처 — `yolov5` **9,847** · `yolo11` **690**(우리 검출기가 새로 찾은 것).
지금 쓰는 마스크 — `seg-v3-s` **10,247** · `sam2.1` 47 · `seg-v2-s` 23 ·
`seg-v1-s` 4. 관찰일 **73일** (2016-03-15 ~ 2019-11-05).

### `derived/` — 다시 만들 수 있지만, 다시 만들려면 사진과 시간이 든다

| | 파일 | 크기 |
|---|---:|---:|
| `crops/` | 10,537 | 703MB |
| `reid/v3/` | `look/` 7,912 | 230MB |
| `reid/fin-filter-emb.npz` | | 13MB |

**`v1`·`v2` 는 2026-08-26 에 지웠다** (`9fc484d` — 명령이 어느 격자를 읽을지
박아 두던 것도 함께 걷었다). `v3` 안에서 **화면이 읽는 것은 넷뿐이다** —
`items.json`(n 7,912 · `min_area` 15,000) · `emb-dinov2.npz` ·
`cls-dinov2.npz` · `look/`. `chips.npz`(32MB) · `emb.npz`(14MB) ·
`curves.npz`(3.5MB)는 명령이 쓴다. **GCP 에 갈 때 이 구별이 181MB 대 242MB 를
가른다** (`devlog/20260826_001` 5절).

### 가중치 — `runs/` 2 개 · 60MB

`seg-v3-s` · `pose-v1`. **검출·옛 분할 가중치는 이 기계에 없다** — NAS
`dolfinserver2_backup/weights/` 에 있다.

### 로컬 `db/` — 저장소 밖(`.gitignore`)

| | 크기 | 무엇 |
|---|---:|---|
| `fin.db` | 36MB | **지금 것** |
| `fin.before-promote.2026-08-24.bak` | 36MB | 되돌릴 자리(옛 상자를 들이기 전). 지금 것은 이것의 상위집합 |
| `fin-test.2026-08-24.bak` | 36MB | 승격 실험 사본. 승격이 끝났으니 지울 것 |

**옛 운영 DB 는 `db/` 가 아니라 형제 저장소에 있다** —
`../dolfinserver/db.sqlite3` (927MB · 사진 544,585 · 상자 999,528).
`FIN_SRC_DB` 의 기본값이 그 자리이고, **표본을 넓히는 유일한 공급원이다.
지우지 말 것.**

### NAS `dolfinserver2_backup/`

- `db/fin.db.2026-08-25.m710q.bak` (08-25 11:27) — **가장 최근**. 다만 지금
  로컬 것이 이보다 앞선다(사진 +3,746 · 상자 +4,655 · 개체판정 +19).
  **떠야 한다** (`manage.py backup --derived`)
- `db/fin.db.2026-08-24.bak` · `db/fin.before-promote.2026-08-24.bak` ·
  `db/fin-test.2026-08-24.bak` — 기계 이름 없는 옛 갈래
- `MANIFEST.2026-08-25.m710q.json` · `derived/` · `weights/`

## 서버를 둘로 나눈다 (2026-08-26 에 정했다)

**개체를 만들고 지느러미를 개체에 넣는 일은 GCP(`dolfinid2`)에서만 한다.
나머지는 m710q 에서만 한다.**

되는 이유는 **`Identification`·`Individual` 을 쓰는 곳이 `review/views.py`
하나뿐**이기 때문이다 (명령은 하나도 안 쓴다 — `finseg/reid.py` 는 읽기만).
GCP 가 그 둘의 유일한 주인이면 **병합이 아니라 통째로 갈아 끼우기**가 되고,
m710q 는 읽기 사본만 든다 (`reid_cls` 가 그것으로 배운다).

| 방향 | 나르는 테이블 | 성격 | |
|---|---|---|---|
| m710q → GCP | `Image`·`Run`·`Box`·`Crop`·`Mask`·`Review` | upsert | **섰다** (2026-08-27) |
| GCP → m710q | `Individual` · `Identification` | 통째로 갈아 끼움 | **섰다** (2026-08-27) |

**보내는 길**: `deploy/gcp/from_work_to_reid.sh` — `export_from_work_to_reid`
로 담고, 보내고, 저쪽 컨테이너 안에서 `import_from_work_to_reid` 로 넣는다
(헛돌리기 한 번 먼저). **이름에 방향이 들어 있어** 반대 방향이 그대로 짝이 된다.

**`INSERT OR REPLACE` 를 쓰면 안 된다.** SQLite 에서 그것은 부딪히는 행을
지우고 다시 넣는데, `Identification.box` 가 `CASCADE` 라 **상자 하나를
갱신하려다 그 상자에 달린 개체 판정이 조용히 함께 지워진다.** 참된
upsert(`ON CONFLICT DO UPDATE`)만 쓴다 — 행을 안 지우므로 CASCADE 가 안 돈다.
넣은 뒤에 저쪽 레인을 **다시 세서** 어긋나면 통째로 되돌린다.

**되받는 길**: `deploy/gcp/from_reid_to_work.sh`. **갈아 끼우는 쪽이라 더
위험하다** — 보내는 길은 최악이 "덜 갔다" 지만 이쪽은 최악이 "사람의 판정을
지웠다" 다. 넣기 전에 넷을 본다: 담긴 것이 개체 레인뿐인가 · 칸이 같은가 ·
**줄어들지 않는가**(저쪽은 쌓기만 하니 줄면 사고다) · **가리키는 상자를 여기가
다 갖고 있는가**(없으면 "보내는 길을 먼저 돌려라" 라고 말한다). 그리고
갈아 끼우기 **전에 한 벌 뜬다**(`fin.before-reid-import.*.bak`).

**언제 돌리나** — 되받는 길은 **쓰려고** 돈다(분류기를 다시 배우기 직전,
성적을 잴 때). **판정이 사라질 걱정으로 도는 것이 아니다**: 그것은 백업이
맡는다(저쪽 매시 + 여기 05시 당겨오기). 보내는 길은 **격자를 갈아 끼웠을 때**.
둘 다 사건에 걸리는 일이지 주기가 아니다 — 다만 사람이 잊으니 주 1회쯤
안전망으로 두면 된다.

실측 (2026-08-27) — 보내는 길 27.7MB · 10초, 되받는 길 96KB. `finseg_review` +97 이 건너갔고
`finseg_identification` 1,323 · `finseg_individual` 46 은 **그대로**였다
(그 4건은 GCP 에서 만든 것이다).

**`Review` 도 주인이 하나다** (2026-08-26). `/reid` 격자에서 "지느러미 아님"
을 쓰던 길(`reid_cls_set`)을 걷어냈다 — 그래서 **번호 공간을 가를 일이 없고**,
되받는 것은 `Individual`·`Identification` 둘을 통째로 갈아 끼우는 것뿐이다.

거르는 일은 **검토 화면의 `re-ID 후보` 대기열**이 받는다 (`batch` 의 `reid`
모드). 격자에 실렸는데 사람이 한 번도 안 본 것을 **`p(fin)` 낮은 것부터** 낸다
— `fin_filter --write` 가 `items.json` 에 적어 둔 값이다. 지금 **6,174장**이고
맨 앞이 `p(fin)=0.0` 이다. 이미 찍힌 것은 `/reid` 에서 흐리게 **보이기는 한다**
(읽기만 한다).

`/reid` 에서 거르던 것을 여기로 옮긴 이유는 하나다 — **거르는 것이 개체를
정하는 일과 다른 일**이라서, 두 자리에 나누면 `Review` 의 주인이 둘이 된다.

**막는 자리는 앱이다 — `FIN_ROLE`** (2026-08-26 에 넣었다).

| | 걸리는 길 |
|---|---|
| `work` (기본) | `/` `/review` `/edit` `/compare` `/detect` `/photo` `/healthz` |
| `reid` | `/reid` `/catalog` `/api/reid/*` `/reid/chip/…` `/healthz` |

**nginx 로 막으면 안 된다.** 정작 새는 자리가 안 막힌다 — `runserver` 앞에는
nginx 가 없는데 구멍이 바로 거기다: **`/reid` 는 열기만 해도 보류함
`Individual` 이 하나 생긴다** (`review/views.py` 의 `get_or_create`). 구경도
쓰기라 링크를 숨기는 것으로도 안 막힌다. **URLconf 에서 아예 뺀다**
(`review/urls.py` 의 `patterns_for`) — 경로가 없으면 실수로 도는 길이 없다.
템플릿이 `{% url %}` 를 하나도 안 써서(링크가 전부 경로다) 빼도 안 깨진다.

기본은 `work` 라 **이 기계의 습관은 안 바뀐다** — `runserver` 로 띄우면
`/reid` 만 사라진다. GCP 의 `.env` 에만 `FIN_ROLE=reid` 를 적는다. 여기서
격자를 눈으로 봐야 하면 시험 서버(8085)가 `reid` 로 떠 있고, **그것은 DB
사본을 본다.**

`/healthz` 가 역할을 낸다 — **배포가 뒤바뀐 것을 그것으로 잡는다.** 개체 분류를
받을 자리가 `work` 로 떠 있으면 화면은 멀쩡히 200 을 내면서 그날 판정을 한 건도
못 받는다. 차림표에도 `작업 자리`·`re-ID 자리` 로 늘 보인다.
nginx 막기는 버리지 않았다 — 앱이 정본이고 그것은 이중이다.

**`fin.db` 를 통째로 주고받는 길은 버렸다.** 격자를 v4 로 갈면 새 상자·크롭이
GCP DB 에도 있어야 조각이 붙는데, 그때 DB 를 통째로 보내면 GCP 가 쌓은 판정이
날아간다.

## 운영·시험·개발 세 자리 (2026-08-26)

| | 무엇 | 역할 | DB |
|---|---|---|---|
| `/srv/dolfinserver2` · nginx **8085** | **운영** | `work` | **진짜 `fin.db`** |
| GCP `dolfinid2` | **운영** | `reid` | 거기 `fin.db` (GCP 가 주인) |
| `/srv/dolfinserver2-test` · nginx **8086** | 시험 | 골라 띄운다 | NAS 백업 사본 |
| `manage.py runserver` | 개발 | 골라 띄운다 | 사본 (`FIN_DB`) |

**`runserver` 는 이제 시험이다.** 정식 검토 화면은 컨테이너다.

**`fin.db` 가 `/srv/dolfinserver2/db/` 로 옮겨 갔다.** 저장소 `db/` 를 안 쓴다 —
컨테이너가 보는 파일과 파이프라인이 쓰는 파일이 다르면 **판정이 한쪽에만
쌓인다.** 지금은 둘이 같은 inode 를 연다. `settings` 의 `FIN_DB` 기본값이 그
자리이고, 없는 자리를 가리키면 시스템 검사가 말한다 (`finseg/checks.py` —
**안 말하면 빈 DB 가 만들어지고 명령이 아무 말 없이 0건을 처리한다**).
저장소에 남은 `db/fin.db.repo-retired-2026-08-26` 은 옮기던 날의 사본이다.

**크롭·조각은 저장소 것을 읽기전용(`:ro`)으로 건다.** 파생물이고 파이프라인이
계속 다시 쓰는 것이라 사본을 두면 갈아 끼울 때마다 낡는다. `/srv` 에 두었던
945MB 사본은 걷었다.

**시험 DB 는 NAS 백업에서 뜬다** (`deploy/host/test_db.sh`). 사본이라 무엇을
해도 사람의 판정이 안 다치고, **덤으로 백업이 성한지를 잰다** — 백업에서
복원해 화면이 도는 것을 본 적이 없으면 그것은 백업이 아니라 파일일 뿐이다.

```bash
sudo deploy/host/bootstrap.sh            # 운영 자리 · nginx 8085
sudo deploy/host/bootstrap.sh --test     # 시험 자리 · nginx 8086
deploy/host/test_db.sh                   # 시험 DB
deploy/build.sh 0.2.1                    # 테스트 → 버전 → build (→ --push)
sed -i 's/^IMAGE_TAG=.*/IMAGE_TAG=0.2.1/' /srv/dolfinserver2/.env
cd /srv/dolfinserver2 && docker compose up -d
```

```bash
curl -s http://m710q:8085/healthz | python3 -m json.tool
# {"status":"ok","version":"0.2.0","role":"work","box":10537,
#  "individual":45,"identification":1319,"reid_items":7912}
```

`/healthz` 는 **격자가 비면 `reid` 자리에서 503 을 낸다** — 조각이 덜 오면
화면은 200 을 내면서 격자만 조용히 비어서, 화면만 봐서는 자료가 덜 온 것인지
분류가 안 된 것인지 갈리지 않는다.

**`DEBUG=0` 이면 Django 가 크롭·사진을 안 낸다** (`finweb/urls.py`). nginx 의
`/crops`·`/photos` 가 받는다. 조각(`/reid/chip/…`)만 Django 가 낸다 — 같은
번호로 `look/` 과 `chips/` 중 있는 것을 고르는 판단이 view 에 있어서다.

## GCP(`dolfinid-2`)가 섰다 (2026-08-26)

**`dolfinid-2` 는 cdGTS·ghdb·fcmanager·strati2026 이 이미 도는 GCP VM 이다**
(2 vCPU · 3GB · 여유 26GB). ssh 별칭 `dolfinid`, sudo 무암호, tailscale
`100.126.94.48`.

| | |
|---|---|
| 자리 | `/srv/dolfinserver2` · 컨테이너 `127.0.0.1:8012` |
| 화면 | **`https://reid.nopeoplestime.info`** — **공개** (2026-08-27) |
| 역할 | `reid` · 격자 7,912 · 상자 10,537 |
| 백업 | 시간별(그쪽 cron) → m710q 가 05시에 당겨온다 → NAS |

**밖으로 열었다** (2026-08-27). 여는 조건이 둘이었다 — **https**(certbot,
형제 `cdgts` 와 같은 방식)와 **`.env` 의 `FIN_HTTPS=1`**. 뒤엣것을 안 켜면
Django 가 nginx 뒤에서 스스로를 http 로 알아 CSRF 가 스킴 어긋남으로 막는다
(**코드를 맞게 넣어도 403**). 그 손잡이 하나가 `SECURE_PROXY_SSL_HEADER` 와
세션·CSRF 쿠키의 `Secure` 를 함께 켠다 — 따로 두면 하나를 빠뜨린다.

**tailnet 8085 는 닫았다.** 들어가는 길을 하나로 줄인 것이고, 대신 **certbot 이
멎으면 들어갈 뒷문이 없다** — 급하면 `deploy/gcp/nginx.conf` 에 tailnet 리스너를
다시 얹는다 (ssh 와 `docker compose` 는 그대로다).

`FIN_ALLOWED_HOSTS` 에 `127.0.0.1` 을 함께 적었다 — **smoke 가 그리로 묻는다**
(`http://127.0.0.1:8012/healthz`). 도메인만 적으면 배포가 제 검사에서 막힌다.

```bash
deploy/gcp/install.sh          # m710q → GCP 로 호스트 파일 + bootstrap
deploy/gcp/ship.sh 0.4.0       # 이미지를 ssh 로 실어 나른다 (레지스트리 안 거침)
deploy/gcp/seed.sh             # DB(검증된 스냅샷) · 조각 · 크롭  — 27초에 갔다
ssh dolfinid /srv/dolfinserver2/deploy.sh 0.4.0    # 스냅샷 → 스왑 → smoke
```

**`seed.sh` 는 두 번째부터 `--no-db` 다.** GCP 가 뜬 뒤로 그쪽 `fin.db` 는
개체 판정의 **주인**이다 — 밀어 넣으면 그 판정이 사라진다. 스크립트가 거기
DB 가 있으면 멈춘다.

**이미지는 Docker Hub 를 거친다** — `honestjung/dolfinserver2`(공개),
`build.sh <버전> --push`. GCP 의 `deploy.sh` 가 당겨서 뜬다. `ship.sh`
(`docker save | ssh docker load`, 150MB 에 19초)는 **레지스트리가 막혔을 때의
뒷문**으로 남겨 뒀다.

## 문 — 접속 코드 (2026-08-26)

**이 앱에는 사람마다의 인증이 없다.** `login_required` 도 없고 쓰기 경로가
열려 있어 주소를 아는 사람이 판정을 지울 수 있다. `FIN_ACCESS_CODE` 로 문을
막았다 — 처음 들어오면 `/enter`, 맞히면 세션에 표가 30일 남는다.
**m710q 운영과 GCP 에 걸었고, 시험 자리와 `runserver` 는 안 걸었다**(사본이다).

막는 자리는 미들웨어다(`review/gate.py`) — 뷰마다 붙이면 새 뷰 하나가 빠지는
날이 온다. **기본이 막힘이고 예외만 적는다**: `/enter`·`/healthz`·`/static`.
`/healthz` 는 **문 밖에서는 수를 안 낸다** — 밖에서 상자·개체가 몇인지까지
알려 주면 문을 세운 뜻이 절반 없어진다.

**인증의 대신이지 인증이 아니다** — 누가 했는지가 안 남고(`Review.reviewer` 가
`NULL`), 한 사람만 뺄 수 없고, TLS 없이 공개하면 평문으로 간다.

## `/reid` 화면 — 윤곽·밑동과 찍힌 차례 (2026-08-27)

**윤곽·밑동을 조각 위에 얹는다** (`o` 로 토글). `reid.overlay` 가 `chip()` 과
**같은 `frame()`** 으로 옮기므로 자리가 정확히 맞는다. 좌표는 0~1 로 적어
`look/`(사람이 보는 것)과 `chips/`(모델이 먹는 것)의 크기 차이를 타지 않는다.

**그림에 구워 넣지 않았다** — `chips/` 에 선이 들어가면 모델이 그 선을 배운다.

`items.json` 에 싣는다 (`reid_chips --overlay-only` 로 덧쓴다 — 조각을 다시
만들면 한 시간이 넘는데 여기서 더하는 것은 옮겨 적는 일뿐이라 **22초**다).
꼭짓점을 24로 줄였다: 마스크는 중간값 65개인데 notch 를 보는 데 그만큼 필요
없고, 격자는 조각을 한꺼번에 실어 보내는 화면이라 그대로 실으면 페이지가 몇
배가 된다. 실측 — items.json 1.1 → **4.2MB**, 페이지는 gzip 으로 **1.3MB**.

**뒤집힌 것에만 `⇄` 를 붙인다** (4,407 / 7,912). 조각은 앞쪽이 늘 왼쪽에 오게
세운 것이라 원래 오른쪽이 앞이던 사진은 좌우로 뒤집혀 있다 — 원본과 나란히
놓고 볼 때 그것을 모르면 다른 지느러미로 읽는다. 안 뒤집힌 쪽은 원본 그대로라
말할 것이 없다.

**특수 상자 넷**(빼기·새 상자·임시보관함·지느러미 아님)은 들어오는 칸 수에
따라 앉는다 — 한 칸이면 넷을 세로로 반 칸씩, 네 칸이면 각각 온 칸.

**찍힌 차례로도 본다** (`날짜·시간순`). 전에도 `날짜순` 이 있었는데 날짜까지만
보고 그 안은 상자 번호였다 — 잇달아 찍은 것이 흩어졌다. `Image.exifdatetime`
으로 세우되 **여기 시각으로 바꿔서** 쓴다: `USE_TZ` 라 DB 에는 UTC 로 있어
그대로 쓰면 이른 아침 것이 **전날로 보이고**, 화면이 말하는 날과 정렬 축의
날이 어긋나면 사람이 그것을 오류로 읽는다. 격자 파일이 아니라 **DB 에서
읽는다** — 구워 두면 격자를 갈아 끼울 때까지 낡는다.

## 개체에 번호와 별명 (2026-08-27)

`Individual.name` 이 **카탈로그 번호**다 (`JTA001`). 별명(`제돌이`·`춘삼이`)은
`nickname` 으로 따로 받는다 — 한 칸에 섞으면 정렬도 중복 검사도 옛 카탈로그와
맞춰 보는 일도 전부 괄호를 파싱하게 되고, **별명이 붙을 때마다 번호가 든
문자열을 건드리게 된다.**

번호는 UNIQUE, **별명은 아니다** — 겹치는 별명은 사람이 알아보는 문제이지
자료가 깨지는 문제가 아니다. 형식도 강제하지 않는다.

**이름을 바꿔도 자료가 안 흔들린다.** 판정·성적·분류기가 전부 `id` 로 묶고
(`catalog()`·`reid_cls`·`Identification`), 특수 상자는 `kind` 로 찾는다.
**다만 GCP 에서만 바꿀 수 있고 바꿔야 한다** — 여기서 고치면 다음 되받기에
통째로 갈아 끼워지며 사라진다 (`work` 역할에서는 그 길이 안 걸린다).

상자 목록은 **이름순이되 숫자를 숫자로 읽는다** — 그냥 글자로 견주면
`상자 11` 이 `상자 2` 앞에 오고 `JTA010` 다음이 `JTA100` 이 된다.

## 개체가 아닌 자리 둘 (2026-08-27)

`Individual.holding`(참/거짓)을 **`kind`(갈래)** 로 넓혔다 (`0015_individual_kind`).

| | |
|---|---|
| `hold` **임시보관함** | 옛 `보류함`. "곧 뺄 것" 처럼 읽혔는데 몇 달 머무는 것이 있다 |
| `notfin` **지느러미 아님** | re-ID 자리가 `Review` 를 못 쓰니 **제 레인으로 말해 둔다** |

**갈래마다 하나뿐**이다(`UniqueConstraint`) — 둘이 되면 어느 쪽에 넣었는지에
따라 결과가 갈리는데 그 사실이 눈에 안 띈다. 마이그레이션이 자료를 옮긴다:
자동 생성본은 옛 칸을 그냥 지우는데 그러면 **임시보관함의 312장이 진짜 개체로
둔갑한다.**

**옮겨 적는 것은 사람이 한다.** GCP 에서 `지느러미 아님` 에 넣으면 되받은 뒤
검토 화면의 **`저쪽에서 아니라 한 것`** 대기열에 뜨고, 거기서 진짜 분류
(`몸통`·`주둥이`·`새`…)를 눌러 `Review` 로 적는다. 기계가 `기타` 같은 것으로
대신 적으면 **그 거짓 라벨이 검출·분할 학습 자료로 그대로 나간다.** 적고 나면
대기열에서 빠진다.

## 주고받기 — 하루 한 번 (2026-08-27)

```
30 6 * * *  deploy/gcp/exchange.sh        # 되받기 → 보내기
```

**되받기가 먼저다** — 저쪽 판정을 가져와야 분류기를 다시 배운다. 06:30 인 것은
05:00 의 백업 당겨오기 뒤라야 저쪽 스냅샷이 오늘 것이어서다. **분류기 다시
배우기는 안 넣었다**: 성적이 바뀌는 일이라 사람이 보고 있어야 한다 — 자동으로
갈아 끼우면 어느 날 나빠진 것을 며칠 뒤에 안다. 되받기가 뜨는 스냅샷은 최근
7벌만 둔다.

## 백업 — 시간마다 (2026-08-26)

`fin.db` 가 `/srv` 로 옮겨 가면서 백업이 더 급해졌다. `.guides/web/data-safety.md`
의 계약을 형제(`/srv/dolfinserver/scripts/backup_db.py`) 구조로 얹었다.

```
10 * * * * python3 /srv/dolfinserver2/scripts/backup_db.py >> …/logs/backup.log 2>&1
```

`/srv/dolfinserver2/backup/hourly/fin_<기계>_<날짜>_<시>.sqlite3` · **24벌**
(`RETAIN_COUNT × 주기 ≥ 오프사이트 간격` — 튜닝값이 아니다) · 29MB × 24 = 700MB.
**04시 회차만** NAS `dolfinserver2_backup/daily/` 로 (90일 + 매달 1일 영구).

**형제들과 달리 하루가 아니라 시간마다다.** `fin.db` 가 36MB 라 24벌이 700MB
밖에 안 되고, 사람이 판정을 넣는 화면이 하루 종일 돈다 — 하루치를 잃는 것과
한 시간치를 잃는 것은 다르다.

계약에서 지키는 것: **스냅샷을 검사하고**(라이브가 아니라) 걸리면 **정리를
건너뛰고** 증거본과 센티넬을 남긴다(§2 — 이 규칙을 어긴 형제가 실제로 성한
스냅샷 0개가 됐다) · 하위 트랙은 **검증된 스냅샷을 소비**한다(§4 — NAS 도
시험 자리도) · `journal_mode=DELETE`(§5) · **검증 이후에** `django_session`
삭제 + `VACUUM`(§7).

**이름에 기계가 들어가고 정리도 제 갈래만 본다** — 곧 GCP 에도 진짜 `fin.db`
가 생겨 같은 NAS 로 온다.

막히면 `/healthz` 가 **`degraded`(200)** 다. 503 은 "트래픽 보내지 말라" 는
뜻이라 배포의 liveness 대기를 멈춰 세워 **게이트가 아니라 배포 장애가 된다.**

```bash
python3 /srv/dolfinserver2/scripts/backup_db.py   # 손으로
deploy/host/test_db.sh                            # 그 스냅샷으로 되돌려 본다
```

**되돌려 봤다** (2026-08-26) — 시간별 스냅샷으로 시험 자리를 세우니 상자
10,537 · 판정 6,039 · 개체판정 1,319 로 맞았다. **"백업이 돈다" 가 아니라
"백업이 복원된다" 여야 한다.**

시험은 `finseg/tests_backup.py` 8종. **잘 도는 백업이 아니라 실패했을 때의
행동을 잰다** — 계약이 지켜지는지는 손상됐을 때에만 드러나고 그때는 늦다.

`manage.py backup` 은 다른 일이다 — 기계 사이 인계용(가중치·`--derived`).
자리도 갈라 뒀다 (`dolfinserver2_backup/db/` 대 `…/daily/`).

## 검토 화면 (`review/`)

```
돌고래  1 등  2 꼬리  3 주둥이  4 머리  5 몸통  6 가슴
아님    q 새   w 사람   r 기타          배경  9 아무것도아님
축      e 날 · p 아래경계 · f 앞쪽(순환) · x 교정필요 · 점을 끌면 밑동
보류    s 모르겠다 — 판정을 안 남긴다 (묶음 저장에서만)
윤곽    n 편집 창을 연다 (거기서 다시 n 이면 상자에서 새로 그린다)
이동    [ ] 앞뒤 · Home End 처음/끝 · Enter 저장(되짚기에서는 다음 페이지)
대기열  [검토할 것] [검토한 것] [교정 대기] [엔진 바뀜] [새 검출] [윤곽 없음]
```

**대기열이 여섯이다.** `윤곽 없음` 은 오늘 붙였다 — 판정이 붙으면 `검토할
것`·`새 검출` 에서 빠지고, 마스크가 없으면 `엔진 바뀜` 에 안 걸리고,
`verdict='fix'` 가 아니면 `교정 대기` 에도 안 걸려서 **53개가 어디에도 안 뜬
채 남아 있었다.** 화면은 "남은 일 없다" 고 말하는데 `export_yolo` 는 그 상자가
든 크롭을 통째로 뺐다 — 같은 크롭에 걸친 남의 상자까지 함께.

**키는 분류 순서와 따로 둔다** (`models.CLASS_KEYS`). 순서는 YOLO 클래스 번호라
못 바꾸고, 키는 손에 익는 것이라 사람 쪽 사정으로 정한다.

- **되짚기는 즉시 저장된다.** `검토한 것`·`교정 대기` 는 고치면 바로 들어가고
  칸이 초록으로 번쩍인다. `검토할 것`·`엔진 바뀜`·`새 검출` 은 묶음 저장이다
- **원본 사진** `/photo/<box_id>` — 휠 확대 · `Space`+움직임 이동 · `1` 이 상자로
- **윤곽 고치기** `/edit/<box_id>` — **원본 사진이 깔린다.** 상자로 꼭짓점 골라
  함께 옮김 · `Del` 삭제 · **노란 점으로 밑동도** · `f` 앞쪽 · `Ctrl+Z`.
  **`Esc` 로는 안 닫힌다**. 마스크가 아예 없으면 `n` 으로 **상자에서 출발 윤곽**을
  만든다 — 한 점도 안 옮기면 저장이 막힌다(상자 네모가 사람 것인 척 들어가면 안 된다)
- **엔진 비교** `/compare?runs=5,16,23` — 낮은 IoU 부터 보여 준다
- 손 안 댄 묶음은 `Enter` 로 저장 안 된다 (`y` 나 저장 단추라야)

## 모델을 밖으로 냈다 (2026-08-24)

```
/detect                       브라우저에서 돈다 (WebGPU · 추론 94ms)
bundles/<이름>/               model.onnx · seg.onnx · onnxdet.py · MODEL.json
DolfinDesktop2                폴더째 훑는 데스크톱 (연구자용 · 아직 뼈대)
```

**`finseg/onnxdet.py` 가 전처리·NMS 의 원본이다.** 세 곳이 그것을 쓰고, 묶음이
`rules_sha256` 을 들어 받는 쪽이 짝을 확인한다 — **어긋나도 예외가 안 나고
성적만 나빠진다.**

실측으로 축소 필터 하나가 29×21px 상자의 확신을 **0.36 → 0.17** 로 반토막
냈다. 큰 상자는 0.007 밖에 안 달라져 눈으로는 못 잡는다. `PIL.resize` 대신
`PIL.transform` 을 쓴다(cv2 와 1/255 안쪽). **canvas 도 같은 함정**이 있어
`imageSmoothingEnabled = false` 로 껐다.

**NMS 는 모델 밖에 있다**(`nms=False` 로 내보낸다). GPU 셰이더에 안 맞기도 하고,
밖에 두면 **문턱을 다시 안 돌리고 바꿔 볼 수 있다.**

## 검토가 다음 학습으로 이어진다 (2026-08-23)

**`export_detect` 가 `fin.db` 를 읽는다.** 오래 끊겨 있던 자리다.

| | 옛것 | 지금 |
|---|---|---|
| 사진 | 2,508 | **4,240** |
| 상자 | 5,532 | **9,795** |
| 사람이 그린 상자 | 330 | **749** |

인정한 상자 **419개**가 더해지고(옛 검출기가 놓쳐 아무 라벨에도 없던 자리),
아니라고 한 옛 상자 **268개**가 빠지고(옛 DB 에서는 `fin` 으로 나가던 것),
`fin.db` 사진 **1,758장**이 새로 들어온다. 되돌리려면 `--no-fin-db`.

**`--date` 로 훑은 날의 사진만 쓴다.** 검출기 둘이 훑고 사람이 전량 판정한
날이라야 "빠짐없이" 에 가깝다 — 그 날짜는 `infer_boxes` 의 run 기록에서 읽는다
(2016-05-07 은 안 훑어서 빠졌다).

## 아직 끊긴 데

### 이 화면은 **한 사람이 쓰는 도구**다 — 여럿에게 열기 전에 셋이 막힌다

`CLAUDE.md` 가 못 박아 둔 전제이고, 밖으로 열어 놓은 지금도 그대로다.

- **사람마다의 인증이 없다.** 문은 `FIN_ACCESS_CODE` **하나를 모두가
  나눠 쓴다**(`review/gate.py`). `Review.reviewer`·`Identification.reviewer` 가
  늘 비는 것이 그래서고, 오늘 붙인 `ip` 는 그 자리를 **절반만** 메운다 —
  주소는 공유되고 바뀐다. **코드가 한 번 새면 다시 만들 수 없는 판정에 쓰기가
  열린다**
- **두 사람이 서로의 판정을 못 본다.** `/reid` 는 뜰 때 한 번 읽고, 그 뒤로는
  `BroadcastChannel` 만 듣는다 — **그것은 같은 브라우저 안에서만** 도는 길이다
  (`reid.html` 의 `new BroadcastChannel("reid")`). 되읽기도 폴링도 없다.
  둘이 같은 조각을 각자 끌면 **나중 것이 조용히 이기고 둘 다 그 사실을 모른다**
- **`새 상자` 가 부딪힌다.** 이름을 `Individual.objects.count() + 1` 로 짓고
  `get_or_create` 하므로(`views.reid_box`), 둘이 동시에 누르면 **같은 이름이
  나와 한 상자를 나눠 쓴다** — 각자 새로 만든 줄 알면서

**그래서 지금 안전하게 열 수 있는 것은 "보는 것" 까지다.** 판정을 쓰게 하려면
`.guides/web/` 의 배포 다섯 동사와 함께 로그인을 채택할 때다 (`TODOs`).

**`val_date` 2016-03-15 는 새 검출기가 훑은 날이다.** 그날의 정답에 **지금
검출기가 찾은 상자가 섞여 있어**, 거기서 **그 검출기의** 재현율을 읽으면 높게
나온다. 다음 검출기를 재는 데는 문제가 없다 — `export_detect` 가 경고를 띄운다.

**`fin.db` 에는 옛 검출기가 무언가 찾은 사진만 있다.** 옛것이 "비었다" 고 본
사진에서 새것이 헛보는 것은 안 잡히므로, 여기서 나올 정밀도는 **낙관 쪽으로
기운다.**

**사람이 그린 상자는 이미 바닥났다.** 옛 운영 DB 를 날짜별로 펴 보니
관찰일 **255일 · 사진 544,585장 · 상자 999,528개**인데, `detect-v2` 가 쓰는 것은
**28일 · 4,240장 = 0.78%** 다. 그런데 **안 쓴 227일에 사람이 그려 넣은 상자가
한 개도 없다** — 749개 전부 이미 들어가 있다.

유일한 예외가 **2016-03-16**(시민과학 판정 1,206건 · `not_fin` 255건)인데,
그날은 사람이 **거르기만 하고 채우지 않아서** 일부러 뺐다(`--exclude-date`).
놓친 지느러미가 배경으로 들어가기 때문이다.

그래서 검출 자료를 더 늘리는 길은 둘뿐이다 — **새 검출기로 훑고 사람이 보거나**
(`infer_boxes` → `새 검출`, 이번에 690개로 해 봤다), **`/photo` 에 상자 추가를
붙이거나**. 앞엣것은 이제 문턱별 정밀도를 알아서 얼마나 깊이 훑을지 정할 수 있다.

**날을 넓히되 날마다 얕게.** 하루에 만 장씩 있는 날이 여럿인데(2018-10-22 는
10,179장) 통째로 넣으면 그날 바다 상태를 외운다. 지금 20일이 날마다 100장
안팎으로 고르게 뽑힌 것은 의도된 설계다.

날짜별 표: https://claude.ai/code/artifact/e4f94596-f308-4b7d-a65c-663c240fae75

## 조용히 새던 곳들 — 전부 같은 종류였다

**"화면은 됐다고 하는데 값이 자료에 안 닿는다."** 이 저장소에서 가장 자주 겪는
것인데 이틀에 예닐곱 번 만났다. 전부 **숫자를 견줘서** 잡혔지 화면을 봐서
잡힌 것이 아니다 — 화면은 다 멀쩡했다.

| 무엇 | 어떻게 드러났나 |
|---|---|
| 점을 끌어도 `polyMoved` 가 안 섰다 | 셋 중 둘이 200 을 받으면서 윤곽만 빠졌다 |
| 판정이 붙었는데 윤곽이 없는 상자 53개 | 대기열 여섯 중 어디에도 안 떴다 |
| 사람이 그린 윤곽을 `label_of` 가 안 셌다 | 대기열이 0인데 `pending` 이 48이었다 |
| `except cv2.error: return None` | 659개가 전부 실패한 것이 "0/659" 로만 보였다 |
| 크기 슬라이더가 안 먹었다 | 딴 코드를 지우다 함께 잘랐는데 화면은 멀쩡했다 |
| backend 를 짐작해 적었다 | CUDA 가 실패했는데 화면은 "CUDA" 라고 말했다 |

앞의 둘은 `verdict`·`polygon_edited` 처럼 **깃발 하나에 기댄 자리**였다. 그래서
`/edit` 의 `commit` 은 이제 깃발과 **값을 함께** 본다 — 화면의 윤곽이 들어온
것과도 출발 윤곽과도 다르면 사람이 옮긴 것이다. 깃발 세우는 길을 또 빠뜨려도
이쪽이 잡는다.

셋째가 특히 조용했다. `verdict`("이 마스크가 맞나")는 마스크가 있어야 물을 수
있는 말이라 서버가 안 적는 것이 옳은데, `label_of` 가 그 빈 칸을 "아직 안 봤다"
로 읽었다. **그리는 일 자체가 자료에 안 닿고 있었다.**

## 알아 둘 것

- **성적을 내기 전에 "이 자가 무엇을 재나" 를 물을 것.** 이틀에 네 번 걸렸다 —
  SAM2 가 자기 출력을 정답으로 · v1 이 자기 출력을 정답으로 · 옛 검출기가 자기
  학습 날에서 · 새 검출기가 아무도 안 본 상자를 헛것으로. 앞의 셋은 숫자를 낸
  뒤에 알았고 넷째는 자를 대기 전에 알았다
- **그 앞 단계가 하나 더 있다 — "이 목록이 무엇을 다 담고 있나".**
  `fin.db` 는 표본이라 사진 92장의 옛 상자 189개 중 109개만 갖고 있었다.
  그것 하고만 견주니 새 상자가 122개로 나왔는데 옛 DB 를 함께 보니 47개였다
  (`infer_boxes --src-db`)
- **밑동을 수면선으로 자동 제안하지 말 것** (`devlog/…0816_001` 3절 ·
  `finseg/baseline.py` 의 실패 기록)
- **중간값이 판정으로 남는 순환을 만들지 말 것** (`devlog/…0816_003` 3절).
  값이 적고 한두 번에 닿으면 순환이 낫다 (`f`)
- **교정을 담은 축은 다시 저장할 때 함께 실어야 한다.** `*_moved`("이번에
  건드렸나")만 보고 판단했다가 **사람이 찍은 밑동 76건을 잃었다** — `*_human`
  ("이미 사람 것인가")이 그래서 있다
- **`itemOf` 와 `sigOf` 는 축을 더할 때 같이 고친다.** 안 그러면 화면은 바뀌고
  저장은 200 을 받는데 값만 안 들어간다
- **`Review` 는 쌓인다, 덮어쓰지 않는다.** 되짚기에서 자기 기준이 언제
  움직였는지 재려면 그 자취가 있어야 한다
- **2080ti 는 Turing(sm_75) 이라 bf16 이 없다.** AMP 는 fp16
- **`fin.db` 를 두 기계에서 함께 열지 않는다.** 합치는 길이 없다 —
  `Review`·`Identification` 이 둘 다 쌓이는 테이블이라 번호가 겹친다. 옮겨 가서
  일했으면 거기서 다시 떠서 가져올 것
- **정답이 다른 두 판의 성적을 세로로 견주지 말 것.** 정답이 늘면 `좁은 후보`
  는 후보가 함께 늘어 어려워지고 `격자 전체` 는 정답만 늘어 쉬워진다 —
  **반대로 움직인다.** `reid_eval` 의 `후보` 열과 `--as-of` 가 그 자리다
- **숫자를 옮겨 적을 때 무엇을 세는 숫자인지 함께 옮긴다.** `TODOs` 가 오래
  "`m710q` 에 판정 717건이 있다" 고 말했는데, 그 717은 **이 기계의 그날 진행
  상황**이 옮겨 붙은 것이었다. 실제로는 0건이다
- **깃발 하나로 "고쳤나" 를 묻지 말 것.** `polyMoved`·`base_moved` 같은 것은
  세우는 길을 하나만 빠뜨려도 조용히 샌다. 값을 견줄 수 있으면 함께 볼 것
- **화면이 자기 상태를 말하는지 늘 의심할 것.** "값은 맞는데 화면이 안 말한다"
  를 하루에 아홉 번 만난 날이 있다. 붉은 줄(`.warn`)이 그 자리다
