# Handoff — 현재 진행 상황

**2026-08-16.** 첫 바퀴 검토를 시작해 **1,488장(64%)** 을 봤다. 그러면서
**스키마가 자료에 반증되어 여러 번 바뀌었고**, 검토 화면이 크게 늘었으며,
**키포인트 모델을 계획보다 앞당겨 붙였다** (4단계 → 지금).

지나간 것은 `devlog/20260816_001`(첫 바퀴) · `_002`(스키마를 뒤집은 이유) ·
`_003`(조작을 고친 것) · `_004`(키포인트 모델·앞쪽 축·잃을 뻔한 교정),
앞으로 할 일은 `TODOs.md`.

## 지금 있는 것

| | |
|---|---|
| 표본 | 관찰일 20일 (2016-03-15 ~ 2019-11-05) · 상자 2,315 · 사진 1,909 |
| 크롭 | 640×640 · 154MB · `crops/` |
| 마스크 | 2,315개 — 상자마다 하나씩. SAM2.1 · 2080ti · run 5 |
| **검토** | **2,315 / 2,315 — 첫 바퀴 완료** (판정 2,874건) · 관찰일 20일 |
| **밑동 라벨** | **1,509** · 윗윤곽 교정 164 |
| **밑동 제안** | **키포인트 모델이 2,255개** 채웠다 (`infer_base`) |
| 사진 원본 | **붙었다** — `/mnt/p/JikhanJung/dolfinimage`, 1,909장 전부 열림 |

`fin.db` 백업: `../fin.db.20260816-*.bak` (저장소 밖. `.gitignore` 의 `*.db` 는
`.bak` 을 안 잡는다 — 안에 두면 `git add -A` 에 딸려 들어간다)

## 서버는 사진 경로를 물려서 띄운다

```bash
FIN_PHOTOS=/home/jikhanjung/dolfin-photos python manage.py runserver 0.0.0.0:8900
```

`~/dolfin-photos/nas` 가 `/mnt/p/JikhanJung/dolfinimage` 로 가는 심볼릭 링크다.
DB 가 `nas/2016/…` 로 들고 있어서 그렇다 — **코드에서 접두어를 벗기지 않은
이유**는 `devlog/…_002` 6절에 있다.

## 1,488장이 말한 것

| | 값 | 뜻 |
|---|---|---|
| `fix` (등지느러미) | **1,080/1,246 = 87%** | **키포인트 제안을 붙여도 안 떨어졌다** (아래) |
| 윗윤곽 교정 | 67 | 편집기를 쓰고 있다. 여전히 드물다 |
| 교정 대기 | **2** | 되살리기 전에는 75였다 (`devlog/…_004` 4절) |
| `cls != fin` | 242/1,488 = 16% | 관찰일 넷에 몰려 있다 (아래) |
| `edges` | `both` 1,227 · **`tip` 18** · `leading` 1 | 순환을 없앤 뒤 드디어 들어온다 |
| `facing` | 사람 213 · 나머지는 **제안이 98%에 붙는다** | |
| `base_partial` | `front` 10 · `rear` 7 · `both` 7 · `unknown` 60 | |

### 키포인트 제안은 오차를 반으로 줄였지만 `fix` 는 못 낮췄다

`val_date`(안 본 날) 기준 밑동 오차가 **37.6px → 21.6px** 인데, `fix` 는
제안 전 84~91%, 제안 후 70·88·87% 로 그대로다. **손은 덜어 주지만 누르는 횟수는
안 줄인다** — 밑동을 정확히 놓는 일이라 21px 도 눈에 보인다. (이 숫자는
되살린 76건이 섞여 있어 다음 바퀴에 다시 봐야 한다.)

### 옛 검출기가 무너지는 날이 **넷**이다

| 관찰일 | `none` |
|---|---|
| **2016-07-20** | **70/138 (51%)** |
| **2018-10-17** | **29/98 (30%)** |
| **2018-08-19** | **30/111 (27%)** |
| 2017-08-28 | 18/115 (16%) |
| 나머지 아홉 날 | 0~7% |

001 에서는 "하루가 통째로 이상하다" 였는데 넷이 되니 **"어떤 조건에서 무너진다"**
쪽이다. 네 날을 나란히 보면 그 조건이 잡히고, 그것이 5단계 검출기 교체의
근거가 된다. **원본 사진을 볼 수 있다** (우클릭 → 원본 사진).

## 스키마가 오늘 이만큼 바뀌었다

**전부 자료를 보기 전에 정한 규칙이 자료를 보고 나서 틀린 것으로 드러난
경우다.** 이유는 `finseg/models.py`·`finseg/rules.py` 안에 적혀 있다.

1. **분류가 열이다** — 돌고래 몸 여섯(등·꼬리·주둥이·머리·몸통·가슴) ·
   아닌 것 셋(새·사람·기타) · 배경. 옛 검출기가 헛보는 것을 `none`(배경)에
   두면 같은 동물·닮은 실루엣을 배경이라 가르친다.
   **`CLASSES` 순서가 곧 YOLO 클래스 번호라, 한 번이라도 내보낸 뒤에는
   `none` 앞에 덧붙이기만 할 것**
2. **`edges` 는 순수 re-ID 축이다** — `neither` → **`tip`(끄트머리)**, 그리고
   **더 이상 크롭을 버리지 않는다.** 끄트머리만 보인다는 건 앞에 온전한
   지느러미가 있다는 뜻이라, 버리면 가장 좋은 표본이 함께 사라졌다
3. **`base_partial` 은 어느 삽입점이 짐작인가다** (참/거짓 아님).
   뒷삽입점이 짐작이면 결각 쪽 끝이 흔들려 re-ID 값이 크게 떨어지지만,
   앞만 짐작이면 여전히 쓸 만하다
4. **`facing` 이 생겼다** — 밑동 두 점 중 어느 쪽이 앞(머리 쪽)인가.
   3번이 뜻을 가지려면 필요하고, re-ID 의 좌현·우현도 같은 비트다.
   **기하 제안이 98%에 붙고 문턱 위에서 149/149 였다** (`baseline.propose_facing`)
5. **`Mask.base_run`** — 밑동 제안을 채운 실행. `run` 은 폴리곤을 만든 엔진이라
   서로 다르다

## 검토 화면 (`review/`)

```
돌고래  1 등  2 꼬리  3 주둥이  4 머리  5 몸통  6 가슴
아님    q 새   w 사람   r 기타          배경  9 아무것도아님
축      e 날 · p 아래경계 · f 앞쪽(순환) · x 교정필요 · 점을 끌면 밑동
이동    [ ] 앞뒤 · Home End 처음/끝 · Enter 저장(되짚기에서는 다음 페이지)
[검토할 것] [검토한 것] [교정 대기]   우클릭 메뉴
```

**키는 분류 순서와 따로 둔다** (`models.CLASS_KEYS`). 순서는 YOLO 클래스 번호라
못 바꾸고, 키는 손에 익는 것이라 사람 쪽 사정으로 정한다.

- **되짚기는 즉시 저장된다.** `검토한 것`·`교정 대기` 에서는 고치면 바로 들어가고
  칸이 초록으로 번쩍인다. 저장 단추가 없다. `검토할 것` 만 묶음 저장이다
- **`e` 는 차림표를 띄운다** — 순환이 아니다 (`devlog/…_003` 3절)
- **원본 사진** `/photo/<box_id>` — 휠 확대 · `Space`+움직임 이동 · `1` 이 상자로
- **윤곽 고치기** `/edit/<box_id>` — **원본 사진이 깔린다** (확대해도 안 뭉개지고
  크롭 밖까지 보인다. 회색 점선이 크롭 경계 — 그 밖은 내보낼 때 잘린다).
  상자로 꼭짓점 골라 함께 옮김 · `Del` 삭제 · **노란 점으로 밑동도 고친다** ·
  `f` 앞쪽 · `Space` 이동 · `Ctrl+Z`. **`Esc` 로는 안 닫힌다**
- 손 안 댄 묶음은 `Enter` 로 저장 안 된다 (`y` 나 저장 단추라야)

## 키포인트 경로 (4단계 — 붙였다)

```bash
python manage.py export_pose --out datasets/pose-v1
python manage.py train --data datasets/pose-v1                    # [GPU] 갈래는 MANIFEST 가 말한다
python manage.py infer_base --weights runs/fin-pose/weights/best.pt
```

`infer_base` 는 `Mask.base_line` 에 넣는다 — **화면 코드를 안 건드려도 제안이
바뀐다.** `Review` 는 손대지 않으므로 이미 검토한 상자는 사람이 찍은 두 점을
그대로 쓴다. 라벨이 늘면 다시 돌리면 된다 (`--redo`).

## 3단계를 한 바퀴 돌렸다 (2026-08-16 밤)

```
export_yolo --group coarse --val-date 2019-06-17   →  크롭 2,315 전부 사용
train (yolo11s-seg run 14 · yolo11m-seg run 17)
infer --compare-only  (run 16 · run 18 — 현재를 안 건드린다)
eval_masks --runs 5 16 18
```

| | 전체 (자기채점 섞임) | **독립 164장** | 학습에 안 쓴 41장 |
|---|---|---|---|
| SAM2.1 (run 5) | 0.978 | 0.770 | **0.719** |
| **YOLO11s (run 16)** | 0.933 | **0.902** | **0.885** |
| YOLO11m (run 18) | 0.928 | 0.887 | 0.858 |

**`s` 가 `m` 보다 낫다.** 파라미터 2.5배·시간 1.8배를 치르고 더 나빠졌다 —
자료가 1,754장이라 용량이 천장이 아니다. `l`·`x` 는 볼 이유가 없다
(`m` 이 이미 11GB 카드의 94%를 쓴다).

### **정답의 92%가 SAM2 자기 출력이다** — 자를 읽을 때 반드시 볼 것

사람이 윗윤곽을 다시 그린 것은 2,042장 중 164장(8%)뿐이고, 나머지는 `통과` 라
`rules.resolve` 가 마스크 폴리곤을 그대로 정답으로 쓴다. **그 엔진에게는 자기
답을 채점하는 셈**이고, 처음 낸 "SAM2 0.914 대 YOLO 0.902" 는 그래서 틀렸다.

`eval_masks` 가 이제 **`독립` 줄을 늘 함께 낸다.** 후보도 정답과 같은 밑동에서
자른다 — 안 자르면 IoU 가 "잘라낸 양" 과 "윤곽 차이" 를 섞어 잰다.

**이것은 SAM2 만의 문제가 아니다.** YOLO 로 갈아 끼우면 다음 정답의 대부분이
YOLO 출력이 된다 — 바퀴마다 되풀이된다.

### 표류는 발산하지 않는다 — 문턱에서 멈춘다

사람이 다시 그린 163장의 넓이 비(사람/모델)가 **중앙값 0.945 · 줄임 113 대
키움 49** 다. SAM2 가 넓게 따는 버릇이 있고 사람이 깎아 왔다는 뜻이며,
**되먹임이 돌고 있다는 증거**다. 다만 0 이 아니라 **사람이 알아보는 문턱**에서
멈추고, 그 문턱을 정하는 것은 **검토하는 배율**이다. 결각은 작은 흠이라 하필
그 문턱 근처에 있으므로, **가끔 편집 창(확대)에서 몇 장 열어 보는 것**이
문턱을 낮추는 유일하게 싼 방법이다 (`devlog/…_005` 4절).

## 두 번째 바퀴 (2026-08-17)

`promote --run 16` 으로 YOLO11s 를 현재로 올렸고 `infer_base --redo` 로 밑동
제안을 다시 채웠다. 현재 마스크는 **YOLO 2,246 + SAM2 69**(YOLO 가 못 낸 것).

검토 화면에 **`엔진 바뀜`** 대기열이 생겼다 — `Review.mask_id` 와 지금 현재
마스크가 다른 것이다. 거기서 `Enter` 는 **"확인하고 다음"** 이고, 안 누른 것은
"새 마스크도 괜찮다" 는 판정이 된다.

**첫 408장에서 실제로 손댄 것이 4%다** — 밑동 다시 찍음 5 · 윗윤곽 새로 그림 7
· `verdict` 바꿈 12. 나머지는 그냥 확인하고 넘겼다.

### 2차를 `fix` 로 재면 안 된다

2차의 `fix`(87%)는 1차(77%)보다 높게 나오는데 **허수다.** 화면이 옛 `verdict` 를
실어 오고 사람이 안 바꾸면 그대로 다시 저장되므로, **2차의 일이 아니라 1차의
값을 되비춘다.** `밑동 그림` 도 같다 — 사람이 찍었던 것을 잃지 않으려고 계속
실어 보내는 것이다(`base_human`, 어제 76건을 잃고 넣은 장치).

**옳은 자는 "이번에 값이 바뀐 상자가 몇 %인가"** 다.

## 다음에 할 것

1. **YOLO 로 갈아 끼운다** — `infer` 를 `--compare-only` **없이** 돌리면 현재가
   바뀐다. 그때 **밑동 제안 533개가 옛 마스크에 붙어 있어 함께 사라지므로**
   `infer_base --redo` 를 이어서 돌려야 한다. `verdict='ok'` 도 그 엔진에 딸린
   판정이라 다시 받아야 한다 (`models.py`)
2. **두 번째 바퀴 검토** — 새 제안으로. `fix` 와 넓이 비가 어떻게 움직이는지가
   그 바퀴의 답이다
3. 자료 쪽 지렛대 — `dolphin` 86 · `nonfin` 177 이 모자라고 배경은 4.6%다
   (권장 10%)
4. **`base_partial=unknown` 60건** — re-ID 용이라 급하지 않다

## 알아 둘 것

- **밑동을 수면선으로 자동 제안하지 말 것.** 되기는 하지만 답이 아니다
  (`devlog/…_001` 3절). 삽입점 자동 탐지 실패 기록(`finseg/baseline.py`)의
  세 번째 항목이다
- **`edges` 로 크롭을 버리던 규칙을 되돌렸다.** 되돌리려거든
  `finseg/rules.py` 의 2026-08-16 항목을 먼저 읽을 것
- **중간값이 판정으로 남는 순환을 만들지 말 것.** 시간으로 묶는 것은 가리는
  것이지 없애는 것이 아니다 (`devlog/…_003` 3절 — 그 틀린 고침을 한 번 했다).
  값이 적고 한두 번에 닿으면 순환이 낫다 (`f`)
- **재현율의 천장이 옛 YOLOv5 다.** 그것이 무너지는 날이 넷 있다 (위)
- **교정을 담은 축은 다시 저장할 때 함께 실어야 한다.** `*_moved`("이번에
  건드렸나")만 보고 판단했다가 **사람이 찍은 밑동 76건을 잃었다** — `*_human`
  ("이미 사람 것인가")이 그래서 있다 (`devlog/…_004` 4절)
- **`itemOf` 와 `sigOf` 는 축을 더할 때 같이 고친다.** 안 그러면 화면은
  바뀌고 저장은 200 을 받는데 값만 안 들어간다
- **2080ti 는 Turing(sm_75) 이라 bf16 이 없다.** AMP 는 fp16
- **`fin.db` 를 두 기계에서 함께 열지 않는다.** 판정 1,855건은 다시 못 만든다
- **`Review` 는 쌓인다, 덮어쓰지 않는다.** 판정을 고칠 때는 새로 얹는다.
  되짚기에서 자기 기준이 언제 움직였는지 재려면 그 자취가 있어야 한다
- **화면이 자기 상태를 말하는지 늘 의심할 것.** 하루에 "값은 맞는데 화면이
  안 말한다" 를 **아홉 번** 만났다 (`devlog/…_002` 1절 · `_003` 끝 · `_004` 5절).
  붉은 줄(`.warn`)이 그 자리다
