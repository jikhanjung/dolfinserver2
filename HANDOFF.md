# Handoff — 현재 진행 상황

**2026-08-16.** 첫 바퀴 검토를 시작해 **506장(21.9%)** 을 봤고, 그러면서
**스키마 두 자리가 자료에 반증되어 뒤집혔다**. 검토 화면도 크게 늘었다.

지나간 것은 `devlog/20260816_001`(첫 바퀴)·`20260816_002`(스키마를 뒤집은 이유),
앞으로 할 일은 `TODOs.md`.

## 지금 있는 것

| | |
|---|---|
| 표본 | 관찰일 20일 (2016-03-15 ~ 2019-11-05) · 상자 2,315 · 사진 1,909 |
| 크롭 | 640×640 · 154MB · `crops/` |
| 마스크 | 2,315개 — 상자마다 하나씩. SAM2.1 · 2080ti · run 5 |
| **검토** | **506 / 2,315 = 21.9%** — 관찰일 4일치 |
| **밑동 라벨** | **367** (키포인트 문턱 300 넘음) |
| **사진 원본** | **붙었다** — `/mnt/p/JikhanJung/dolfinimage`, 1,909장 전부 열림 |

`fin.db` 백업: `../fin.db.20260816-{0140,0940,1010}.bak` (저장소 밖. `.gitignore`
의 `*.db` 는 `.bak` 을 안 잡는다 — 안에 두면 `git add -A` 에 딸려 들어간다)

## 서버는 사진 경로를 물려서 띄운다

```bash
FIN_PHOTOS=/home/jikhanjung/dolfin-photos python manage.py runserver 0.0.0.0:8900
```

`~/dolfin-photos/nas` 가 `/mnt/p/JikhanJung/dolfinimage` 로 가는 심볼릭 링크다.
DB 가 `nas/2016/…` 로 들고 있어서 그렇다 — **코드에서 접두어를 벗기지 않은
이유**는 `devlog/…_002` 6절에 있다.

## 506장이 말한 것

| | 값 | 뜻 |
|---|---|---|
| `fix` (지느러미만) | **365/424 = 86%** | 밑동 제안이 쓸모없다 (아침 87%에서 안 움직임) |
| 윗윤곽 교정 | **2** | 편집기가 실제로 통과했다. 여전히 드물다 |
| `cls != fin` | 91/506 = 18% | **2016-07-20 하루에 몰려 있다** (아래) |
| `edges != both` | **0/424** | **아직 아무 판단도 안 들어갔다** |
| `base_partial` | 18 | |

내보내기는 **처음으로 진짜 판정으로 돌았다** — 크롭 503 · 라벨 506 · 배경 77.
뺀 1,812 개가 **전부 "아직 안 봄"** 이고, 판정이 든 것 중 버려지는 것은 없다.

### 2016-07-20 은 하루가 통째로 이상하다

| 관찰일 | 검토 | `none` | |
|---|---|---|---|
| 2016-03-15 | 109 | 3 | 3% |
| 2016-05-07 | 116 | 3 | 3% |
| **2016-07-20** | 138 | **70** | **51%** |
| 2016-09-05 | 93+ | 1 | 1% |

옛 YOLOv5 가 그날 절반을 헛봤다. **남은 16일에 같은 날이 더 있으면 검토량
추정이 달라진다.**

## 스키마가 두 곳 바뀌었다 (오늘)

**둘 다 자료를 보기 전에 정한 규칙이 자료를 보고 나서 틀린 것으로 드러난
경우다.** 이유는 `finseg/models.py`·`finseg/rules.py` 안에 적혀 있다.

1. **분류가 여덟이다** — 등지느러미·꼬리·주둥이·**머리·몸통·새**·기타·아님.
   옛 검출기가 헛보는 것이 새 날개와 돌고래 몸통이었고, 그것을 `none`(배경)에
   두면 같은 동물을 배경이라 가르친다
2. **`edges` 는 순수 re-ID 축이다** — `neither` → **`tip`(끄트머리)**, 그리고
   **더 이상 크롭을 버리지 않는다.** 끄트머리만 보인다는 건 앞에 온전한
   지느러미가 있다는 뜻이라, 버리면 가장 좋은 표본이 함께 사라졌다

## 검토 화면 (`review/`)

```
1~8 분류 · e 날 · x 교정필요 · p 아래경계불완전 · 점을 끌면 밑동
우클릭 메뉴 · [ ] 페이지 · Enter 저장
[검토할 것] [검토한 것] [교정 대기]
```

- **원본 사진** `/photo/<box_id>` — 이 상자는 노랑, 같은 사진의 다른 상자는 흐리게
- **윤곽 고치기** `/edit/<box_id>` — 확대·이동, 꼭짓점 끌기/추가/삭제, `Ctrl+Z`
- **되짚기** — `검토한 것` 은 **본 순서대로**. 손댄 칸만 저장한다
- **교정 대기** — `fix` 인데 안 고친 것. 여기 말고는 다시 만날 길이 없다
- 손 안 댄 묶음은 `Enter` 로 저장 안 된다 (`y` 나 저장 단추라야)

## 다음에 할 것

1. **검토를 이어 간다.** 남은 1,809장. 화면에서 **`e`(날) 를 실제로 쓸 것**
2. **키포인트 모델** — 라벨 367장으로 YOLO11-pose. `fix` 86%를 끌어내리는
   유일한 한 수다 (4단계)
3. `export_yolo` → `train` → `eval_masks`. **성적은 `val_date` 로 읽는다**

## 알아 둘 것

- **밑동을 수면선으로 자동 제안하지 말 것.** 되기는 하지만 답이 아니다
  (`devlog/…_001` 3절). 삽입점 자동 탐지 실패 기록(`finseg/baseline.py`)의
  세 번째 항목이다
- **`edges` 로 크롭을 버리던 규칙을 되돌렸다.** 되돌리려거든
  `finseg/rules.py` 의 2026-08-16 항목을 먼저 읽을 것
- **재현율의 천장이 옛 YOLOv5 다.** 2016-07-20 이 그 천장을 처음 보여 준 날이다
- **2080ti 는 Turing(sm_75) 이라 bf16 이 없다.** AMP 는 fp16
- **`fin.db` 를 두 기계에서 함께 열지 않는다.** 판정 506건은 다시 못 만든다
- **`Review` 는 쌓인다, 덮어쓰지 않는다.** 판정을 고칠 때는 새로 얹는다.
  오늘 24행을 지운 것은 판정이 아니라 입력 사고였기 때문이고, 그때도 백업이
  먼저였다
- **화면이 자기 상태를 말하는지 늘 의심할 것.** 하루에 "값은 맞는데 화면이
  안 말한다" 를 다섯 번 만났다 (`devlog/…_002` 1절). 붉은 줄(`.warn`)이 그
  자리다
