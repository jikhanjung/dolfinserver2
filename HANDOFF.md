# Handoff — 현재 진행 상황

**2026-08-15.** 저장소를 열고 자료 경로를 끝까지 놓았다. 아직 사람이 검토한
마스크가 **하나도 없다** — 다음 손이 할 일이 그것이다.

## 지금 있는 것

| | |
|---|---|
| 표본 | 관찰일 20일 (2016-03-15 ~ 2019-11-05) · 상자 2,315 · 사진 1,909 |
| 크롭 | 640×640 · 154MB · `crops/` |
| 마스크 | **없음** (SAM2.1 을 아직 GPU 에서 안 돌렸다) |
| 검토 | **없음** |

표본은 씨앗 `20260815` 로 다시 뽑을 수 있다. `run` 표에 그때 쓴 인자가 남아 있다.

## 다음에 할 일

1. **저장소와 `fin.db`·`crops/` 를 2080ti 로 옮긴다.** 거기가 운영 자리다 —
   NAS 가 마운트돼 있어 사진을 직접 읽고, GPU 가 있다. 이 기계에는 GPU 가 없다
2. `python -m finseg.segment` — 2,315 개, 몇 분
3. **아래 "지느러미의 아래 경계" 를 먼저 정한다.** 정하지 않고 검토하면 스스로와
   어긋난다
4. `uvicorn finseg.review.app:app --host 0.0.0.0 --port 8900` 로 검토
5. `export_yolo` → `train` → `eval`. **성적은 `val` 이 아니라 `val_date` 로 읽는다**

## 정해야 하는 것 — 지느러미의 아래 경계

CPU 로 12장을 돌려 본 결과, **위쪽 윤곽은 아주 깨끗하다.** 앞·뒷날의 굴곡과
흠집까지 딴다 — 나중에 개체 식별로 갈 때 필요한 바로 그 부분이다.

**흔들리는 것은 아래쪽뿐이다.** 지느러미가 등과 만나는 자리와 수면이 겹치는
언저리에서 SAM2 의 경계가 사진마다 다르게 잡힌다. 어떤 것은 등을 조금 물고,
어떤 것은 수면 위 물보라까지 문다.

이것은 모델의 잘못이 아니라 **정의가 없어서 생기는 흔들림**이다. 사람이 먼저
정해야 한다. 고를 수 있는 것은 대략 셋이다.

1. **수면선에서 자른다** — 물 위에 나온 부분만. 재기 쉽고 일관되지만 같은
   개체라도 자세에 따라 아래 끝이 달라진다
2. **지느러미와 등의 해부학적 경계에서 자른다** — 물에 잠긴 부분은 추정해서
   긋는다. 개체 식별에는 이쪽이 맞지만 사람이 긋기 어렵고 느리다
3. **위쪽 윤곽만 쓴다** — 아래는 아예 열어 둔다. 분할 문제로는 어색하지만,
   **정말 필요한 것이 앞·뒷날이라면** 가장 정직하다

**셋 중 무엇이든 정하고 나면 `finseg/rules.py` 옆에 적고 검토 화면에 띄운다.**
지금은 정해지지 않았다.

## 알아 둘 것

- **재현율의 천장이 옛 YOLOv5 다.** 그 검출기가 못 본 지느러미는 이 루프 안에서
  표시할 방법이 없다. `eval.py` 가 내는 숫자에 늘 이 한정이 붙는다.
  자유 그리기가 필요해지는 시점이 오면 그때 만든다
- **2080ti 는 Turing(sm_75) 이라 bf16 이 없다.** AMP 는 fp16 이어야 하고,
  그 판단은 `segment.py:autocast_dtype()` 에 있다
- **DB 를 두 기계에서 함께 열지 않는다.** SQLite 는 쓰기가 하나다.
  형제 프로젝트가 그것으로 프레임 229장을 잃었다
- 옛 DB(`../dolfinserver/db.sqlite3`)는 **읽기 전용으로만** 연다. 운영 웹서버가
  같은 파일을 쓰고 있다

## 아직 없는 것

- **다시그리기 화면.** `fix` 로 표시한 것이 `/api/fixqueue` 에 쌓이기만 한다.
  격자 검토를 한 바퀴 돌려 `fix` 가 얼마나 나오는지 보고 만드는 편이 낫다 —
  적으면 손으로 지워도 되고, 많으면 그것이 경계 정의가 잘못됐다는 신호다
- **자동 시험이 없다.** `rules.label_of` 와 크롭 좌표 사상은 표로 시험할 값어치가
  있다 (틀리면 마스크가 조금 밀린 모습으로만 나타나 눈에 안 띈다)
- `segment.py`·`infer.py`·`train.py`·`eval.py` 는 **GPU 에서 아직 안 돌려 봤다.**
  `segment.py` 만 CPU 로 12장 확인했다
