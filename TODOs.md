# TODOs

앞으로 할 일. 끝난 것은 며칠만 두고 지운다 — 기록은 `devlog/` 에 남는다.

## 다음 (2080ti)

- [ ] **검토를 이어 간다.** 456/2,315 (19.7%). 남은 1,859장.
      세 숫자는 이미 나왔다 (`devlog/20260816_001` 1절) — `fix` 87% ·
      `cls != fin` 17% · 윗윤곽 교정 0건. 이어 갈 때 볼 것은 하나다:
  - **`e`(날) 를 실제로 쓸 것.** 지느러미 379장이 전부 `both` 인데 이것은
    판단이 아니라 기본값이다. `models.py` 가 경고한 축이 이것 — **지금 안
    적으면 re-ID 갈 때 2,315장을 다시 연다**
- [ ] **키포인트 모델 — 이제 할 수 있다.** 밑동 라벨 333장(문턱 300). 640 크롭에
      점 두 개를 얹는 회귀이고 ultralytics 가 그대로 지원한다. **`fix` 87% 를
      끌어내리는 유일한 한 수**라 검토 속도를 가장 크게 바꾼다 (4단계)
- [ ] `export_yolo` → `train` → `eval_masks`. **성적은 `val_date` 로 읽는다**
- [ ] **`m710q` 를 정리한다.** 자료가 2080ti 로 왔고 추적도 뺐다(`5d63a5c`).
      거기서 `git pull` 하면 그쪽 `fin.db`·`crops/` 가 워킹트리에서 지워진다 —
      **`fin.db` 에 이제 사람의 판정 456건이 있다.** 운영 자리는 한 곳이다
- [ ] **크롭을 git 히스토리에서 뺄지 정한다.** `fin.db` 와 달리 **되돌리기가 한
      방향이다** — JPEG 은 델타 압축이 안 돼 팩이 그대로 154MB 이고, `git rm
      --cached` 를 해도 히스토리에 남아 이후 모든 클론이 그 값을 치른다.
      `git filter-repo` + force-push 라야 하고 그때는 다른 클론을 전부 다시 받는다

## 곧

- [ ] **2016-07-20 이 왜 그런지 본다.** 그날 하나만 `none` 이 51% 다 (딴 날은
      1~3%). 파고·역광·물보라 중 무엇인지 보면 **5단계 검출기 교체의 첫 근거**가
      되고, 남은 16일에 같은 날이 더 있는지로 검토량 추정이 달라진다
- [ ] **시험을 자료 경로까지 넓힌다.** 판정 규칙·좌표 사상·규칙 표시는 덮었다
      (`manage.py test`, 26개). 남은 것은 `export_yolo` 의 크롭 클리핑과 제외
      규칙, `import_boxes` 의 이웃 폐포·중복 거르기다 — 둘 다 틀려도 자료가
      조용히 조금 어긋날 뿐이라 눈에 안 띈다. 검토 화면도 시험 밖이다
- [ ] `--pad` 를 바꾸면 이웃 목록이 어긋날 수 있다. 내보낼 때 한 번 더 막지만
      경고를 띄우는 편이 낫다

## 멀티유저로 갈 때

- [ ] `rules.effective_review()` 를 합의 규칙으로 바꾼다 (사람마다 최신 → 다수결
      또는 신뢰가중). **부르는 쪽은 안 바뀐다** — 그러라고 함수로 뽑아 두었다
- [ ] 같은 상자를 여러 사람이 보게 배정하고 **일치도를 잰다**. 그것이 곧 판정
      규칙이 명확한지에 대한 답이다
- [ ] 로그인·계정. `auth` 는 이미 켜 두었다
- [ ] `.guides/web/` 의 배포 다섯 동사(preflight·deploy·seed·smoke·rollback)와
      백업 레인 채택. **`fin.db` 는 사람의 판정이라 다시 만들 수 없다**

## 나중

- [ ] **검출기 교체.** 지금은 옛 YOLOv5 상자에 의존한다 — 그것이 못 본 지느러미는
      이 루프 안에서 표시할 방법이 없고, 재현율의 천장이 거기 걸린다.
      자유 그리기가 필요해지는 시점이 오면 그때 만든다
- [ ] **re-ID.** 뒷날 결각으로 개체 매칭. 밑동 현이 정규화(회전·크기)의 기준이
      된다. 옛 DB 의 JTA### 189건이 씨앗이고, 좌현·우현을 갈라야 한다
      (**임베딩 학습에 `fliplr` 을 쓰면 안 된다**)
- [ ] dockerize

## 안 하기로 한 것

- **다시그리기 화면.** 456장에 **윗윤곽 교정이 0건**이다. "얼마나 되는지 보고
  만든다" 고 미뤄 뒀던 것인데 답이 나왔다 — SAM2 마스크 자체는 안 틀린다.
  `verdict=fix` 는 전부 밑동 문제였다. 나중에 0 이 아니게 되면 그때 다시 본다
- **밑동을 수면선으로 자동 제안하기.** 되기는 하지만 **답이 아니다** — 수면선은
  자세에 따라 움직여서 re-ID 정규화 기준이 개체가 아니라 그날 물결을 재게 되고,
  하필 SAM2 가 가장 흔들리는 자리이며, 무엇보다 **그럴듯한 오답이라 사람의 눈을
  끌고 간다**. 이유 셋은 `devlog/20260816_001` 3절, 앞선 두 번의 자동 탐지 실패는
  `finseg/baseline.py`
