# TODOs

앞으로 할 일. 끝난 것은 며칠만 두고 지운다 — 기록은 `devlog/` 에 남는다.

## 다음

- [ ] **여럿에게 열려면 셋을 먼저 막는다** (2026-08-27 · `HANDOFF` 의
      `## 아직 끊긴 데` 첫 절). 지금 안전하게 열 수 있는 것은 **보는 것까지**다.
      ① **사람마다의 인증** — 문이 코드 하나이고 모두가 나눠 쓴다.
      `reviewer` 가 늘 비는 이유이고, 오늘 붙인 `ip` 는 절반만 메운다.
      ② **둘이 서로를 못 본다** — `/reid` 는 뜰 때 한 번 읽고 그 뒤로는
      `BroadcastChannel`(같은 브라우저 안에서만) 뿐이라, 같은 조각을 각자
      끌면 **나중 것이 조용히 이긴다**. 되읽기나 짧은 폴링이 필요하다.
      ~~③ **`새 상자` 가 부딪힌다**~~ — **막았다** (2026-08-27, `0.17.0`).
      `name` 이 UNIQUE 이므로 누가 이겼는지를 DB 가 정하게 두고, 부딪히면
      다음 번호로 다시 든다.
      **여럿이 쓰게 되면 `.guides/web/` 의 배포 다섯 동사도 그때 채택한다**
      (아래 `## 나중에` 에 적어 둔 그 항목이다)
- [ ] **1차 목표를 채운다** (`/dataset` 이 진척과 다음에 할 일을 낸다).
      개체 42 → **60** · 조각 563 → **1,200** · 모든 개체를 **10장 이상 ·
      2일 이상**으로. 근거는 `reid.GOAL` 과 `devlog/20260827_002`.
      **지금 42마리를 전부 10장으로 올리는 데 조각 109장이면 된다** —
      몇 마리를 40장으로 만드는 것보다 훨씬 세다(4~9장 11.1% → 10~19장 30.6%,
      그 뒤 20장 이상은 33.8%로 평평하다).
      **성적으로 검수하지 말 것** — 개체가 늘면 문제가 어려워져 top-1 이
      안 오르거나 내릴 수 있다. 검수는 `reid_eval --as-of` 다
- [ ] **`/reid` 를 "찍힌 차례로 빨리 넣는" 화면으로 만든다 — 지금 가장 값이
      큰 일.** 길을 그렇게 정했다(`devlog/20260827_001` 25절). 남은 둘 —
      ~~① 잇달음이 어디서 끊기는지 안 보인다~~ — **절반 했다** (`0.21.0`).
      **날이 바뀌는 자리**에 금을 긋고 격자 위에 날짜띠를 띄운다. 남은 것은
      **같은 날 안에서 시간 간격이 벌어지는 자리**다 — 하루가 수백 장이라
      날만으로는 잇달음이 안 갈린다. **며칠 뒤 지울 것**
      ② **넣는 길이 끌기뿐이다** — 스무 장을 화면 건너로 끄는 것이 느리다.
      고른 뒤 키 하나로 **`보기` 로 편 상자에 넣기**가 있으면 판단 한 번이
      바로 저장이 된다.
      ~~③ 정렬이 안 기억된다~~ — **고쳤다** (`0.20.0`). 기본이 `날짜·시간순`
      이고 `정렬`·`좌우`·`넣은 것 숨기기` 를 기억한다
- [x] ~~**`rough` 를 스냅한 윤곽에서 다시 잰다**~~ — **했다** (`0.20.1` 전,
      두 자리 격자 모두). `reid.snap_to_edge` + `reid_chips --rough-only`.
      중앙값 0.0440 → 0.0468 · 등수 상관 0.791 · 위 25% 가 3분의 1 갈렸다.
      **남은 절반은 "사람이 알아보는 것과 맞나" 를 다시 재는 일**이다 —
      사람이 윤곽을 그린 것과 개체가 붙은 것이 겹치는 게 21조각뿐이라 못 쟀다.
      **며칠 뒤 지울 것**
- [x] ~~**`PCA 256 + L1 1e-5` 로 갈아 끼운다**~~ — **접었다** (2026-08-27).
      +2.5%p 가 좋게 하는 것이 `묶음 제안` 의 top-5 인데 **그 길을 안 쓴다.**
      갈아 끼우면 `LINK_DIST` 를 다시 재야 하고 격자 순서가 흔들린다 —
      **안 쓰는 것을 좋게 하려고 쓰는 것을 흔드는 셈.** 개체 수가 늘거나
      notch 축이 서면 그때 함께 올린다. **며칠 뒤 지울 것**
- [ ] **확신을 온도로 고친다 — 오늘 잰 것 중 화면에 바로 이득인 것**
      (2026-08-29, `reid_cls --calib`). 화면이 말하는 퍼센트가 실제와 **21%p**
      벌어져 있다 (평균 확신 70.4% 대 실제 49.5% · ECE 0.210).
      `--arcface --arc-m 0 --arc-s 64` 로 바꾸면 ECE 가 **0.145** 가 되는데,
      **`--arc-s` 는 사실상 온도라** 손실을 안 바꾸고 지금 로짓을 나누기만 해도
      같은 것을 얻을 듯하다 (`m 0` 과 `m 0.05` 의 ECE 가 0.145 대 0.150 으로
      같다 — 여백은 확신에도 기여를 안 한다).
      **온도를 재는 날에서 고르면 샌다** — 배우는 날 안에서 가를 자리가 먼저다.
      그리고 지금 잰 확신은 낱장 기준인데 **화면은 묶음 로짓을 평균한다**
      (`--group`) — 화면 조건에서 다시 잴 것
- [ ] **화면의 확신 정렬을 다시 볼 것** (2026-08-29). `1등 확률` 이
      `1·2등 차이` 만큼, 오히려 조금 더 잘 듣는다 (상위 10% 에서 95.8% 대
      95.0%). `HANDOFF` 의 `1등 점수는 쓸모없다(38%)` 는 **kNN 코사인**에 대한
      말이라 이것을 뒤집는 건 아니지만, **분류기 확률로는 재 본 적이 없었다**
- [ ] **`items.json` 의 좌표 자릿수를 줄인다** (2026-08-29). `/reid` 첫 그림에
      남은 유일한 덩어리다 — 실려 오는 4.38MB 중 **3.26MB(75%)가 윤곽
      폴리곤**이고, 점이 중앙값 20개인데 좌표가 소수 넷째 자리다. 셋으로
      줄이거나 점을 솎으면 절반 아래로 떨어진다.
      **격자를 다시 만드는 김에 함께 할 일이지 따로 할 일이 아니다** —
      조각·임베딩·분류기가 `items.json` 의 **차례**로 묶여 있다
- [ ] **화면 로직을 잴 자를 세울지 정한다** (2026-08-29). `0.21.0` 을 낼 때
      `jsdom` 으로 43가지를 짚어 놓고 **저장소에는 안 넣었다** — node 와
      jsdom 이 있어야 도는데 그것은 이 저장소의 의존이 아니다.
      **버그가 화면에서 나온 것이 처음이 아니다**(두 창 선택 사고 ·
      여러 줄 `{# #}`). 정할 것은 시험 하나가 아니라 **자를 세울지**다
- [ ] **후보를 다섯에서 열로 늘릴지** — top-5 77.5% → top-10 84.5% 로 7%p 를
      더 건지지만, **제안 자체가 지금 쓸모가 없어서** 이것도 함께 미룬다
- [x] ~~**백본을 얼린 채 키워 본다**~~ — **했고, 졌다** (2026-08-27).
      ViT-B/14(768)가 어느 차원에서도 ViT-S 를 못 이긴다 — top-5 는 3%p 넘게
      진다. **ViT-L(90분)은 안 뽑았다**: B 에서 방향이 났고 L 이 그것을 뒤집을
      근거가 없다. 대신 **통제줄로 넣은 PCA 가 원본을 이겼다** — 답은 키우는
      쪽이 아니라 줄이는 쪽에 있었다. **며칠 뒤 지울 것**
- [ ] **뒷날 띠 위에서 다시 분할한다 — 재 보니 여기가 유일하게 남은 길이다**
      (`devlog/20260827_003`). 뒷날을 따라 **원본 해상도로 512×128 띠**를 떠서
      거기서 다시 분할하면 notch 가 30px 넘게 된다(조각 128px 에서는 2~5px).
      **정답은 이미 있다** — 사람이 그린 윤곽 272개를 띠 좌표로 옮기면 된다.
      **하한도 안다**: 학습 없는 밝기 스냅 한 번이 AUC +0.098 이었다.
      먼저 확인할 것 — 그 272개가 **70점짜리 클릭**이라 notch 를 담고 있는지
      몇 장 확대해 원본에 겹쳐 볼 것. 안 담고 있으면 천장이 거기다
- [ ] **`seg-v4` 에 경계를 재는 자를 따로 둔다.** `IoU 0.841` 은 넓이 겹침이라
      **경계가 notch 크기(현의 2~5%)에서 어긋나도 거의 안 변한다** — "마스크가
      웬만큼 맞는다" 와 "뒷날이 안 되풀이된다" 가 둘 다 참인 이유다.
      사람 윤곽에 대한 뒷날 거칢 비(지금 **157%**)가 그 자의 후보다
- [ ] ~~**조각 해상도를 올려 본다 — 이제 여기가 가장 유망하다**~~ —
      **계산해 보니 그것만으로는 안 풀린다** (2026-08-27). 224 로 키워도
      notch 는 5px 남짓인데 ViT-S/**14** 의 패치는 14px 다 — **notch 가 패치
      하나보다 작다.** 패치 둘을 채우려면 입력이 1,200px 쯤이라 말이 안 된다.
      남는 길은 셋이고 결이 다르다: **notch 를 직접 재는 축**(파인 곳의
      깊이·너비·개수) · **뒷날만 크게 잘라 따로 보기**(finFindR 이 그렇게 한다) ·
      **패치가 없는 백본**(`resnet18` 경로로 싸게 물어볼 수 있다).
      아래 것은 그대로 남는다 —
      `reid.CHIP` 이 **128px** 인데 정답 상자의 한 변(√면적) 중앙값이 **159px**,
      75분위 204 · 95분위 276 이다. **위쪽 절반은 있는 화소를 버리고 있다.**
      진짜 notch 는 현의 2~5%라 128px 조각에서 **2.5~6px** — 그 근처에서
      해상도가 곧 천장이다. 192~224 로 만들어 같은 자로 잴 것.
      덤으로 `chip(color=True, cut=True)`(배경만 지우고 색은 남긴 조각)도
      아직 안 재 봤다 — 논문이 pigmentation 을 판별 단서로 꼽는다
- [x] ~~**`reid_cls --folds` — 자를 먼저 조인다**~~ — **붙였다** (2026-08-27).
      질의 137 → **493**, 눈금 0.73%p → **0.20%p**. `--folds 5 --seeds 3` 에
      45.0 / 75.1 (44.9 ± 0.1). **폴드끼리 13.4%p 흔들리는데 씨앗끼리는 ±0.1**
      이라, 잡음이 초기값이 아니라 **어느 날을 재느냐**에서 온다는 것이
      드러났다. 폴드 나누는 법을 고르게 바꾸니 합계가 42.7 → 45.0 으로
      움직였다 — **절대값은 여전히 갈래를 탄다.** 사는 것은 참값이 아니라
      같은 폴드에 두 자를 세울 수 있게 되는 것이다 (`devlog/20260827_001` 21절).
      **며칠 뒤 지울 것**
- [ ] **`reid_eval` 에 top-10 을 낸다.** finFindR 82% · CurvRank 83% ·
      Kim et al. 84.8% 가 전부 top-10 인데 우리는 top-1/top-5 만 쫓는다.
      **같은 줄에 설 수 있는 자를 갖는 것 자체가 가치가 있다** — 다만
      **개체 수를 반드시 함께 적는다**(우리 42 대 저쪽 79)
- [ ] **`/dataset` 에 열을 더한다** (표의 열이지 테이블의 필드가 아니다).
      마지막 판정 날짜(`Identification.at` 의 최댓값) · 그 개체가 격자에 남긴
      후보 수 · **번호가 붙은 개체 n/42**(연구원 작업의 진척이 그것인데 지금
      목표 넷은 조각을 모으는 쪽만 본다). 셋 다 있는 자료에서 계산만 하면 된다
- [ ] **`/reid` 상자 목록에 찾기 칸.** 지금 42개면 없어도 되는데 번호가 붙기
      시작하면 `JTA037` 을 스크롤로 찾게 된다. 100개 넘어가면 없으면 못 쓴다
- [ ] **옛 상자를 더 길어 온다 — 지금 가장 값이 큰 일.** 쉬운 것은 대충 분류가
      끝났다는 것이 사람의 판단이다. 남은 격자를 더 훑는 것보다 **후보 자체를
      늘리는 편**이 낫다. 비효율적인 것은 알고 하는 것이다.
      옛 DB 에 15,000px² 이상 상자가 **400,309개 · 245일** 있고 쓴 것은 20일뿐.
      **날을 넓히되 날마다 얕게** — 하루 만 장짜리 날을 통째로 넣으면 그날
      바다를 외운다.
      ```
      import_boxes --src <옛 DB> --only-dates <안 쓴 날> --boxes N
      crops → infer(seg-v3-s) → infer_base(pose-v1)
      reid_chips --out reid/v3 --auto-facing --auto-cls --min-area 15000
      reid_chips --out reid/v3 --emb-only --backbone dinov2 --emb-name emb-dinov2.npz
      fin_filter --score reid/v3 --write        # 몸통·바위를 짚어 준다
      reid_cls --fit-all reid/v3/cls-dinov2.npz # 화면이 쓸 분류기
      ```
      **m710q CPU 로도 된다** — 2,290상자에 크롭 15분 · 마스크 40분 · 밑동
      20분 · 조각/임베딩 15분. GPU 면 훨씬 빠르다.
      이때 어제 거른 19장도 함께 빠진다(위의 미룬 항목)
- [ ] **분류기 머리를 키운다.** 지금은 **384차원 → 42클래스 선형 한 층**이다.
      **자는 이제 섰다**(`--folds`). 남은 순서는 얼린 백본 키우기가 먼저다 —
      그것이 "천장이 특징이냐 라벨이냐" 를 갈라 주고, 머리를 키우는 값이
      거기서 정해진다.
      ~~가중치 감쇠가 안 움직였으니 자료를 다 못 담고 있다~~ — **틀렸다**
      (2026-08-27). 용량을 줄이니 올랐다(PCA 384 → 256 에서 +2.4%p).
      L2 는 `wd 0` 과 `wd 1e-1` 이 같아 **이 자료의 지렛대가 아니다.**
      그리고 **백본을 키우는 쪽은 이미 졌다** — ViT-B 가 ViT-S 를 어느
      차원에서도 못 이긴다. 그러니 아래는 **자료가 는 뒤**다 —
      ~~2층 MLP(384→256→42)~~ — **재 봤고 졌다** (2026-08-29, `--hidden`).
      조각이 563 → 1,221 이 되어 조건이 걸린 날 바로 쟀다. 같은 5000 에폭에서
      선형 47.2 / 73.4 대 MLP256 **46.2 / 71.6** 이고 1200 에폭에서도 진다.
      씨앗 폭도 선형 ±0.0 대 MLP ±0.2~0.5 — 손잡이가 5배인데 답이 더 흔들린다.
      **미룬 근거였던 두 실측(ViT-B 가 짐 · PCA 로 줄이니 오름)이 그대로 유효하다.**
      연장(`--hidden`)은 남긴다 — 자료가 또 배로 늘면 다시 물을 값이 있다.
      **며칠 뒤 지울 것**
      ~~ArcFace 같은 여백 손실~~ — **재 봤고 여백은 졌다** (2026-08-29,
      `--arcface`). 어느 스케일에서도 `m` 을 키우면 단조롭게 내린다
      (m 0.1 −2.5%p · 얼굴 관례 0.5 는 −16%p). 얼굴은 클래스당 사진이 수백
      장인데 우리는 개체당 19장이다. **정규화만 남기면(m 0 · s 64) 성적은
      비긴다** — 다만 `s=30` 으로 시작했으면 "−5%p 로 진다" 라고 적었을 것이다.
      **며칠 뒤 지울 것**
      남은 것: **백본 마지막 블록 녹이기**(이것만 GPU 몫).
      자는 `reid_cls` 그대로 쓴다 (kNN 26.7% · 지금 49.5%)
- [x] ~~**닫힌 집합 분류기를 붙인다**~~ — 붙였고 **기준선의 두 배**다
      (kNN 25.5% → 44.5% · 묶어서 47.5% / top-5 75%). 화면도 그것으로 후보를
      내고 점수가 **확률**이 됐다 (`devlog/20260825_001`)
- [ ] **`rough`(notch)를 다시 정한다.** 사람이 알아보는 것과 안 맞는다 —
      상자에 넣은 조각과 안 만진 조각의 notch 분포가 사실상 같았고(0.0450 대
      0.0443), notch 사분위별 top-1 차이도 8pp뿐이다(24.2% → 32.5%).
      **매끈한 지느러미가 실제 벽인데** 그것을 가르는 축이 지금 없다.
      `묶음 제안` 이 잘 안 되는 것도 여기서 온다
- [ ] **격자를 다시 만들 때 오늘 거른 19장이 빠진다** (4,059 → 4,040).
      `아무것도아님` 10 · `몸통` 7 · `꼬리` 1 · `머리` 1 을 `/reid` 에서 손으로
      걸렀고 `Review` 에 들어가 있다. **지금 안 만든다** — 격자가 0.5% 줄자고
      15분(조각·DINOv2 임베딩·`pfin`)을 쓸 값이 없다.
      `seg-v4` 가 나오거나 옛 상자를 더 길어 올 때 **함께** 만든다.
      그때 `reid_chips --out … --min-area 15000 --auto-facing --auto-cls`
      → `fin_filter --score … --write` 순서다.
      남은 것 중 `p(fin)<0.3` 이 28장 더 있는데, 그중에는 진짜 지느러미인데
      필터가 낮게 준 것도 섞여 있다 (재는 날에서 지느러미의 11%를 낮게 줬다)
- [ ] **묶음 문턱을 실측으로 조인다.** 지금 `LINK_DIST 0.06` · `LINK_GAP 2`
      (같은 날·같은 쪽에서 97% 정밀도, 잡히는 비율 5%). 써 보니 묶음이 그리
      좋지 않았다 — 문턱을 좁히거나(0.04면 96%·7%) 매끈한 것은 아예 안 묶는
      쪽을 볼 것

- [x] ~~**상위 3 후보 70장을 본다**~~ — **봤고, 값을 했다** (2026-08-24).
      개체 42마리 중 **23마리가 옛 격자 + 새 조각이 섞인 것**이고 날을 건너뛴
      짝이 1,289 → **5,379** 가 됐다. 시험 사본을 **승격**해 지금 그것이
      `fin.db` 다 (상자 5,882 · 격자 `reid/v2` 4,059).
      **화면은 `FIN_REID=reid/v2` 로 띄운다** — 안 그러면 옛 격자를 보게 되고
      개체 판정이 가리키는 조각이 거기 없어 조용히 빈다
- [ ] **표본을 더 넓힌다 — 이제 길이 뚫렸다.** 옛 DB 에 15,000px² 이상 상자가
      **400,309개 · 245일** 있는데 쓴 것은 20일이다. 2080ti 에서 `import_boxes
      --only-dates` → `crops` → `infer` → `infer_base` → `reid_chips` 를 돌리면
      하루에 수천 장이다. **날을 넓히되 날마다 얕게**
- [x] ~~**같은 날 백업이 서로 덮어쓴다**~~ — 두 갈래로 갈라 막았다 (2026-08-25).
      **한 기계 안**: `.part` 로 뜨고 읽어 본 뒤 `os.replace` 로 갈아 끼운다.
      전에는 본 자리에 바로 떠서 `integrity_check` 가 **덮어쓴 다음에** 깨진
      것을 잡았다 — 그때는 새것도 못 쓰고 옛것도 없었다.
      **기계 사이**: 이름이 날짜뿐이라 나중에 뜬 기계가 먼저 것을 갈아 치웠다
      (2026-08-24 에 m710q 가 2080ti 것을 덮었다. 상위집합이라 잃은 게 없었을
      뿐 **운이었다**). 이제 `fin.db.<날짜>.<기계>.bak` 이고 `MANIFEST` 도
      같이 갈랐다. `--keep` 은 제 갈래만 지운다.
      남아 있던 `fin.db.2026-08-24.bak.part` 는 **최종 백업의 완전한
      부분집합**임을 확인했고(개체 판정 447건, 밖에 있는 것 0) 다음 `backup`
      이 쓸어 갔다.
      **실전에서 한 번 겪었다** — 2026-08-25 07:54 백업 도중에 기계가 재부팅
      됐는데, `.part` 만 남고 `fin.db.2026-08-24.bak` 은 **그대로였다**.
      고치기 전이었으면 그 자리가 반쯤 쓰인 채 남고 8월 24일 것이 사라졌다
- [ ] **승격 같은 사건은 제 나름의 복원점을 남겨야 한다** (`--label` 쯤).
      지금 NAS 의 `fin-test.*`·`fin.before-promote.*` 는 손으로 만든 것이다.
      날짜·기계는 이제 이름에 있으니 **남은 축은 "무슨 일이 있었나"** 다
- [ ] **기계 이름 없이 뜬 옛 백업을 손으로 치운다.** `fin.db.2026-08-24.bak`
      — 어느 기계 것인지 알 수 없어 `--keep` 이 일부러 안 건드린다.
      그렇다고 말은 해 주지만, 두면 30일 치인 줄 알고 지나간다
- [ ] **넓이 문턱을 정한다 — 15,000 이냐 30,000 이냐.** `reid/v1` 은 15,000 으로
      만들어졌는데 `reid.MIN_AREA` 는 30,000 이다. 30,000 으로 다시 만들면
      **사람이 이미 분류한 조각 1,034장이 격자에서 사라진다.** `reid_chips` 가
      값을 `items.json` 에 적고 다르면 멎게 해 두었지만 **어느 쪽이 옳은지는
      실측할 일**이다 (`reid.py` 주석: "이 값은 출발점이지 답이 아니다")
- [ ] **개체 분류를 이어간다** (`/reid` · **`FIN_REID=reid/v2`**). 격자 4,059 중
      상자에 든 것 **530** · 개체 **42** · 날을 건너뛴 개체 32 · 안 넣은 것 3,2xx.
      `묶음 제안` 으로 한 번에 여러 장씩 넣을 수 있다(5장이면 top-5 80%).
      **늘릴 것은 둘 다다** — 개체 수(개체군 100~120까지)와 개체당 조각 수
      (4~9장 11% · 20장 이상 34%).
      **좌우가 갈려 못 재는 짝**은 여전히 있다 — 개체마다 한쪽만 모으면 그만큼
      못 쓴다
- [x] ~~**재는 것을 명령으로 뽑는다**~~ — `reid_eval` (2026-08-24).
      자리 둘(좁은 후보·격자 전체)에 `후보` 열과 `--as-of` 가 붙었다.
      옛 표가 `--as-of 448` 에서 재현되어 **같은 것을 재고 있음이 확인됐다**
      (`뒷날 곡선` 한 줄만 16.3 → 19.8 로 갈린다 — 지금 것이 `reid.distance`
      에 묶인 쪽이다)
- [ ] **`ResNet18 · 실루엣만` 줄을 되살린다.** 옛 표의 29.1% 자리인데 지금
      표에 없다 — ImageNet 가중치를 새로 통과시켜야 하는데 m710q 에 캐시가
      없다. **무늬 때문인지 모양 때문인지를 가르는 줄**이라 값이 있다
      (지금은 `조각 41.9` 대 `실루엣 29.1` 로 무늬 쪽이 낫다고만 알고 있다)
- [x] ~~**전체에 돌리기 전에 `edges`·`base_partial` 을 어떻게 할지 정한다**~~
      — 재 봤다 (2026-08-24). **그 둘은 안 막고 있었다**: `usable()` 이 판정이
      없으면 `edges` 를 `"both"` 로, `base_partial` 을 빈 값으로 읽어 통과시킨다.
      실제로 막는 것은 `facing`(→ `propose_facing`)과 `cls`(→ 분할 엔진)였다.
      섞어 보니 **후보가 2.6배가 됐는데 첫 정답 순위는 2.0배만 밀렸다**
      (39위 · 비례라면 52위). 걸러지지 않은 조각이 옛 것과 구별되지 않는다
- [x] ~~**조각 만들기를 명령으로 뽑는다**~~ — `reid_chips` (2026-08-24).
      옛 스크립트를 **화소까지 재현한다**(1,735/1,735 동일 · 임베딩 코사인
      1.00000). 그 덤으로 `emb.npz` 가 학습 없는 ImageNet 특징이라는 것이
      정황이 아니라 계산으로 확인됐다
- [ ] **`bundles/v1` 을 다시 뽑는다.** 지금 묶음에 든 분할이 `seg-v2` 다 —
      `--seg-run 41` 로 하면 v3-s(20MB)가 들어간다. DolfinDesktop2 가 그것을
      받아 쓴다
- [ ] **표본을 넓힌다 — 다만 길이 하나뿐이다.** 옛 운영 DB 를 날짜별로 펴
      보니 관찰일 **255일 · 사진 544,585 · 상자 999,528** 인데 `detect-v2` 가
      쓰는 것은 **28일 · 4,240장 = 0.78%** 다. 그런데 **안 쓴 227일에 사람이
      그린 상자가 하나도 없다** — 749개 전부 이미 썼다. 완전 라벨의 공급이
      끝났다는 뜻이라, 남은 길은 **새 검출기로 훑고 사람이 보는 것**뿐이다
      (`infer_boxes` → `새 검출`). 문턱별 정밀도를 이제 알아서 얼마나 깊이
      훑을지 정할 수 있다: `≥0.40` 87.3% · `0.25~0.40` 64.4% ·
      `0.15~0.25` 53.9% · `<0.15` 39.4%.
      **날을 넓히되 날마다 얕게** — 하루 만 장짜리 날을 통째로 넣으면 그날
      바다 상태를 외운다. 날짜별 표는
      https://claude.ai/code/artifact/e4f94596-f308-4b7d-a65c-663c240fae75
- [ ] **사진을 미리 1280 으로 줄여 둘지 정한다.** 지금은 NAS 원본(장당 9MB ·
      4928×3280)을 심볼릭 링크로 걸고 매 에폭 읽는다. 1280 으로 줄이면
      4,240장이 38GB → **약 1.3GB** 이고 디코드 비용도 함께 준다. 라벨은
      정규화 좌표라 손댈 필요가 없다. **표본을 수만 장으로 넓힐 때 이것이
      먼저다.** WSL 안에 두면 `ext4.vhdx` 가 그만큼 커지고 저절로 안 줄어드니
      D 드라이브 쪽이 낫다
- [ ] **`base_partial=unknown` 60건을 비운다.** 축을 넓히기 전 참/거짓으로
      찍힌 것이라 앞·뒤가 없다. re-ID 용이라 급하지 않다
- [ ] **가끔 편집 창(확대)에서 몇 장 열어 볼 것.** 표류는 발산하지 않지만
      **사람이 알아보는 문턱**에서 멈추고, 그 문턱을 정하는 것은 검토 배율이다.
      notch 는 작은 흠이라 하필 그 문턱 근처에 있다
- [ ] **자료를 늘린다.** `dolphin` 86 · `nonfin` 177 이 모자라 그 두 클래스가
      평균을 끌어내린다. 배경 크롭도 4.6%(권장 10%)다. **모델 용량은 천장이
      아니다** — `s`(9.7M)와 `m`(22M)이 같은 자리에서 멈췄다
- [ ] **두 번째 바퀴는 `fix` 로 재지 말 것.** 2차의 `fix` 는 1차에서 그대로
      물려받은 값이라 **2차의 일이 아니라 1차의 값을 되비춘다.** 실제로 1차
      77%에 2차 87%가 나왔는데 그 87%는 허수다. `밑동 그림` 도 같다 — 사람이
      찍었던 것을 잃지 않으려고 계속 실어 보내는 것이다(`base_human`).
      **옳은 자는 "이번에 값이 바뀐 상자가 몇 %인가"** 이고 지금 4%다.
      1차는 빈손에서 판정을 만드는 일이고 2차는 이미 있는 판정을 새 마스크에
      대고 확인하는 일이라, **같은 자로 잴 수 없다**
- [ ] **바퀴마다 넓이 비를 본다** — 사람이 **이번에 다시 그린** 것의
      (사람 넓이/모델 넓이). 1차 SAM2 에서 중앙값 0.945 로 모델이 넓게 땄다.
      2차는 아직 7장뿐이라 못 잰다 — 30장쯤 쌓이면 볼 것.
      1 에 가까우면 그 버릇을 안 물려받은 것이다 (`devlog/…_005` 4절)
- [x] ~~**`m710q` 를 쓸 수 있게 맞춘다**~~ — 2026-08-24 에 복원해 돌아간다
      (`fin.db`·크롭 3,005·조각 3,479 · 원본 사진은 `/nas` 심볼릭 링크).
      **NAS 경로가 기계마다 달랐다** — 거기서는 `/nas/JikhanJung` 이고
      `/mnt/p` 로 박혀 있어 `backup` 이 "NAS 가 안 붙어 있다" 고 잘못 말했다.
      뿌리를 `finseg/nas.py` 한 곳으로 뺐다.
      아래 것은 그대로 남는다 —
      **`fin.db` 는 한 기계에서만 연다** — 합치는 길이 없다.
      `git pull` 이 그쪽 옛 `crops/`·`db/fin.db` 를 지우는데 **지워져도 된다**:
      히스토리의 그때 `fin.db` 를 꺼내 보니 판정이 **0건**이었다.
      (전에 "판정 717건이 있다" 고 적혀 있었지만 그 717은 `devlog/…0816_003`
      의 *이 기계* 진행 상황이 옮겨 붙은 것이었다)
- [ ] **크롭을 git 히스토리에서 뺄지 정한다.** **되돌리기가 한 방향이다** —
      JPEG 은 델타 압축이 안 돼 팩이 그대로 154MB 이고, `git rm --cached` 를
      해도 히스토리에 남는다. `git filter-repo` + force-push 라야 하고 그때는
      다른 클론을 전부 다시 받는다

- [ ] **DolfinDesktop2 에 Qt 화면을 붙인다.** 지금은 `python -m dolfin` 으로만
      돈다. 폴더 고르기 → 진행줄 → 결과 목록 → `scan.json`.
      `.guides/desktop/` 의 표준을 따르고, **패키징 전에 `file-locations.md`
      부터 읽을 것** — `AppId` 를 한 번 잘못 정하면 되돌릴 수 없다
- [ ] **일반인용 `/detect` 를 `dolfinserver` 로 옮긴다.** 여기 것은 PoC 다
      (`CLAUDE.md` 의 "이 저장소가 하지 않는 것"). 옮길 때 정할 것 둘 —
      모델 37MB 를 아무나 받는다는 것, 그리고 화면이 기대치를 말해야 한다는 것
      ("등지느러미만 찾는다 · 끄트머리만 보이는 것도 찾는다")

## GCP 로 나가기 (`dolfinid2`) — **역할을 나눈다**

> **자리가 넷이다** (2026-08-26): m710q 운영(`work` · 8085) · GCP 운영(`reid`) ·
> m710q 시험(8086, NAS 백업 사본) · `runserver`(개발). `fin.db` 는
> `/srv/dolfinserver2/db/` 에 있고 저장소 `db/` 는 안 쓴다.

**개체를 만들고 지느러미를 개체에 넣는 일은 GCP 에서만. 나머지는 m710q 에서만.**
설계는 `HANDOFF.md` 의 `## 서버를 둘로 나눈다`, 잰 것은 `devlog/20260826_001`.

- [x] ~~**`Review` 번호 공간을 가른다**~~ — **가를 일이 없어졌다** (2026-08-26).
      `/reid` 에서 "지느러미 아님" 을 쓰던 길을 걷어내 `Review` 의 주인도
      m710q 하나가 됐다. 거르는 일은 검토 화면의 `re-ID 후보` 대기열이 받는다
      (지금 **6,174장**, `p(fin)` 낮은 것부터). **며칠 뒤 지울 것**
- [ ] **`re-ID 후보` 6,174장을 훑는다.** 옛 상자에서 길어 온 조각이라 몸통·
      바위·물보라가 섞여 있고, `fin_filter` 가 그 2,321장이 분할 엔진의 분류를
      **전부 `fin` 으로 통과**했다고 적어 두었다. 앞쪽(`p(fin)` 0.0대)만 훑어도
      격자가 눈에 띄게 깨끗해질 것이다 — **GCP 로 내보내기 전에 하는 편이
      값이 크다**(거기 격자는 여기서 만든 것을 받는다)
- [x] ~~**보내는 길 (m710q → GCP)**~~ — 섰다 (2026-08-27).
      `export_from_work_to_reid` / `import_from_work_to_reid` /
      `deploy/gcp/from_work_to_reid.sh`. 27.7MB · 10초. **며칠 뒤 지울 것**
- [x] ~~**되받는 길 (GCP → m710q)**~~ — 섰다 (2026-08-27).
      `deploy/gcp/from_reid_to_work.sh`. 갈아 끼우기 전에 한 벌 뜨고, 줄어들면
      멎고, 가리키는 상자가 없으면 "보내는 길을 먼저" 라고 말한다.
      **며칠 뒤 지울 것**
- [x] ~~**역할을 배포에서 막는다**~~ — **배포가 아니라 앱에 넣었다** (2026-08-26).
      `FIN_ROLE`(`work`|`reid`)이 `review/urls.py` 의 `patterns_for` 로 길을
      가른다. **nginx 로 막으면 정작 새는 자리가 안 막힌다** — `runserver` 앞에는
      nginx 가 없는데 구멍이 거기다. 기본은 `work` 라 이 기계 습관은 안 바뀐다.
      시험 4종이 "403 이 나오나" 가 아니라 **"길이 아예 없나"** 를 잰다
      (`RoleTests`). **며칠 뒤 지울 것**
- [x] ~~**`/healthz`**~~ — 넣었다 (2026-08-26). 버전·**역할**·DB·상자·개체·
      개체판정·격자 크기. `reid` 자리에서 격자가 비면 **503** 이다.
      **며칠 뒤 지울 것**
- [ ] **`docker-compose.reid.yml`** — 사진·크롭 마운트 없는 판.
      GCP 로 가는 짐은 `fin.db` 34MB + `reid/v3` 중 **181MB** = 215MB
      (crops 703MB 를 넣으면 918MB). **사진은 안 간다** — 7,927장 74.3GiB 이고
      NAS 전체는 5.9TB 다. 화면이 읽는 것은 `items.json`·`emb-dinov2.npz`·
      `cls-dinov2.npz`·`look/` 넷뿐이라 `chips.npz`·`emb.npz`·`curves.npz` 는
      안 간다
- [x] ~~**백업 레인 — m710q**~~ — 세웠다 (2026-08-26). 시간별 ·
      `integrity_check` 게이트 · 실패 시 prune 건너뜀 + 센티넬 → `/healthz`
      `degraded` · 반출 위생 · 24벌 · 04시 회차만 NAS. **되돌려도 봤다**
      (시험 자리를 그 스냅샷으로 세웠다). 시험 8종. **며칠 뒤 지울 것**
- [x] ~~**백업 레인 — GCP**~~ — 세웠다 (2026-08-26). 그쪽 시간별 cron +
      m710q 가 05시에 당겨온다(`pull_gcp_backup.sh` — 받아서 **다시 검사**하고
      NAS 로 한 벌 더). 신선도 게이트 26시간. **며칠 뒤 지울 것**
- [x] ~~**GCP 자리·배포 스크립트**~~ — `deploy/gcp/` (install·ship·seed·
      bootstrap·deploy·smoke). tailnet 전용 nginx. **며칠 뒤 지울 것**
- [x] ~~**이미지를 레지스트리로 올릴지 정한다**~~ — 올린다 (2026-08-26).
      `honestjung/dolfinserver2` 공개 · `build.sh --push` · GCP 는 이제
      `deploy.sh` 가 당겨서 뜬다(`ship.sh` 는 레지스트리가 막힐 때의 뒷문으로
      남긴다). **며칠 뒤 지울 것**
- [ ] **호스트 파일도 이미지에서 꺼내게 한다** (`_extract_and_deploy.sh`).
      형제들은 그렇게 해서 운영에 저장소가 아예 필요 없다 — 그쪽으로 가면
      `deploy/gcp/install.sh` 가 은퇴한다. 이미지에 이미 `deploy/` 가 실려 있어
      꺼내 쓰기만 하면 된다
- [x] ~~**공개로 열지 정한다**~~ — 열었다 (2026-08-27).
      `https://reid.nopeoplestime.info` · certbot · `FIN_HTTPS=1` ·
      tailnet 8085 닫음. **며칠 뒤 지울 것**
- [ ] **문 앞에 사람이 아닌 것이 온다.** 이제 주소를 아는 누구나 `/enter` 까지
      온다. 지금 막는 것은 **주소 단위 10번에 15분 잠금**뿐이고, 그 셈은
      `LocMemCache` 라 **gunicorn 워커 둘이 각각 센다**(실질 20번). 값싼 보강
      둘: nginx 의 `limit_req` 를 `/enter` 에만 걸기, 그리고 코드를 더 길게.
      **긴 코드가 잠금보다 확실하다**
- [ ] **HSTS 를 켤지 정한다.** certbot 이 자동으로 안 켠다. 켜면 브라우저가
      http 로 아예 안 가지만, **되돌리기가 오래 걸린다**(브라우저가 기간만큼
      기억한다) — 이 도메인을 계속 쓸 것이 확실해진 뒤에 켠다
- [ ] **뒷날 곡선을 결각다운 값으로 바꿔 다시 잰다.** 좌표 64점을 DINOv2 뒤에
      이어 붙여 봤더니 **top-1 은 여섯 갈래 중 다섯에서 오르고 한 번도 안
      내렸는데(평균 +0.8%p) top-5 는 넷에서 내렸다** (`devlog/20260827_001` 10절).
      폭이 1~2문제라 아직 쓸 것이 못 되지만 **방향이 안 뒤집힌 것은 잡음으로
      보기 어렵다** — 지금 형태(좌표 64점)가 마스크 몇 픽셀에 통째로 흔들리는
      것이 문제로 보인다. 파인 곳의 **깊이·너비·개수**나 굽은 정도(도함수)로
      바꾸면 덜 민감해지고 차원도 준다 (정답 563조각에 128차원은 부담이다).
      **top-5 가 내리는 것을 특히 볼 것** — 화면이 내미는 것이 top-5 다
- [ ] **우클릭에 개체 제안 top-5 (확률과 함께).** 서버는 거의 다 돼 있다 —
      `/api/reid/suggest` 에 상자 하나만 넘기면 그대로 top-5 이고, `k` 를 받고,
      `prob` 이 참이면 그 수가 확률이다.
      **조건은 조각 수가 아니라 카탈로그가 개체군을 얼마나 덮느냐다** — 지금
      개체 45 인데 개체군이 100~120 이라, **카탈로그에 없는 개체면 다섯 개가
      전부 틀린다.** 그 상태로 붙이면 "제안이 자꾸 틀린다" 로 읽혀 기능 자체에
      대한 신뢰를 먼저 깎는다.
      붙일 때 둘을 함께 둘 것 — **`여기 없다` 를 한 번에 말할 자리**(top-5 가
      75% 라 넷 중 하나는 정답이 없다. 없으면 사람이 가장 그럴듯한 것을 고르고
      **그 순간 자료가 조용히 오염된다**), 그리고 **우클릭으로만 부르기**
      (늘 보이면 `사람이 판단하고 모델은 순서만 돕는다` 가 뒤집힌다)
- [ ] **짐작인 `facing` 을 표시하고 고칠 수 있게 한다.** 격자의 **6,177장이
      기하 짐작**이고(사람이 적은 것은 1,735), 틀리면 조각이 **거울로 뒤집혀
      틀린 쪽 무리에 조용히 섞인다** — `fliplr` 을 금지한 것과 같은 이유로
      re-ID 를 망친다. 사람이 적어 둔 2,673개에 대 보면 정확도 98.3%인데,
      **그것은 검토를 마친 크고 뚜렷한 상자들이라 낙관 쪽으로 치우쳐 있다.**
      고치는 길은 `지느러미 아님` 과 같은 수 — `방향 뒤집힘` 특수 상자
- [ ] **좌우반전을 아예 없앨지 정한다.** 안 뒤집으면 **`facing` 이 틀린 것이
      눈에 보인다**(지금은 뒤집혀서 틀린 무리에 자연스럽게 섞인다). 대신
      notch 가 좌·우에서 반대쪽에 오고 `curve_of`·`roughness`·임베딩이 그 위에
      서 있어 **성적이 이전과 비교 불가능해진다** — 하려면 격자를 v4 로 새로
      뜰 때 함께
- [ ] **누가 했는지가 여전히 안 남는다.** 밖에서 여럿이 분류하기 시작하면
      `Review.reviewer`·`Identification.reviewer` 가 전부 `NULL` 이라 **일치도를
      재려 해도 못 잰다**. 공개한 지금이 로그인을 붙일 값이 가장 큰 자리다
- [ ] **`manage.py backup` 을 계약 §4 에 맞출지 정한다.** 지금은 라이브 DB 를
      온라인 백업 API 로 뜬다(검사도 한다). 계약은 하위 트랙이 **검증된
      스냅샷**을 소비하라고 말한다 — 시간별 트랙이 생겼으니 그것을 읽게 바꿀
      수 있다. 다만 그쪽은 백업이라기보다 **기계 사이 인계**라 성격이 다르다
- [ ] **`/photo` 를 되살릴지 정한다.** 안 가져가면 "사진이 이 기계에 없다" 고
      곱게 죽는다. 되살리려면 **축소본이 답이다** — 긴변 2400px q82 로
      **3.4GiB**(원본의 4.6%), 1600px 이면 1.8GiB. `photo.html` 의 SVG 는
      `viewBox` 가 원본 좌표계라 **상자 좌표를 한 줄도 안 고친다**
- [x] ~~**`/`·`/review`·`/edit` 가 크롭 없이 200 을 낸다**~~ — `FIN_ROLE=reid`
      에서 셋 다 **404** 다 (2026-08-26 에 실측). 살아 있는 척하던 화면이
      사라졌다. **며칠 뒤 지울 것**

## 곧

- [ ] **키포인트 모델을 다시 학습시킨다.** 지금 가중치는 라벨 791장으로 배운
      것이고 이제 1,509장이다. `export_pose` → `train` → `infer_base --redo`
- [ ] **옛 검출기가 무너지는 날 넷을 나란히 본다** (2016-07-20 51% ·
      2018-10-17 30% · 2018-08-19 27% · 2017-08-28 16%). 하루가 아니라 넷이니
      "어떤 조건에서 무너진다" 쪽이다. 다만 그 넷은 **정밀도**가 무너지는
      날이고, 전체로는 헛것이 3.5%뿐이라 새 검출기의 값은 **재현율** 쪽에서
      나와야 한다
- [ ] **`close_mosaic` 을 줄인다.** 마지막 10에폭이 **두 run 다 값을 안 했다** —
      `detect-merged` 는 111~120이 108의 최고를 못 넘었고 `detect-v2` 도 같다.
      다음 학습에서 5나 0으로 줄여 볼 것
- [ ] **`mosaic` 을 `val_date` 에서 다시 잰다.** 대조 실험에서 차이가 없었지만
      (mAP50 +0.006 · 재현율 −0.006) 그것은 `val` 기준이고, `val` 은 학습과 같은
      날들이라 **모자이크의 진짜 값 — 처음 보는 배경에 대한 일반화 — 을 못
      잰다**.
      (`close_mosaic` 쪽은 위 항목으로 갈라 두었다)
- [ ] **`교정 대기`·`윤곽 없음` 을 0 으로 유지한다.** 지금 둘 다 0. 한때 75건까지 갔는데
      그것은 사람이 찍은 교정이 조용히 사라지고 있다는 신호였다 —
      **이 대기줄이 그 손실을 잡아냈다** (`devlog/…_004` 4절)
- [ ] **시험을 더 넓힌다.** 131개. 판정 규칙·좌표 사상에 더해 **저장 경로와
      대기열**을 덮었다 (`review/tests.py` 의 `NewBoxQueueTests` — 마스크가
      없으면 `verdict` 를 안 적는 것과 있으면 적는 것을 **짝으로** 잰다).
      그 뒤로 `보류`·`윤곽 없음`·`export_detect` 의 판정 읽기·`label_of` 의
      사람 윤곽·`onnxdet` 의 전처리와 NMS·`import_detections`·개체 분류,
      그리고 **`reid_eval` 의 순위 계산과 `catalog(as_of=)`** 까지 덮었다
      (자가 틀리면 표는 멀쩡한 채로 방법을 잘못 고르게 한다).
      아직 안 덮인 것: `export_yolo` 의 크롭 클리핑과 제외 규칙,
      `import_boxes` 의 이웃 폐포·중복 거르기, 그리고 **브라우저 쪽 전부**.
      `polyMoved` 가 빠진 것을 **글자로**(템플릿을 정규식으로 훑어) 잡고 있는데
      이건 성긴 대용품이다 — **브라우저를 실제로 돌려 볼 자리가 필요하다.**
      `itemOf`·`sigOf` 같은 순수 함수부터 뽑아 덮는다 (`devlog/…0816_004` 4·5절)
- [ ] `--pad` 를 바꾸면 이웃 목록이 어긋날 수 있다. 내보낼 때 한 번 더 막지만
      경고를 띄우는 편이 낫다
- [ ] **`other` 가 늘면 또 쪼갠다.** 오늘 새·몸통·머리·가슴지느러미·사람을
      그렇게 뽑아냈다. **내보내기 전에** 할 것 — 그 뒤로는 `none` 앞에
      덧붙이는 것만 된다

- [ ] **백업을 자동으로 돌린다.** 지금은 손으로 `manage.py backup` 을 부른다.
      옛 운영 DB 는 NAS 에서 날마다 01:00 에 뜨는데(`dolfinid_backup/`)
      **정작 다시 만들 수 없는 `fin.db` 가 그 레인 밖이었다.** cron 이든
      systemd timer 든 하루 한 번은 돌아야 한다.
      이름이 기계별로 갈렸으니 **두 기계에서 함께 돌려도 안 부딪친다**
- [x] ~~**복원을 한 번 해 본다**~~ — 2026-08-24 · m710q. sha256 이 MANIFEST 와
      맞고 `integrity_check ok`, 판정 6,014·마지막 id 6,051 이 그대로였다.
      화면까지 봤다: `/`·`/reid`·`/photo`·`/edit`·`/compare` 200 이고
      **이미지 세 갈래**(크롭·원본 사진·조각)가 다 나온다.
      크롭·조각은 40장씩 골라 sha256 을 견줬다 — 다른 것 0.
      **확인 안 한 백업은 백업이 아니다**

## 멀티유저로 갈 때

- [ ] `rules.effective_review()` 를 합의 규칙으로 바꾼다 (사람마다 최신 → 다수결
      또는 신뢰가중). **부르는 쪽은 안 바뀐다** — 그러라고 함수로 뽑아 두었다
- [ ] 같은 상자를 여러 사람이 보게 배정하고 **일치도를 잰다**. 그것이 곧 판정
      규칙이 명확한지에 대한 답이다
- [ ] 로그인·계정. `auth` 는 이미 켜 두었다.
      **접속 코드(`FIN_ACCESS_CODE`)가 지금 그 자리를 대신한다** — 문은 되지만
      인증은 아니다: 누가 했는지가 안 남고(`Review.reviewer` 가 `NULL`),
      한 사람만 뺄 수 없다. 여럿이 분류하기 시작하면 **일치도를 재려 해도
      누가 했는지가 없어서 못 잰다** — 아래 항목이 그것에 걸린다
- [ ] `.guides/web/` 의 배포 다섯 동사와 백업 레인 채택.
      **`fin.db` 는 사람의 판정이라 다시 만들 수 없다**.
      **이제 멀티유저 조건이 아니다** — GCP 가 수집을 받으면 거기가 주인이
      되므로 그 전에 서야 한다 (`## GCP 로 나가기` 의 백업 레인 항목)

## 나중

- [ ] **검출기를 실제로 갈아 끼운다.** 새것이 옛것을 이기면 `import_boxes` 가
      옛 상자 대신 새 검출기를 부르게 한다. 그래야 **재현율의 천장이 풀린다** —
      지금은 옛 YOLOv5 가 못 본 지느러미를 이 루프 안에서 표시할 방법이 없다
- [ ] **`detect` 자료도 사람의 검토로 늘린다.** 지금 완전 라벨은 사람이 상자를
      채워 넣은 6일뿐이다. `/photo` 에 **상자 추가**를 붙이면 그 자리에서
      늘릴 수 있고, 그것이 재현율을 재는 유일한 길이다
- [ ] **re-ID.** 뒷날 notch으로 개체 매칭. 밑동 현이 정규화(회전·크기)의 기준이
      된다. 좌현·우현을 갈라야 한다 (**임베딩 학습에 `fliplr` 을 쓰면 안 된다**).
      ~~옛 DB 의 JTA### 가 씨앗~~ — **아니다**: 이름 165종 · 상자 277개인데
      2개 이상인 60종이 **전부 같은 날**이고(날을 건너뛴 것 0종) 15,000px²
      이상은 8개뿐이다. 정답은 우리가 만드는 수밖에 없다
- [x] ~~dockerize~~ — 2026-08-26 완료. `deploy/` · 시험 서버
      `http://m710q:8085` (`devlog/20260826_001`). **며칠 뒤 지울 것**

## 안 하기로 한 것

- **연속 프레임 대조학습** (`reid_train`). 재 보니 **열린 판에서 기준선을 못
  넘는다** — InfoNCE 31.7% · batch-hard triplet 35.9% 대 학습 없는 ImageNet
  37.8%. 여백·에폭·차원을 다섯 갈래로 훑어도 같고, 배우는 개체를 5~30으로
  흔들어도 안 오른다.
  **애초에 묻는 것이 우리 문제가 아니었다** — 대조학습은 *처음 보는 개체*를
  알아보라고 배우는 것인데, 개체군이 100~120이라 개체 수의 천장이 개체군
  크기다. 우리가 묻는 것은 "카탈로그의 몇 번인가" 이고, 그러면 **분류**가
  맞는 자다 (닫힌 판에서 kNN 25.5% → 분류기 **44.5%**).
  **대신 하는 것**: 사람이 최대한 많이 분류하고, 그 정답으로 얼린 특징
  (DINOv2·ResNet) 위에 분류기를 배워 **큰 정답지를 만든다**. 명령은 `reid_cls`.
  `reid_train` 은 지우지 않고 머리에 이 사연을 적어 두었다 — 그 문서에 재어 둔
  것들(연속 프레임 짝의 신뢰도, `fliplr` 금지)이 다른 자리에서 쓰인다

- **밑동을 수면선으로 자동 제안하기.** 되기는 하지만 **답이 아니다** — 수면선은
  자세에 따라 움직여서 re-ID 정규화 기준이 개체가 아니라 그날 물결을 재게 되고,
  하필 SAM2 가 가장 흔들리는 자리이며, 무엇보다 **그럴듯한 오답이라 사람의 눈을
  끌고 간다**. 이유 셋은 `devlog/…_001` 3절, 앞선 두 번의 자동 탐지 실패는
  `finseg/baseline.py`
- **중간값이 판정으로 저장되는 순환.** 날(`e`)이 값 넷을 순환했는데, 목표에
  닿으려면 중간값 둘을 반드시 지나야 하고 그것이 판단이 아닌데도 저장됐다.
  **묶는 시간을 늘려 가리려던 첫 고침이 틀렸다** — 천천히 누르면 그대로 쌓이고,
  한 번 저장된 뒤에는 원래 값으로 돌아오는 것조차 정말 보내야 하는 변경이 된다.
  값을 고르게 하는 것이 답이었다 (`devlog/…_003` 3절).
  **다만 순환 자체가 금지는 아니다** — 앞쪽(`f`)은 값이 셋이고 기본이 "모름"
  이라 왼쪽은 한 번, 오른쪽은 붙여서 두 번이라 저장이 묶인다. 값이 적고 목표가
  한두 번 안에 잡히면 순환이 더 빠르다. 가르는 것은 순환이냐가 아니라
  **중간값이 판정으로 남느냐**다
- **검출과 분할을 한 모델로 합치기.** 원본을 640으로 줄이면 지느러미가 16px 이
  되고 **notch 는 그 그림에 아예 없다.** 검출은 낮은 해상도로 충분하고 분할은 크롭
  해상도가 필요하므로 **2단이 맞다** — 지금 구조가 이미 그것이다
- **`edges` 로 크롭 버리기.** 되돌렸다 (`finseg/rules.py` 2026-08-16 항목).
  끄트머리만 보인다는 건 앞에 온전한 지느러미가 있다는 뜻이라 **가장 좋은
  표본을 골라서 버리는 규칙**이었다. 다시 넣으려거든 그 항목부터 읽을 것
