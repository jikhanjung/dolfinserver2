# devlog 색인

개발 로그·계획 문서 색인. **파일명 slug 자체가 요약**이라 디렉토리 listing 이
1차 인덱스이고, 이 문서는 번호 규칙과 주의사항을 더한다.

## 명명 규칙

- **작업 기록**: `YYYYMMDD_{nnn}_{title}.md` (001부터 단조 증가)
- **계획 문서**: `YYYYMMDD_P{nn}_{title}.md` (P01부터)
- **리뷰 문서**: `YYYYMMDD_R{nn}_{title}.md` (R01부터)

**본문은 무엇을 했는지보다 왜 그렇게 했고 무엇을 버렸는지를 적는다.**
버린 안과 그 이유가 남아야 같은 실패를 다시 시도하지 않는다.

## 기록

| | |
|---|---|
| [001](20260815_001_저장소_구성과_자료_경로.md) | 저장소 구성 · 자료 경로 · Django ORM 전환. **삽입점 자동 탐지 실패 기록이 여기 있다** |
| [002](20260815_002_2080ti_환경과_규칙_표시_그리고_시험.md) | 2080ti 환경 · 검토 화면에 규칙 띄우기 · 첫 시험 26개. **sam2 설치와 bf16 오답 기록** |
| [001](20260816_001_첫_바퀴_검토_시작과_검토_화면_세_고침.md) | 첫 바퀴 검토 456장 · `fix` 87% · 검토 화면 세 고침. **수면선 제안을 버린 이유와 `Enter` 두 번 사고** |
| [002](20260816_002_검토를_쓰면서_드러난_것들과_스키마_두_곳을_뒤집다.md) | 분류 8개로 쪼갬 · 윤곽 편집기 · 되짚기 · NAS. **`edges` 로 크롭을 버리던 것을 되돌린 이유** |
| [003](20260816_003_순환을_없애고_되짚기를_즉시_저장으로.md) | 되짚기 즉시 저장 · 꼭짓점 상자 선택 · 사진 확대. **날(`e`) 순환을 없앤 이유와 틀린 첫 고침** |
| [004](20260816_004_키포인트_모델과_앞쪽_축_그리고_잃을_뻔한_교정.md) | 키포인트 모델 · 앞쪽 축 · 사람 분류. **교정 76건을 잃었다 되살린 기록과 기하 규칙 예측 셋이 빗나간 실측** |
| [005](20260816_005_첫_학습과_자기채점을_알아챈_것.md) | 첫 학습(s·m) · `--group` · `/compare`. **정답의 92%가 SAM2 자기 출력이었다는 것과 그리기 없이 표류를 재는 법** |
| [001](20260817_001_두_번째_바퀴가_숫자로_돌았다.md) | 엔진 교체 · 2차 검토 · v2 학습 · 옛 운영 DB. **자기채점 열과 독립 열이 반대로 움직였다** |
| [002](20260817_002_검출로_넘어가며_같은_함정을_세_번째로_만났다.md) | 5단계 시작 · `export_detect`·`eval_detect` · 옛 학습 자료를 찾음. **`val_date` 가 옛 모델의 학습 날이었다** |
| [001](20260818_001_공평한_날에서_재고_정밀도를_물을_화면을_냈다.md) | 공평한 날에서 잼 · `infer_boxes` · `새 검출` 대기열 · **화면·저장 첫 시험**. **옛것이 놓친 109개 중 84개를 찾는다 · `fin.db` 는 표본이라 옛 상자를 다 갖고 있지 않았다** |
| [001](20260824_001_검출_한_바퀴를_닫고_모델을_밖으로_내보냈다.md) | 새 검출 690 전량 검토 · `export_detect` 가 `fin.db` 를 읽는다 · `detect-v2` · ONNX·브라우저·묶음. **조용히 새던 다섯 곳과 NAS 를 날짜별로 편 결과 — 완전 라벨의 공급이 끝났다** |
| [002](20260824_002_다른_기계로_옮겨_앉고_재는_것을_명령으로_뽑았다.md) | m710q 로 옮겨 앉기(NAS 경로가 기계마다 다르다 · 복원을 실제로 해 봤다) · `reid_eval` 로 곡선 대 전체 모양 다시 재기. **정답이 늘면 두 자리가 반대로 움직인다 — `후보` 열과 `--as-of` 가 그 자리다** |
| [003](20260824_003_옛_검출기_상자_100만_개에서_후보를_길어_왔다.md) | 옛 상자 100만 개에서 후보 2,324장을 길어 격자에 섞기 · `reid_chips` · `FIN_REID`. **검토 안 된 상자에서 막는 축은 `facing` 하나였고, 넓이 문턱이 코드(30,000)와 자료(15,000)에서 갈려 있었다** |
| [004](20260824_004_묶어서_묻기로_바꾸고_자를_닫힌_판으로_고쳐_잡았다.md) | `reid_probe`(InfoNCE·triplet) · DINOv2 · `/catalog` · 묶어서 묻기. **열린 판이 아니라 닫힌 판이 이 과제의 자다 — 개체군이 100~120이라 개체 수의 천장이 개체군 크기다** |
| [001](20260825_001_닫힌_집합_분류기가_기준선을_두_배로_넘었다.md) | `reid_cls` 닫힌 집합 분류기(25.5% → **44.5%**) · 화면이 확률로 후보를 낸다 · `fin_filter`(전체로는 지지만 엔진이 놓친 몸통·바위를 짚는다) · 우클릭 차림표·뱃지. **자를 세 번 잘못 댔고 셋 다 자료가 든 규칙을 안 읽어서였다** |
| [001](20260826_001_화면을_컨테이너에_담고_판정이_돌아올_길을_먼저_쟀다.md) | 검토 화면을 도커로(157MB · torch 없이) · `/srv/dolfinserver2` 시험 서버 · nginx 8085 · GCP `dolfinid2` 로 가는 길을 재기. **사진 74.3GiB 를 재고 버렸다(NAS 전체는 5.9TB) — re-ID 만이면 215MB 다 · 병합에서 46행짜리 `Individual` 이 6,039행짜리보다 어렵다**
| [001](20260827_001_두_자리가_서로_주고받고_개체가_아닌_자리를_만들었다.md) | 밖으로 열기(https·접속 코드) · **두 레인**(`…from_work_to_reid`/`…from_reid_to_work`) · 하루 한 번 주고받기 · **개체가 아닌 자리 둘**(`kind`) · `/reid` 윤곽·밑동·찍힌 차례 · **`/dataset` 자료 상태와 1차 목표** · 판정에 때와 자리(`ip`) · **`/catalog` 에서 번호를 붙이는 길**(`JTA###` 는 공식 ID 라 미리 안 박는다) · 0.16.0~0.19.1 여섯 판 · **`reid_cls --folds`(자를 돌린다)** · **백본을 키우러 갔다가 줄이는 것이 이겼다**(PCA 256 + L1 1e-5 로 +3.2/+2.5) · top-10 · **그러고도 갈아 끼우기를 접고 `찍힌 차례로` 로 길을 정했다** · 배포 중 안내 화면(502 포함) · `rough` 를 스냅한 윤곽에서 다시 잼 · **연구원이 하루에 번호를 0 → 31 붙였다**. **`INSERT OR REPLACE` 가 CASCADE 로 판정을 지운다 · 윤곽이 절반에서 거울상이던 것은 규칙이 두 곳에 있어서였다 · `gate.client_ip` 이 위조할 수 있는 헤더 자리를 읽고 있었고 그것이 접속 잠금의 열쇠였다 · 여러 줄 `{# #}` 는 Django 주석이 아니라 563번 찍혀 페이지의 절반이었다 — 두 번째라 시험으로 못 박았다 · **L2 는 `wd 0` 과 `wd 1e-1` 이 같아 아무 일도 안 하는데 축을 죽이는 쪽(PCA·L1)은 든다** · PCA 축을 전부로 잡으면 재는 날이 새어 top-1 이 1.2%p 부푼다 · **성적 48.1%는 카탈로그가 답을 들고 있을 때의 값인데 그것이 개체군의 38%뿐이라 화면에서는 18%다 · notch 가 조각에서 2~5px 라 ViT 의 14px 패치보다 작다 · **두 창을 쓰면 안 보이는 선택이 다음 드롭에 딸려 갔는데, 쌓아 둔 `Identification` 이 그 자취를 들고 있어 잡혔다** · 설치된 nginx 설정이 저장소본보다 낡아 있었다** |
| [002](20260827_002_같은_개체군_선행연구와_우리_자를_맞춰_봤다.md) | **Kim et al. (2024) — 같은 제주 개체군을 든 선행연구**와 우리 파이프라인을 맞춰 봤다. 골격이 거의 같고 갈라지는 자리가 셋(배경 제거·닫힌 판·사람이 루프 안에). **합성 데이터는 안 하기로 했다 — 그 이득이 열린 판에서 측정된 것이고, 우리는 실물 400,309개를 아직 안 캤다** · 개체 수가 다르면 같은 top-k 가 아니다(42 대 79) · **1차 목표(개체 60 · 조각 1,200)의 근거 절반이 여기서 나왔다** |
| [003](20260827_003_뒷날_형태를_키로_쓰려다_윤곽이_천장임을_알았다.md) | **뒷날 형태를 벡터화해 개체를 가릴 수 있나** — 축·정렬·표현을 여섯 갈래로 바꿔 봤다. **전부 AUC 0.59~0.63 에 모인다.** 그런데 밝기 경계로 윤곽을 스냅하는 것 하나가 **+0.098**(0.527 → 0.625) — **막는 것은 자가 아니라 윤곽이다.** 마스크는 notch 를 지우는 게 아니라 **없는 요철을 만든다**(사람 윤곽의 157%) · `뒷삽입점은 안정적` 이라는 내 가정이 틀렸다(0.093 대 apex 0.054) · **여러 형식이 한 자리에 모이면 입력을 의심한다** |
| [001](20260829_001_격자를_쪽으로_나누고_넣을_때마다_통째로_다시_짓던_것을_걷었다.md) | **`/reid` 이 무거운 까닭이 그림이 아니라 다시 짓는 것이었다** — 조각 하나를 상자에 넣을 때마다 격자 7,912칸과 상자 목록을 통째로 `innerHTML` 로 다시 지었다. 쪽 나누기(200장씩) + 바뀐 것만 갈아 끼우기(`sync`)로 **DOM 마디 112,439 → 3,320 · 넣을 때마다 5,694ms → 50ms**. 날짜띠(`날짜·시간순` 일 때만) · 카탈로그 크기 슬라이더 기억 · `0.21.0` GCP 배포. **`sel` 은 쪽을 안 건넌다** — 안 보이는 선택이 딸려 가는 2026-08-27 의 사고가 창 하나 안에서 다시 열리는 자리였다 · **지은 글에 `sel` 과 `--nfs` 가 섞이면 `sync` 가 거짓말을 한다**(뒤엣것은 슬라이더를 밀 때마다 든 칸을 새로 짓게 했다) · 미끄러지는 창을 버렸다 — 쪽 번호가 "어디까지 봤나" 를 말해 주는 유일한 것이다 · **남은 첫 그림 2초의 3.26MB(75%)가 윤곽 폴리곤이다** |
| [002](20260829_002_사흘_동안_한_번도_안_돈_cron_과_대조군이_뒤집은_결론.md) | **주고받기 cron 이 사흘 내리 죽었는데 한 번도 성공한 적이 없다는 것을 사흘 뒤에 알았다** — `python3` 가 cron PATH 에서 시스템 것을 잡았다. `deploy/_venv.sh`(`import django` 까지 해 본다) + 상태를 `/healthz` 가 읽는다(`degraded`/200). **cron 과 똑같은 `env -i` 로 돌려 확인**. 그리고 **2층 MLP 를 조건이 걸린 자리에서 재고 졌다**(선형 47.2/73.4 대 MLP256 46.2/71.6, 같은 5000 에폭). 화면 분류기를 36마리짜리에서 59마리로 갈았다. **같은 crontab 의 다른 둘이 멀쩡히 돌아서 "cron 은 되고 있다" 로 보였다 — 부분적으로 도는 것이 아예 안 도는 것보다 알아채기 어렵다** · **400 에폭에서만 견주니 MLP 가 이겼는데 대조군이 뒤집었다 — 구조를 바꾸며 학습량을 안 맞추면 구조 탓을 하게 된다** · **대조군에서 진짜가 나왔다: 기본 에폭 400 이 덜 배운 자리였고(→2000, +2.3%p), 스크립트는 이미 2000 을 안내하는데 기본값이 400 이라 기록의 모든 성적이 덜 배운 것이었다** · `reid_cls --help` 가 `+2.4%p` 때문에 한 번도 뜬 적이 없었다 · **`우리 42마리` 가 박혀 있었다 — 하필 "숫자를 그대로 옮기지 말라" 고 말하는 문장이** |
| [001](20260831_001_뒷날의_0.641_이_짝을_여섯_배로_늘리니_0.568_이_됐다.md) | **띠 재분할이 시작 전에 막혔다** — 정답(사람 윤곽)의 꼭짓점 간격 7.9px 이 notch 너비(3~8px)와 같고, **크롭이 이미 원본을 1.87배 늘린 것**이라 `원본이면 30px` 이 틀렸다(현이 1,500px 인 상자 43개 · 개체 붙은 것 0). 그리고 **`20260827_003` 의 0.641 을 짝 6배에서 다시 재니 0.568**. 방향은 살고 크기가 반이 됐다. `reid_notch` 에서는 **`ic04` 가 `rough` 를 대신할 자로 섰다**(상관 +0.098 대 +0.019 · 사분위 단조). **계획서의 숫자를 자료에서 다시 세지 않았던 것이 그 항목이 며칠 산 이유다** · **표본이 작으면 크기 말고 방향만 믿는다** · **같은 날만 오르고 날 건너뛰면 안 오르는 것**이 되풀이되는 신호다(`rough` 에서 한 번, 현 크기에서 한 번) · 자를 안 남겨 다시 짜느라 네 시간을 썼다 → **`reid_edge` 로 남겼다** · 아침에 붙인 `--help` 시험에 **내가 걸렸다** |
| [002](20260831_002_개체당_조각_수는_지렛대가_아니었다.md) | **`20장 이상인 개체만` 으로 돌렸더니 49.5 → 59.9 인데, 개체 수만 맞춘 대조군이 60.7 이었다** — 10%p 는 전부 클래스가 67→22 로 준 것이고 개체당 조각이 보탠 것은 0. 10장 문턱에서도 같다. **그러면 `4~9장 11.1% → 10~19장 30.6%` 곡선의 인과가 뒤집힌다** — 조각이 많아서 쉬운 게 아니라 **쉬워서 조각이 많다**(눈에 띄는 개체가 더 자주 찍힌다). `reid.GOAL` 의 근거 절반이 그 읽기 위에 서 있었다. **`--as-of` 를 `reid_cls` 에도 붙여 2026-08-27 을 자릿수까지 재현했고**(개체 42 · 조각 563 · 44.9±0.1), 그 고정된 문제에서 epochs 400→2000 이 **+2.7%p** — 사흘치 중 처음으로 사과 대 사과인 숫자다. `/catalog` 에서 우클릭으로 조각을 다른 개체로 옮긴다. **거르면 늘 둘이 바뀐다**(오늘만 세 번째) · **규칙을 문서에 적어 두는 것만으로는 안 지켜진다 — 손잡이가 재는 명령마다 있어야 한다** |
| [003](20260831_003_얼린_백본이_천장이었다.md) | **`TODOs` 의 마지막 항목(백본 마지막 블록 녹이기)을 열었더니 자릿수가 달랐다** — 1블록 **+9.9%p**(폭 ±0.6) · 2블록 **+14.4%p**. 오늘 진 넷(ViT-B·2층 MLP·ArcFace 여백·뒷날 기하)이 **전부 특징을 안 건드리고 있었다**. **에폭은 값이 없다**(20→40이 폭 안) — **블록 수가 지렛대다**. **캐시가 이것을 CPU 에서 돌게 했다**: 얼린 앞 11블록은 매 에폭 같은 값이라 한 번만 계산하면 76→16.6ms/장(마지막 블록만 녹인 학습이 얼린 forward 와 거의 같았다 — forward 가 값을 다 먹고 있었다). 곁들여 **조각이 원본의 절반을 버리고 있었다**(현이 원본 186px 인데 조각에서 98px, 그걸 224로 늘려 넣는다 → `--chip-size 224` 는 **계산이 안 느는 변경**이다). **DINOv3 를 붙였다**(`dinov3s` 가 크기·차원이 같아 사전학습만 다른 대조) · `backbone.py` 한 자리. **밟을 뻔한 넷**: `--emb-only` 가 화면 정렬을 덮어쓴다 · `--emb-name` 기본이 ResNet 임베딩을 덮는다 · `--unfreeze` 가 백본을 박아 뒀다 · **`reid_cls` 에 `cuda` 가 한 줄도 없었다**. 그리고 **`pgrep -f` 자기-매칭에 두 번**(큐가 영영 안 끝나고, `pkill` 이 제 셸을 죽였다). **GPU 로 넘긴다** — 7·8절이 인수인계다 |
| [001](20260901_001_호스트를_안_읽고_없다고_했고_격자_둘은_짝이_아니었다.md) | **`dolfinid:` 를 안 읽고 "묶음이 없다" 고 했다** — m710q 에서 `ls` 하고 없다기에 문서를 고치고 커밋까지 했는데, 묶음은 08-31 부터 저쪽에 81MB 로 있었고 겹치는 파일은 **sha256 까지 같았다**. **자리가 셋인 저장소에서 한 기계에 없는 것은 "없는 것" 이 아니다** — 없다고 말하기 전에 **어디를 봤는지를 함께 말한다**. 진짜로 안 되어 있던 것은 **224 조각** 하나였다(*"여기서 만든다"* 고 적어 놓은 것). **그리고 224 를 뜨니 `v3` 와 짝이 아니었다** — 7,912 대 **7,888**, 24개가 적고 **그중 6개에 개체 판정이 붙어 있다**. 격자는 자료가 아니라 **그날의 사람 판정으로 거른 파생물**이라 뜬 날이 다르면 상자가 다르고, **그 차이가 조각 크기의 차이인 척한다**. 오늘 DB 로 `v3-128` 을 함께 떠 **`v3-128 == v3-224`**(7,888 · 정답 1,552)를 확인했다 — **대조군이 덤을 하나 냈다**: 빠진 24개가 크기 탓이 아니라는 것. `fin-public.db` 는 만드는 법이 저장소에 없어 규칙을 정해 다시 떴다(`VACUUM INTO` — **원본을 읽기 전용으로만 연다**) · `items.json` 은 뜬 날을 안 들고 있어 **디렉토리 이름 말고는 물을 데가 없다** · 화면이 보는 `reid/v3` 는 `--no-chain` 으로 안 건드렸다 · **`--chip-size 224` 는 아직 아무도 재지 않았다** |
| [001](20260902_001_얕은_조건의_결론_다섯이_깊은_조건에서_뒤집혔다.md) | **GPU 매트릭스 약 90칸 — CPU 에서 정직하게 재어 온 결론 다섯이 뒤집혔다.** 전부 같은 패턴이다: *얕은 조건(얼린 특징·얕은 녹임)에서 내린 결론은 깊은 조건에서 안 선다.* ViT-B·224·DINOv3·층별 lr 이 모두 **깊게 녹이면** 산다. **사전학습의 차이가 정확도가 아니라 "몇 블록까지 견디나" 로 나온다** (v2 는 6블록에서 무너지고 v3 는 계속 오른다). 최고는 **뒷날 크롭 × DINOv3-B 10블록 76.8**. **배치 128 이 −4%p** — 같은 에폭에서 걸음 수가 8분의 1이다(우리가 인수인계에 잘못 적어 보낸 줄이다). `reid_matrix.sh` 의 grep 필터가 Traceback 을 삼킨다 |
| [002](20260902_002_모르는_개체가_아는_개체들_사이에_떨어진다.md) | **거절 문턱 — 중심까지의 거리로는 안 갈린다** (AUROC 0.561 ± 0.040 · 반지름 정규화도 못 보탠다). 까닭이 문제의 본질이다: **카탈로그 밖 돌고래도 같은 종이라 아는 개체들의 영토 안에 떨어진다** — 개 사진 속의 자동차를 찾는 보통의 OOD 와 달리 **거의 똑같이 생긴 것들 사이에서 새 얼굴 찾기**다. softmax 와 갈리는 것은 **낮은 확신의 두 원인**(전부에서 멀다 / 둘 사이에서 헷갈린다)이고 대응이 다르다 |
| [003](20260902_003_선행연구_대조_문서를_다시_채점한다.md) | **`20260827_002` 의 평가들을 엿새 뒤에 다시 채점했다** — 그 문서는 안 고치고 여기서 채점한다. *"정직하게는 우리가 뒤에 있다"* 가 뒤집혔고(top-10 84.8 대 **95.3**), *"저쪽이 앞선 자리: 백본을 실제로 학습시킨다"* 는 **우리의 최대 지렛대가 됐다**(+27%p 의 대부분). *"개체당 장수가 병목"* 은 대조군이 뒤집었고, *"ArcFace 가 먼저 갈 자리"* 는 가 봤더니 **여백이 졌다** |
| [001](20260903_001_용량과_깊이가_고원을_만들고_두_시각의_앙상블이_그_위에_섰다.md) | **끝 그림 — 전체 조각 L + 뒷날 크롭 L 의 로짓 평균 89.2 ± 0.5**(top-10 97.3). 얼린 선형 49.5 에서 이틀에 **+39.7%p**. DINOv3-L 곡선에 **고원이 있다**(14~18 이 84.5~84.9 — 그 안의 순위는 잡음이라 계산이 싼 14 를 골라도 손해가 없다). **앙상블은 수가 아니라 상보성이다**(셋·넷 < 둘) — **크롭은 단독으로는 졌는데 멤버로는 +4.6 을 보탠다**. 다른 실수를 하기 때문이고, **단독 성적으로 멤버를 고르면 이것을 놓친다**. 가중치는 지렛대가 아니다(0.45~0.60 이 평평 — **비율에 둔감한 것 자체가 배포에 좋은 성질**이다). MegaDescriptor 얼린 실측(kNN 51.9 · 선형 64.3) |
| [001](20260905_001_화면이_앙상블을_읽게_하고_남은_하나를_2080ti_로_넘긴다.md) | **89.2 를 쟀는데 화면은 44.5 로 돌고 있었다** — 재는 것과 쓰는 것이 갈린 자리를 폈다. 화면이 **임베딩·머리 짝을 여러 벌** 읽고(`ensemble.json`) `reid.ens_logits` 하나로 합친다. **합치는 규칙을 한 곳에 둔 것이 요점**이다 — 89.2 는 일회성 스크립트가 낸 것이라 화면이 따로 구현하면 **다른 것을 내면서 그걸 모른다**. `reid_ensemble` 이 같은 함수로 채점하므로 저쪽에서 89.2 가 재현되면 화면이 내는 순위도 그것이다. **버린 것: 늘 표준화 걸기** — 한 벌에서 순위는 같은데 퍼센트가 **0.993 → 0.209** 가 됐다. 그래서 화면이 내미는 수를 `prob`·`rank`·`sim` 셋으로 갈랐고 **앙상블의 수는 확률이라 부르지 않는다**(온도 보정 전이다). `/reid` 툴팁에 top-5 와 **"여기 없을 수 있다"** 한 줄 · `갈래 N`. **남은 하나를 2080ti 로 넘긴다** — RTX 8000 과 같은 Turing 이라 **1~2시간 셈**(잰 것이 아니다), 메모리 5~6GB. **걸릴 자리는 격자다**: 화면은 `v3`(7,912)인데 챔피언은 `v3-224`(7,888) 라 **임베딩을 `v3` 기준으로 안 내면 파일이 와도 조용히 한 벌로 돈다** |
| [001](20260906_001_재던_것을_화면에_올리는_길을_뚫고_크롭_레시피를_얼린_자로_잡았다.md) | **①②를 받아 끝냈다** — `--fit-all --unfreeze` 가 머리(npz)와 녹은 블록(`.pt`)을 따로 남기고, `reid_chips --emb-only --deep` 이 그 블록으로 격자 전체를 **흘려서** 임베딩을 낸다(캐시하면 3.1GB). **접을 필요가 없었다** — `BB.split` 의 `tail` 이 내는 것이 곧 임베딩이라(`m(x)` 와 최대차 0.00e+00) 머리는 다시 선형 한 층이고 화면 코드가 안 바뀐다. **인수인계 한 줄이 자료 앞에서 두 줄이 됐다**: "격자가 안 맞는다" 는 차례 이야기였는데 **화소도 갈려 있었고**(128 대 224), 빠진 24개는 자료가 없는 게 아니라 **재판정된 비지느러미**였다(`--boxes --dry-run` 한 번). 성적은 전체 조각 **84.1**(저쪽 84.9 재현)인데 **뒷날이 67.7**(저쪽 84.0)로 안 섰다. **원인을 18분짜리가 아니라 3분짜리로 잡았다** — 얼린 선형이 대리자다(전체 55.6 대 크롭 41.4, 저쪽이 적은 −0.7~−3.0 밖이라 **학습이 아니라 격자**가 갈린다). 배율을 세니 **가로 5.3배**였고, `20260902_001` 3절의 "비율 무시하고 늘린다" 와 "notch 가 1.4~1.8배" 가 **같이 설 수 없는 두 문장**이었다 — 비율을 지키니 크롭이 전체를 넘는다(57.1 대 55.6). 다시 재니 **뒷날 67.7 → 87.6 · 앙상블 85.6 → 89.0**(저쪽 89.2 와 같은 자리). **멤버를 3.6%p 좋게 만들었는데 앙상블은 0.2%p 내렸다** — `20260903_001` 의 "앙상블은 수가 아니라 상보성" 을 반대쪽에서 다시 본 것이다(그때는 약한 멤버가 +4.6, 이번은 **센 멤버가 더 보태지 않는다**). **단독 최고가 84.9 → 87.6 이 된 것**이 이번의 실익이다. **버린 것**: 왼쪽 더 담기(0.7%p 앞서나 8문항 차이이고 **상보성을 깎는다**) · 224 출력(이득 없음) · 격자를 통째로 다시 뜨기(0.3% 위해 레인을 흔든다) |

## 계획

| | |
|---|---|
| [P01](20260902_P01_삼엽충으로_옮겨_보며_방법과_돌고래를_갈랐다.md) | **자매 과제(화석 삼엽충 사진 분류)로 무엇이 건너가나** — `20260902_001` 6절의 *"옮겨 갈 것은 숫자가 아니라 방법이다"* 를 실제로 시험했다. 셋으로 갈렸다: 그대로 건너가는 **방법 셋**(정렬이 모델보다 세다 · 화소 예산의 재배분 · 블록 수가 지렛대) · 이름만 바꿔 건너가는 **함정 아홉**(날 → 표본으로 가르기 · 좌우 → 보존 상태 · 마스크 밖 0 → 모암·백화 · `--as-of` → 동정 개정) · **건너가지 않는 설계 다섯**. 셋째가 이 문서의 값이다 — **닫힌 판 · `fliplr` 금지 · 크기 정규화**는 방법처럼 생겼는데 이 자료의 성질에서 나온 것이다(삼엽충은 좌우대칭이라 `fliplr` 이 되레 유효하다). 상류(모으기·상자)가 먼저라는 것과 기성 모델(Grounding DINO·SAM2·CLIP)을 어느 칸에 놓나도 적었다. **삼엽충 자료는 한 번도 안 봤다** — 예측이지 답이 아니다 |

## 다른 문서와의 관계

| | |
|---|---|
| `HANDOFF.md` | **지금** 상태 |
| `TODOs.md` | **앞으로** 할 일 |
| `devlog/` | **지나간** 것의 영구 기록 |

앞의 둘은 완료분을 며칠만 두고 지워 현행화한다 — 기록은 여기 남는다.
