# 원본 자료 — 저장소 밖에 둔다
*.db
*.db-wal
*.db-shm
*.sqlite3
# 옛 운영 DB 백업을 놓고 들여다보는 자리. **통째로 무시한다** — 972MB 짜리가
# 들어오고, 여기 것은 전부 다시 받을 수 있는 사본이다
db/

# 학습 자료·가중치·산출물
datasets/
runs/
weights/
*.pt
*.onnx
crops/
out/

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# 환경
.env

# 형제 devdocs 클론으로 가는 링크 — 이 저장소에는 커밋하지 않는다
.guides
# 저장소 안에 잘못 클론된 devdocs. 자리는 `..` 이고 링크는 `.guides` 다.
# 지우기 전까지 `git add -A` 에 딸려 들어가지 않게 막아 둔다
devdocs/

# 내려받은 것 — 저장소에 안 넣는다 (ort 26MB · 모델 37MB)
static/vendor/
static/models/
bundles/
reid/
