# 2080ti 에서만 — SAM2.1 추론과 YOLO 학습
#
#   pip install -r requirements.txt -r requirements-gpu.txt
#
# torch 는 드라이버에 맞는 판을 골라야 한다. CUDA 12.1 이면
#   --extra-index-url https://download.pytorch.org/whl/cu121
# CPU 로 시험만 할 때는
#   --index-url https://download.pytorch.org/whl/cpu
#
# **2080ti 는 Turing(sm_75) 이라 bf16 이 없다.** AMP 는 fp16 이어야 한다 —
# 그 판단은 finseg/management/commands/segment.py 의 autocast_dtype() 에 있다.
# `torch.cuda.is_bf16_supported()` 는 2080ti 에서도 True 를 낸다(에뮬레이션).
# 그래서 그것을 묻지 않고 **연산능력을 직접 본다.**
torch
torchvision
ultralytics
# 이름을 `sam2 @ ...` 로 적으면 안 된다. 저장소의 메타데이터 이름이 `sam-2` 라
# pip 가 이름이 어긋난다며 거절한다 (실제로 그렇게 막혔다).
sam-2 @ git+https://github.com/facebookresearch/sam2.git
# sam2 가 선언하지 않는다. `build_sam2_hf()` 가 부를 때에야 없는 것이 드러나므로
# **모델을 내려받는 첫 순간에 터진다** — 여기 적어 두고 함께 깐다.
huggingface_hub
